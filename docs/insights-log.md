# Insights Log

**Purpose**: Lightweight capture of technical insights discovered during development sessions. Acts as a triage inbox — when 3+ insights cluster around a topic, promote to a Learning Note or ADR.

**Promotion rules**:
- **3+ related insights** → Learning Note (implementation journey)
- **Architectural decision with trade-offs** → ADR
- **Recurring pattern or gotcha** → Add to CLAUDE.md or development-guidelines.md

---

## Insights

| Date | Milestone | Topic | Insight | Verification | Status |
|------|-----------|-------|---------|--------------|--------|
| Mar 7 | M16 | Skills/AutoTrigger | Skill descriptions with explicit TRIGGER phrases ("commit this", "build it", "create a PR") enable auto-matching without slash commands. The description field is the matching criteria — Claude compares user requests semantically against it. Adding natural-language trigger phrases covers how users actually phrase requests vs how the skill is named | Say "commit this" without /forager-commit — skill should auto-activate | Raw |
| Mar 7 | M16 | Skills/SubAgents | Sub-agents do NOT inherit skills from the main conversation. Must explicitly list skills in the agent.md `skills:` field. Skills load at agent start (not on-demand like main conversation). Only custom agents in .claude/agents/ can use skills — built-in agents (explorer, plan) cannot | Delegate to pre-implementation agent → verify it has service-check + prd-audit loaded | Raw |
| Mar 7 | M16 | Skills/ProgressiveDisclosure | Keep SKILL.md under 500 lines. Put reference material in separate files that Claude reads only when needed. Scripts in a `scripts/` folder execute without loading source into context — only output consumes tokens. This is the "table of contents vs full document" principle for context management | Check context usage with and without script — script output only vs inline command source | Raw |
| Mar 5 | M10.6.16 | Import/Architecture | The import pipeline assumes 1 input line = 1 parsed ingredient everywhere: `parseBatch()` returns N results for N lines, `RecipeImportPreviewView` maps by index, category assignments keyed by index, save creates 1 `Ingredient` per result. Splitting "X or Y" alternatives into multiple ingredients requires breaking this 1:1 contract across 4+ layers. The LLM prompt change is trivial; the plumbing is the real work. Documented as M10.7 idea in roadmap | N/A — future feature | Raw |
| Mar 4 | M10.6.16 | CoreData/Relationships | `computeIngredientMatches()` re-parses ingredient text through local regex, producing different normalized names than the AI parser used during import (e.g., "chopped green onion" vs "green onion"). Template lookup fails, but the `Ingredient.ingredientTemplate` Core Data relationship still points to the correct template with category. Fix: after re-match, if result has no category but `ingredientTemplate?.category` exists, carry it forward via `withCategory()`. Principle: always check linked relationships as fallback before declaring data absent — relationships are authoritative, re-derived data is not | Import recipe with AI → save → recipe detail shows categories for all ingredients | Raw |
| Mar 4 | M10.6.15 | Import/CategoryPreservation | `reMatchIngredient()` was removing explicit user category assignments when re-match returned nil category. Fix: only update categories on positive signal (re-match finds a category), never remove on negative signal (re-match returns nil). Same pattern fixed in `EditRecipeView.commitIngredientEdit()` by carrying over the previous template's category via `withCategory()`. Design principle: user-explicit state should never be overwritten by absence-of-data from automated re-parsing | Import recipe → categorize ingredient → edit text → verify category persists after re-match | Raw |
| Mar 4 | M10.6.15 | Parsing/Normalization | Added Phase 0c (parenthetical stripping) and Phase 0d (single-char artifact removal) to `IngredientTemplateService.normalize()`. Order matters: strip parentheticals before case normalization and pluralization to avoid confusing those phases. Phase 0d guards single-char "a" since it could be part of compound names. Renamed "Boxed & Canned" → "Pantry" with migration placed before the creation loop in DefaultSeeder so existing categories are renamed before new ones are checked | Import "shrimp (peeled and deveined)" → template name should be "shrimp"; "2 avocados" → "avocado" not "avocado s" | Raw |
| Mar 4 | M10.6.14 | SwiftUI/DataBinding | `EditRecipeView` re-parses ingredients from scratch via `IngredientMatchService` instead of using the existing `ingredient.ingredientTemplate` Core Data relationship. When the re-parsed name differs from the LLM-assigned template name (e.g., local parser → "garlic" vs LLM template → "garlic clove"), the match fails and category is nil. Fix: nil-coalescing fallback `matchInfo?.categoryName ?? ingredient.template?.category` preserves the established relationship | Open saved imported recipe → all ingredients show category green dots | Raw |
| Mar 4 | M10.6.14 | LLM/PromptDesign | LLM prompt had a gap: singular form was specified but unit/count words weren't explicitly excluded from ingredient names. Result: "2 cloves garlic" → `name: "garlic clove"`. Normalization Phase 0 only stripped unit words as prefixes, not suffixes. Fix: add explicit LLM prompt rule + suffix stripping in normalization as belt-and-suspenders. Parser/normalizer interface contracts need both sides to agree on name format | Import recipe with LLM → verify template names don't contain unit words | Raw |
| Mar 3 | M10.6.13 | CoreData/HouseholdKey | `findByCanonicalName` has no householdKey filter — it finds templates across all scopes but never updates their householdKey. Templates created before M10.6.11 retain `householdKey=nil` forever, making them invisible in household-scoped IngredientsView. Fix: update householdKey on found templates in both repository and service layers (belt-and-suspenders) | Import recipe in household → verify ALL templates have correct householdKey in debug log | Raw |
| Mar 3 | M10.6.13 | SwiftUI/StateRace | `dismissAfterSave()` calls `cancelImport()` (state → `.idle`) synchronously before SwiftUI `onChange` can catch the transient `.saved` state — both transitions happen in the same MainActor turn. SwiftUI batches state changes within a turn, so `onChange` only sees final `.idle`. Fix: explicit `onSaveComplete` callback invoked before state reset avoids the race entirely | Import from browser → save → verify both sheet AND browser dismiss | Raw |
| Mar 3 | M10.6.13 | Swift/Concurrency | `@MainActor`-isolated singletons (like `DebugLogService.shared`) can't be called synchronously from non-`@MainActor` code. Wrapping in `Task { @MainActor in }` is the correct fire-and-forget pattern for logging — log entries may appear slightly out of order but that's acceptable for diagnostics | Build succeeds with zero concurrency errors | Raw |
| Mar 3 | M10.6.12 | CoreData/ChildContext | Recipe import uses child contexts for atomic save. `findOrCreateTemplate()` has a fallback catch block that creates templates directly — this fallback was missing `householdKey`, `awakeFromInsert()` set nil householdKey when `Household.id` is nil in child contexts, and callers redundantly called `incrementUsage()` (which does `context.save()` per ingredient — fragile in child context flow). Fix pattern: ensure ALL code paths set householdKey, guard nil Household.id, and remove redundant saves. Child-context error paths are the #1 source of silent data bugs | Import recipe in household context → verify templates have correct householdKey and usageCount=1 | Raw |
| Mar 3 | M10.6.11 | Architecture/HouseholdScoping | `HouseholdIngredientTemplateRepository.findOrCreate()` never set `householdKey` on new templates. `IngredientsView` filters by `householdKey == currentHouseholdKey`, so templates created during import were invisible to household users (householdKey=nil vs expected UUID). Bug was hidden in simulator (no household → nil==nil passes filter) but surfaced on device with active household. Fix: provider closure pattern on IngredientTemplateService + RecipeImportService, same as LLMSettingsService.householdAPIKeyProvider | Import recipe on device with household → verify templates appear in IngredientsView | Raw |
| Mar 3 | M10.6.11 | Testing/SimVsDevice | Simulator testing without a configured household masks household-scoping bugs because `currentHouseholdKey` is nil and all entities default to nil householdKey — the filter always matches. Device testing with an active household exposes missing householdKey assignments. Lesson: always test household-dependent features on a device with an active household, not just simulator | Compare IngredientsView template count on sim (no household) vs device (with household) after import | Raw |
| Mar 3 | M10.6.10 | SwiftUI/StateObject | Views using `@EnvironmentObject` can't access those services in `init()`. To create a `@StateObject` that depends on an environment service (e.g., `IngredientAutocompleteService` needing `IngredientParsingService`), create a local instance from `PersistenceController.shared.container.viewContext` — stateless services like parsing work fine as separate instances | Build succeeds; autocomplete dropdown populates in RecipeDetailView and RecipeImportPreviewView | Raw |
| Mar 3 | M10.6.10 | SwiftUI/VisualFeedback | Binary status checks (`categoryName != nil`) conflate distinct states. Using `IngredientMatchResult.status` enum (`.ready`/`.needsCategory`/`.needsTemplate`) with distinct icons (green checkmark / amber dashed / gray plus+NEW badge) gives users clear, actionable feedback on what each ingredient needs | Visual: import recipe with mixed match states → verify three distinct icon styles appear | Raw |
| Mar 3 | M10.6.10 | Architecture/AutocompleteReuse | Same autocomplete dropdown pattern reused across 3 views but with different selection handlers: CreateRecipeView appends to form array, RecipeDetailView calls `recipeServiceM75.updateIngredient()` for Core Data persistence, RecipeImportPreviewView updates in-memory dicts. The view-specific handler is the only varying part — the dropdown UI and search service are identical | Test autocomplete in all three contexts: create, detail edit, import edit | Raw |
| Mar 2 | M10.6.9 | Import/CategoryPersistence | When the import save pipeline uses name-keyed category dictionaries (`parsedName.lowercased() → category`), normalization mismatches silently drop categories. Example: preview keys by "diced tomatoes" but `findOrCreateTemplate()` normalizes to "tomato" — lookup fails, category lost. Fix: index-based passing (`[Int: String]`) eliminates all name-matching fragility | Import a recipe with AI parsing → verify categories persist to saved recipe ingredients | Raw |
| Mar 2 | M10.6.9 | AI/CategoryValidation | LLM parsers will confidently return categories not in the user's list (e.g., "Other", "Pantry") even when the prompt says "use categories from this list." Strengthening the prompt helps but isn't sufficient — must validate AI-returned categories against the actual list case-insensitively and fall back to nil when no match found | Import with AI parse → check that all displayed categories exist in user's category list | Raw |
| Mar 2 | M10.6.9 | Architecture/IndexVsNameKeys | Index-based data passing between pipeline stages is more robust than name-based when any stage applies transformations (normalization, singularization, qualifier stripping). Name keys break silently when the key-building stage and the key-consuming stage use different transformation levels. Index keys are transformation-invariant | Code review: verify no name-keyed dicts are used between stages with different normalization levels | Raw |
| Mar 2 | M10.6.9 | Grocery/MergeLogic | `findExistingItem()` was matching ALL grocery list items including completed ones. When a user checks off "flour" then adds a recipe with flour, the merge silently updated the completed item — user saw no change in their active list. Fix: skip completed items so a fresh entry is created | Add same ingredient twice → check off → add again → verify new unchecked entry appears | Raw |
| Mar 2 | M10.6.9 | Import/CountValidation | `tryLLMParsing()` iterated over `llmResults` not `texts` — if LLM returned fewer results than inputs, ingredients were silently dropped. No template, no Ingredient entity. Fix: fall back to local pipeline on count mismatch so all ingredients are always created | Import recipe with 10+ ingredients → verify all appear in saved recipe | Raw |
| Feb 11 | M7.6.1 | iOS/LaunchScreen | UILaunchScreen plist dict does NOT resolve asset catalog dark mode appearance variants — must use LaunchScreen.storyboard approach instead | Manual: toggle device appearance, delete app + reboot to clear cache | Raw |
| Feb 11 | M7.6.1 | iOS/LaunchScreen | iOS caches launch screens at the system level, not the app level. Even clean build + reinstall can show stale launch screen. Only reliable fix: delete app + reboot device | Manual: change launch screen asset, rebuild, verify on device | Raw |
| Feb 11 | M7.6.1 | Xcode/IB | Hand-crafted storyboard XML requires `targetRuntime="iOS.CocoaTouch"` (not `"AppleSDK"`). Get correct tools version from `ibtool --version`. Xcode 26.2 uses `24506` | `ibtool --compile` gives detailed errors if wrong | Raw |
| Feb 11 | M7.6.1 | iOS/Vision | `VNGenerateForegroundInstanceMaskRequest` provides production-quality subject extraction for removing image backgrounds programmatically | Visual: compare input/output PNGs for clean edges | Raw |
| Feb 11 | M7.6.2 | Swift/Release | `print()` is NOT compiled out in Release builds — it still writes to stdout. Use `#if DEBUG` guards or `os.Logger` (which respects privacy redaction) for production apps | `xcodebuild -configuration Release build` compiles; Console.app on device to verify no output | Raw |
| Feb 11 | M7.6.2 | Swift/Compilation | `#if DEBUG` is a compile-time flag — unit tests always run in DEBUG configuration, so you cannot write a test asserting "this code is absent in Release". Verification requires Release build + manual inspection | Release build compiles; no automated test possible for conditional compilation | Raw |
| Feb 11 | M7.6.1 | Xcode/pbxproj | `PBXFileSystemSynchronizedRootGroup` (used by Services/) auto-detects new files. Manual `PBXGroup` (used by forager/ and foragerTests/) requires adding PBXFileReference, PBXBuildFile, group children, and build phase entries by hand | Build succeeds after adding; missing entries cause "file not found" errors | Raw |
| Feb 11 | M7.6.2 | SwiftUI/ViewBuilder | `#if DEBUG` / `#else` inside `@ViewBuilder` closures works cleanly as long as both branches return the same type — no type erasure needed. Good pattern for swapping debug vs production UI | Build succeeds in both configurations; visual check in simulator | Raw |
| Feb 11 | M7.6.2 | Xcode/Simulator | You can test Release builds in the simulator: Edit Scheme (Cmd+<) → Run → Info → change Build Configuration to "Release". Remember to switch back to Debug when done | Run in simulator with Release config; verify debug UI is absent | Raw |
| Feb 8 | M7.6 | CoreData/Schema | CloudKit Production schema is append-only once deployed — record types and fields can never be removed or renamed. Clean up before first Production deploy | CloudKit Console: verify Development schema before deploying | → ADR 007 |
| Feb 11 | M7.6.2 | Swift/Preview | `#Preview` macros are stripped from Release builds by the compiler, like `#if DEBUG`. Print statements inside preview blocks don't need explicit gating | Build Release config — preview code absent from binary | Raw |
| Feb 11 | M7.6.2 | Xcode/Scheme | Switching Xcode Run scheme to Release for testing persists in `.xcscheme` file. Always switch back to Debug after, or you'll commit a Release config that disables `#if DEBUG` code and debug symbols | `git diff` the scheme file after testing; restore if changed | Raw |
| Feb 11 | M7.6.2 | Xcode/pbxproj | Dead test files in the app target compile into the production binary — class metadata and string literals survive even if functions are never called. Remove from `PBXSourcesBuildPhase` or move to test target | Build succeeds after removal; `git diff` pbxproj confirms entries removed | Raw |
| Feb 12 | M7.6.4 | CoreData/Migration | Lightweight migration supports entity deletion, toOne→toMany cardinality changes, and hierarchy restructuring out of the box. Only attribute type changes (String→Int), entity hierarchy merges, and data transforms require custom mapping models or staged migrations | `NSMappingModel.inferredMappingModel(forSourceModel:destinationModel:)` returns non-nil if supported | → LN 31 |
| Feb 8 | M8.3 | Parser/Architecture | Confidence-based routing (regex fast path + NLP fallback) avoids NLP overhead for 85%+ of inputs while maintaining accuracy for edge cases | Unit tests: verify regex returns >0.8 confidence for common patterns, NLP activates below threshold | → ADR 010 |
| Feb 12 | M7.6.4 | CoreData/Versioning | Each `.xcdatamodel` inside `.xcdatamodeld` is a full schema snapshot. The `.xccurrentversion` plist tells Core Data which is active. Old model versions must remain in the bundle — Core Data needs them as the source to compute lightweight migrations | Add new model version, verify `NSMappingModel.inferredMappingModel` returns non-nil for old→new | → LN 31 |
| Feb 12 | M7.6.4 | Xcode/CoreData | Editing `.xccurrentversion` outside Xcode while the project is open can get reverted — Xcode overwrites it from its in-memory project state on build. Either close Xcode first or re-set the file after build. The committed state is what matters for production | Edit file, run build, re-read file to check if reverted | → LN 31 |
| Feb 12 | M7.6.5 | CoreData/Codegen | With Manual/None codegen, `@NSManaged` properties that don't exist in the schema compile fine but return `nil` at runtime. Core Data uses KVC under the hood — accessing an undefined key on NSManagedObject returns nil instead of crashing. This makes code-schema mismatches silent and hard to catch | Declare a property not in schema, set a value, save, re-fetch — value is nil | → LN 31 |
| Feb 12 | M7.6.5 | CloudKit/Schema | CloudKit cannot rename fields. To "rename" an attribute, add a new field, migrate data in code on first launch, and leave the old field as an orphan. Both fields coexist permanently in the CloudKit schema | CloudKit Console: verify both old and new fields appear after Production deploy | → LN 31 |
| Feb 12 | M7.6.6 | CoreData/Migration | When data is stored as a hack (e.g., tags encoded in `sourceURL` with a `tags:` prefix), migrating to a proper field is simpler than a CloudKit field rename — add the new attribute, write an idempotent one-time migration predicated on `oldField BEGINSWITH X AND newField == nil`, and clear the old value. No CloudKit orphan created because the data was never in a "real" schema field | Build succeeds; create recipe with tags, relaunch, verify tags migrated and sourceURL cleared | → LN 31 |
| Feb 12 | M7.6.3 | Xcode/CoreData | Xcode silently rewrites `.xccurrentversion` from its in-memory project state — even `git checkout --` is ineffective while Xcode is open. This caused a crash when it reverted v5→v2, losing the `tags` attribute. Fix: close Xcode before restoring, or use a direct file write tool. Committed state is correct but the build uses whatever Xcode last wrote to disk | Close Xcode, edit file, reopen, verify model inspector shows correct version | → LN 31 |
| Feb 12 | M7.6.3 | Xcode/pbxproj | `PBXFileSystemSynchronizedRootGroup` (Services/) auto-detects new files on disk. Manual `PBXGroup` (forager/) requires 4 project file entries: PBXFileReference, PBXBuildFile, group children, PBXSourcesBuildPhase. Missing any one causes "cannot find X in scope" at build time | Create file in each dir, build — Services/ compiles, forager/ fails until pbxproj updated | Raw |
| Feb 16 | M15 | Design/Tooling | The `frontend-design` Claude Code plugin provides structured design critique that catches visual consistency issues the author misses: font size proliferation (13 sizes → 8), insufficient card shadow weight against warm backgrounds, lack of visual identity on recipe cards (added category header bands), and button size mismatches between primary/secondary. Running it against HTML mockups before implementing in Swift saves significant rework — the feedback loop is minutes vs hours of refactoring compiled UI code | Create HTML mockup → run `frontend-design` review → iterate → then implement in Swift | → LN 33 |
| Feb 16 | M15 | Design/Typography | Serif fonts in utility apps create a "two apps glued together" feeling when only applied to one content type (recipes). In editorial-first apps (NYT Cooking, Bon Appetit) where the entire experience is serif, it works. In a utility-first app where most screens are lists/checkboxes, visual hierarchy should come from size and weight contrast within a single font family, not from switching families | Visual: compare mockup with and without serif on recipe screens — unified font reads as one app | → LN 33 |
| Feb 28 | M10.8 | iOS/Text | `Text + Text` concatenation is deprecated in iOS 26 — use `Text("\(Text(a))\(Text(b))")` string interpolation instead. Each interpolated `Text` retains its own modifiers (font, color, bold). The import preview's existing `+` pattern will need the same migration eventually | Build with iOS 26 SDK — warnings appear for `+`, disappear with interpolation | Raw |
| Feb 28 | M10.8 | SwiftUI/FocusState | Multiple `@FocusState` properties (e.g., `focusedIngredientId: UUID?`, `focusedStepIndex: Int?`, `focusedMetadata: MetadataFocus?`) provide automatic mutual exclusion. Setting focus on one nils the others, triggering their `onChange` handlers to commit pending edits. Eliminates need for explicit "exit other editing modes" code | Add multiple @FocusState vars with onChange handlers — verify setting one commits the other | Raw |
| Feb 28 | M10.8 | SwiftUI/TextField | `TextField("", text:, axis: .vertical)` with `.submitLabel(.done)` gives wrapping text display (good for long instruction steps) where the Done key fires `.onSubmit` instead of inserting a newline. Without `axis: .vertical`, long text scrolls horizontally. Without `.submitLabel(.done)`, Return inserts newlines | Create TextField with both modifiers — verify text wraps AND Done submits | Raw |
| Feb 28 | M10.8 | SwiftUI/FocusState | `@FocusState` + conditional view (`if editing { TextField }`) requires `.onAppear { focusedField = .value }` on the TextField to reliably gain focus. Setting the FocusState before the view exists (in the tap handler) can race with view insertion. The `.onAppear` pattern fires after the TextField enters the hierarchy | Tap to edit → verify keyboard appears without extra tap | Raw |
| Feb 28 | M10.8 | SwiftUI/Focus | UUID-based `@FocusState` tracking is essential when the list supports reorder/delete — integer indices go stale after mutations. The import preview uses `Int` indices (safe because that list is static), but recipe editing lists are mutable so must use `IngredientInput.id: UUID` | Reorder ingredients while editing — UUID tracking stays correct, index-based would crash | Raw |
| Feb 28 | M10.8 | Xcode/Versioning | `CURRENT_PROJECT_VERSION` appears 6 times in pbxproj (2 per target × 3 targets). CLI version bumping must target only the app target's entries (first 2). `agvtool` doesn't work cleanly without `VERSIONING_SYSTEM = apple-generic` set, so direct pbxproj editing is more reliable for this project | `grep -c 'CURRENT_PROJECT_VERSION' project.pbxproj` returns 6; only first 2 are app target | Raw |
| Feb 16 | M15 | Design/Shadows | On warm cream backgrounds (#FDFBF7), shadow level 1 (`0 1px 3px at 8%`) is nearly invisible — cards blend into the background. Shadow level 2 (`0 2px 8px at 12%`) is the minimum for tappable cards to read as interactive surfaces. Shadow level 1 should be reserved for non-interactive elements like type-sample cards or static info panels | Toggle between shadow-1 and shadow-2 on cards against cream background | → LN 33 |
| Feb 16 | M15 | Design/Typography | SF Mono for quantities in a grocery list creates visual dissonance — the monospaced glyphs look like they belong to a different app. `font-variant-numeric: tabular-nums` on the system font (SF Pro) gives the same column-alignment benefit without any visible font switch. In SwiftUI: `.monospacedDigit()` modifier. Reserve true monospace for code/hex values only | Side-by-side: SF Mono quantities vs system font with tabular-nums — alignment identical, visual consistency improved | → LN 33 |
| Feb 12 | M7.6.3 | SwiftUI/Lifecycle | `PersistenceController.shared` initializes during `foragerApp.init()` via stored property access. `loadPersistentStores()` is synchronous and blocks until SQLite files open + CloudKit mirroring sets up — SwiftUI cannot render any frame until init completes. Fix: defer store loading to `prepare()` called from splash `.onAppear` | Delete app, rebuild — splash with spinner appears before stores finish loading | Raw |
| Feb 25 | M10.9 | Xcode/pbxproj | `PBXFileSystemSynchronizedRootGroup` auto-copies ALL files as bundle resources. Files already handled by build settings (Info.plist via `INFOPLIST_FILE`, entitlements via `CODE_SIGN_ENTITLEMENTS`) cause "Multiple commands produce" errors. Fix: add a `PBXFileSystemSynchronizedBuildFileExceptionSet` with `membershipExceptions` listing those paths relative to the group root | Build fails without exceptions, succeeds with them | Raw |
| Mar 1 | M10.6.5 | LLM/ErrorHandling | `parseBatchWithLLM()` returning `nil` for both "not configured" and "API call failed" is a sentinel value anti-pattern — callers can't distinguish between the two failure modes. Adding `@Published var lastLLMError: String?` is pragmatic: callers check it after nil return for the real error (invalidAPIKey, timeout, rateLimited, malformedResponse). A `Result<T, LLMParserError>` return type would be cleaner but requires touching all 7+ call sites | Build; trigger LLM parse failure → toast shows specific error instead of generic "unavailable" | Raw |
| Feb 25 | M10.9 | Xcode/pbxproj | When converting from `PBXGroup` to `PBXFileSystemSynchronizedRootGroup`, the XCVersionGroup for `.xcdatamodeld` can be safely removed — Xcode reads the `.xccurrentversion` file from the bundle on disk to determine the current model version. All individual PBXFileReference entries for model versions become unnecessary | Build succeeds, Core Data model resolves correctly, check `.xccurrentversion` file exists on disk | Raw |
| Feb 12 | M7.6.3 | CoreData/Init | `NSPersistentCloudKitContainer(name:)` + `persistentStoreDescriptions` assignment is fast (model parsing + config). The slow call is `loadPersistentStores()` which opens SQLite and registers CloudKit mirroring. Splitting into two phases lets SwiftUI render between them | Time each phase with `CFAbsoluteTimeGetCurrent()` — container creation <0.01s, store loading 0.5-2s | Raw |
| Feb 12 | M7.6.3 | SwiftUI/Observation | A `let` stored property on an `App` struct does NOT trigger SwiftUI re-renders when its `@Published` properties change — only `@StateObject`/`@ObservedObject` do. Bridge with `@State` + `.onReceive(publisher.$property)` to avoid disrupting singleton ownership patterns | Change `@Published` value on singleton — verify UI updates only with onReceive bridge | Raw |
| Feb 12 | M7.6.7 | CloudKit/Environment | `NSPersistentStoreCloudKitEnvironment` defaults to Production when not explicitly set. Debug builds set it to "Development"; Release builds omit it, pushing schema to Production. To verify schema in the Development Console before deploying, must use a Debug build on a physical device | Run Debug on device → check Development Console; Run Release on device → schema goes to Production | → LN 34 |
| Feb 12 | M7.6.7 | CloudKit/Schema | `NSPersistentCloudKitContainer` does NOT automatically push schema for all entities — it pushes lazily when records are created. Entities with no instances (e.g., Household on a fresh device) won't appear in CloudKit Console. Must call `initializeCloudKitSchema(options: [])` once during development to force a full schema push, then remove it | Add temporary call, run on device, verify all record types appear in Console | → LN 34 |

| Feb 15 | M7.6.8 | CloudKit/Schema | `cloudkit.share` record type is only created in the schema when `container.share()` is first called — it doesn't exist by default. If you deploy schema to Production before ever creating a share in Development, Production will be missing this type entirely. `NSPersistentCloudKitContainer`'s mirroring delegate fails to initialize with "Cannot create new type cloudkit.share in production schema". Fix: create a share in Development, then re-deploy to Production | Create household in Dev → check `cloudkit.share` exists in Dashboard → Deploy to Production | → LN 34 |
| Feb 15 | M7.6.8 | CloudKit/Timing | `container.share()` can fail with `partialFailure` / "Failed to modify some records" on fresh installs because `NSPersistentCloudKitContainer` exports records asynchronously after `context.save()`. The record may not exist on the CloudKit server yet when share() runs. Retry with exponential backoff (2s, 4s) resolves this | Delete app → reinstall → create household immediately → observe if first share attempt fails | → LN 34 |
| Feb 15 | M7.6.8 | SwiftUI/ZStack | Views with `.ultraThinMaterial` (or any non-clear background) in a ZStack intercept all touch events, even if the underlying layer has `.onTapGesture`. Hit testing follows ZStack render order (last = top). Fix: add tap gesture directly on the material-backed view | Place a tap target behind a material view → tap is absorbed by material | Raw |
| Feb 15 | M7.6.8 | CloudKit/Sharing | `container.share([household])` only migrates the root record (Household) to the shared store — child records (HouseholdMember) stay in the owner's private store, invisible to other participants. Store essential cross-device data (like owner display name) directly on the shared root record | Create household, share, check shared store on joinee — only Household entity present, no HouseholdMember | → LN 34 |
| Feb 17 | M15.7 | SwiftUI/Accessibility | `@Environment(\.accessibilityReduceMotion)` must be declared on the specific struct that uses it — not a parent struct in the same file. When RecipeListView.swift contains both `RecipeListView` and `RecipeDetailView` as separate structs, adding `reduceMotion` only to `RecipeListView` causes "cannot find in scope" when `RecipeDetailView` references it. Each struct is a separate scope | Add `reduceMotion` to parent-only → child struct fails to compile; add to child → builds | → LN 35 |
| Feb 17 | M15.7 | SwiftUI/Accessibility | `@ScaledMetric(relativeTo:)` is a zero-effort way to make fixed-size UI elements respect Dynamic Type. Changing `var size: CGFloat = 56` to `@ScaledMetric private var size: CGFloat = 56` makes the element grow/shrink with the user's text size preference. The `relativeTo:` parameter ties scaling to a specific text style so the element scales proportionally | Toggle Dynamic Type in Settings → verify ring/circle sizes change proportionally | → LN 35 |
| Feb 17 | M15.7 | SwiftUI/Animation | The pattern `withAnimation(reduceMotion ? nil : .spring(...))` is the correct way to guard animations: passing `nil` as the animation parameter makes the state change instant (no animation). For state-change animations where some transition is needed for UX clarity, use `.easeInOut(duration: 0.15)` as the reduced-motion alternative — fast enough to not trigger motion sensitivity but visible enough to show state changed | Toggle Reduce Motion in Settings → verify decorative animations gone, state changes still perceptible | → LN 35 |
| Feb 17 | M15.7 | iOS/ContentUnavailableView | `ContentUnavailableView` (iOS 17+) is a direct replacement for custom empty state views. It provides `.search(text:)` for search-specific empty states and a general `Label + description + actions` pattern for other contexts. Adopting it eliminates custom view maintenance and ensures visual consistency with system conventions | Replace custom empty state → verify in simulator that layout matches system apps | Raw |
| Feb 15 | M7.6.8 | CloudKit/Schema | Repurposing a deprecated Core Data field (ownerEmail → ownerDisplayName) avoids a CloudKit schema change entirely. Since CloudKit cannot delete fields, unused fields are free real estate for new purposes — just add a computed alias and update all call sites | Read `ownerDisplayName` on joinee device after sync — value matches what owner set during creation | → LN 34 |
| Feb 15 | M7.6.8 | iOS/CKShare | iOS 16+ strips `nameComponents` from CKShare for the current user (owner). `share.currentUserParticipant?.userIdentity.nameComponents` returns nil on the owner's device. Other participants' names may also be nil unless they've granted discoverability. Never rely on CKShare.participants for display names | Log `share.participants` on owner device — own entry has nil nameComponents | → LN 34 |
| Feb 25 | M10.5 | Testing/DataSource | Major recipe sites (AllRecipes, SimplyRecipes, NYT Cooking, FoodNetwork, Epicurious, BBCGoodFood, BudgetBytes, RecipeTinEats) all block automated fetching. TheMealDB (`themealdb.com/api/json/v1/1/search.php?s=keyword`) is a free, unblocked API returning structured JSON with ingredient/measure pairs — ideal for generating test corpus data | `curl` the API endpoint — returns JSON with ingredients/measures for any search term | Raw |
| Feb 25 | M10.5 | Testing/Resources | `#filePath` in XCTest resolves to the source file's compile-time path. Using `.deletingLastPathComponent()` chains to navigate to sibling directories avoids adding hundreds of test data files as bundle resources in pbxproj. Files just need to exist on disk at the expected relative path | Build + run test — `#filePath` resolves correctly, FileManager reads corpus files | Raw |
| Feb 25 | M10.5 | Parser/Accuracy | On a 50-recipe corpus, ML parser handles 81.7% of ingredient lines, regex only 18.3%, NLP 0%. Average parsing confidence is 0.932 (high), but average classification confidence is only 0.520 (low). Classification is the bottleneck, not parsing — improving OCRLineClassifier will yield the biggest accuracy gains | Run `testRunFullCorpus()` — check console output for distribution stats | Raw |
| Feb 25 | M10.5 | Parser/Accuracy | Messy/prose-style recipes (blog intros, ingredients embedded in paragraphs) yield dramatically fewer detected ingredients: 33 lines vs 100-130 for structured formats. The OCRLineClassifier's heuristic scoring struggles with ingredients that lack standard formatting cues (leading dash, qty at start of line) | Compare messy category ingredient count to clean category in corpus results | Raw |
| Feb 25 | M10.5 | SwiftUI/BindingFromEnum | Creating `Binding<T>` from an enum's associated value requires a custom getter/setter pattern. The getter extracts the value via `if case .state(let val) = enumVar`, the setter wraps it back. Useful for passing bindings to child views when state is modeled as an enum | Bind a TextField to an enum's associated String — text edits propagate correctly | Raw |
| Feb 26 | M10.5.4 | LLM/OAuth | Anthropic explicitly banned third-party OAuth (Jan 9 2026 enforcement, Feb 19 2026 policy update). OpenAI OAuth is for ChatGPT→your backend, not your app→OpenAI API. Only Google Gemini supports OAuth but with consent screen complexity. API keys are the only universal LLM auth approach | Check Anthropic docs for OAuth policy; OpenAI Apps SDK docs; Google AI Studio | Raw |
| Feb 26 | M10.5.4 | Parser/DescriptorGaps | Bare descriptors without "a" prefix (`"Dash of olive oil"`, `"Bunch of coriander"`) were falling through all parser patterns. The regex parser had the "a pinch/dash/..." pattern but not the bare form for `bunch`, `sprinkling`, `squeeze`. Both the "a" prefix AND bare descriptor patterns must be kept in sync | Parse "Bunch of herbs" → qty=1, name="herbs", notes="bunch" | Raw |
| Feb 26 | M10.5.4 | Parser/InvertedStructure | Some ingredient lines put the descriptor before the quantity: `"Juice of 1/2 lemon"` instead of `"1/2 lemon juice"`. This "prefix-before-quantity" pattern is common for juice/zest but breaks all standard qty-first parsing. Requires a dedicated pattern that looks for `^(juice|zest) of \d+` | Parse "Juice of 2 lemons" → qty=2, name="lemons", notes="juice of" | Raw |
| Feb 26 | M10.5.4 | Parser/PracticalCeiling | After 10 regex fixes across 2 rounds, the pipeline reached ~88% qty extraction rate. The remaining ~12% are genuinely semantic gaps (prose-embedded quantities, "to serve" patterns, ambiguous multi-ingredients) that no regex can solve. This validates the LLM integration strategy — regex handles the structured 88%, LLM handles the semantic 12% | FM comparison: 25 remaining gaps, all semantic in nature | Raw |
| Feb 26 | M10.3.8 | SwiftUI/PreviewEnrichment | Read-only preview enrichment (parse + lookup without writes) is a clean pattern for import previews. Computing matches synchronously in `.task {}` works well for recipe-sized data (~15 ingredients × <0.05s parse + <0.1s lookup = ~0.5s total). For larger datasets, you'd need background contexts to avoid blocking | Import a recipe → verify ingredient status icons appear without delay | Raw |
| Feb 26 | M10.3.8 | Architecture/Reuse | `IngredientStatus` from `RecipeFormModels.swift` reuses cleanly in import preview — same `.ready`/`.needsCategory`/`.needsTemplate` semantics apply to both manual entry and import preview. Keeping consistent visual language (same icons, colors, descriptions) across both flows reduces cognitive load | Compare ingredient row in CreateRecipeView vs RecipeImportPreviewView | Raw |

| Feb 16 | M15 | Design/ColorSemantic | Using the same color palette for two different semantic meanings (category colors on grocery chip pills AND recipe card header bands) creates user confusion. Colors should have one meaning per context. Recipes don't have categories in the data model — the band color was derived from dominant ingredient category, which is ambiguous. Removed recipe bands entirely; category colors are now exclusive to grocery list context | Compare recipe list before/after — clean cards without bands avoid the "what does green mean here?" question | → LN 33 |
| Feb 16 | M15 | Design/InlineEditing | When a display format will transition to inline editing, the display layout should match the editing layout to avoid layout shift. Ingredient rows display as `• qty unit name` (one line) rather than a two-column table because the editing UX uses a single inline text field parsed by `IngredientParsingService`. Column layouts would visually break on edit-tap | Tap ingredient row to edit — verify no layout shift between display and edit modes | → LN 33 |
| Feb 16 | M15 | Design/StepNumbers | Large decorative step numbers (28pt) in recipe instructions invert the visual hierarchy — the number dominates while the instruction text (the actual content) recedes. Reducing to 17pt inline with the same baseline as the text makes numbers serve as navigation anchors without competing with content. This matches how printed cookbooks handle step numbers | Compare 28pt vs 17pt step numbers — text readability improves at 17pt, scanning remains easy | → LN 33 |
| Feb 16 | M15 | Design/DayDotInitials | Day-of-week dots without labels require the user to count from a known start day (Monday? Sunday?) to identify which dot is which. Adding single-letter initials (M/T/W/T/F/S/S) inside 22pt circles makes them self-documenting — no legend, no counting, no ambiguity. The slight increase in dot size (10pt→22pt) is justified by the cognitive load reduction | Compare unlabeled 10pt dots vs lettered 22pt dots — users identify specific days instantly with letters | → LN 33 |
| Feb 22 | M8.4 | CoreData/Validation | Core Data `validateForInsert()` runs at `context.save()` time, not at object creation. A service method like `createRecipe()` can succeed in creating the object but return `nil` when the internal save fails validation — the root cause is hidden behind a generic error message. Tests that just check `XCTAssertNotNil` see the symptom (nil) but not the cause (missing required field) | Add `instructions: "Test"` to test calls → save succeeds; check errorMessage on service for actual failure reason | Raw |
| Feb 22 | M8.4 | CoreData/ModelVsSwift | The `.xcdatamodeld` can mark an attribute as Non-Optional (required), but Swift codegen still types it as `String?` (`@NSManaged public var displayText: String?`). Code compiles fine with nil, but `context.save()` throws `NSCocoaErrorDomain Code=1570 "displayText is a required value"` at runtime. The Swift type system gives no compile-time protection for Core Data model-level required constraints | Pass nil for a model-required field → observe Code=1570 at save time; fix by providing a value | Raw |
| Feb 23 | Research | RecipeImport/Persistence | `RecipeService.createRecipe()` calls `save()` immediately — any import preview flow that calls this creates persisted records before user confirmation. Import must use a draft model (RecipeFormData or child NSManagedObjectContext) and only persist on final confirm. The existing create/edit views already follow this pattern via `@State var formData = RecipeFormData()` | Check RecipeService.swift:44 — `save("create recipe")` runs inline; CreateRecipeView.swift:60 shows draft pattern | Raw |
| Feb 23 | Research | RecipeImport/FormData | `RecipeFormData` has no `init(from: Recipe)` initializer — research doc assumed one existed. Building an import preview requires either adding an init that populates from extracted data, or a dedicated `ImportDraft` struct with confidence/provenance metadata. For v1, extending RecipeFormData with `init(from extractedData:)` is the pragmatic path | Grep for `init(from.*Recipe)` returns zero matches; RecipeFormModels.swift:98 shows struct definition | Raw |
| Feb 23 | Research | iOS/FoundationModels | Foundation Models `@Generable` APIs compile in extension mode (`swiftc -application-extension`) with no unavailability annotations, but the ~1.2 GB runtime model load likely exceeds the 120 MB iOS extension memory limit. Treat as "expected to fail at runtime" not "guaranteed to fail at compile time" — validate with engineering spike before cementing as hard constraint | Compile test passes; runtime test in share extension needed to confirm memory behavior | Raw |
| Feb 23 | Research | RecipeImport/Architecture | Foundation Models and BiLSTM-CRF are complementary, not competing: Foundation Models excels at document-level understanding (section detection, OCR error correction) while BiLSTM-CRF excels at token-level extraction (qty/unit/name from an ingredient line). The ideal import pipeline uses Foundation Models for "what kind of text is this?" and BiLSTM-CRF for "parse this ingredient line" | Compare accuracy: Foundation Models on full recipe text vs BiLSTM-CRF on individual ingredient lines | Raw |
| Feb 23 | Research | Process/PRDReview | Architecture reviews are most valuable when they verify claims against actual code. The Codex review's strongest findings came from checking code examples against the real codebase (persistence timing, missing initializer). Its weakest finding was purely stylistic (legal section tone). Lesson: prioritize "does this match reality?" over "is the prose precise enough?" | Compare finding impact: code-verified findings (#1, #3) drove architecture changes; style findings (#5) were low-priority | Raw |
| Feb 24 | M10.1 | SwiftUI/Toolbar | When a child view needs its own Cancel/Save in a parent's NavigationStack, the parent must hide its toolbar items for that state and the child adds its own via `.toolbar`. `ImportJobState.isReviewing` computed property enables this cleanly — parent checks `!state.isReviewing` on its Cancel button, child uses `.cancellationAction` and `.confirmationAction` placements | Build succeeds; verify both Cancel and Save appear on preview but not double-Cancel with parent | Raw |
| Feb 24 | M10.1.9 | iOS/ShareExtension | Share extensions work well for content *creation* (posting, saving) but poorly for content *import* where the user needs to see results in the receiving app immediately. The flash-and-disappear share sheet plus manual app switching creates a jarring UX. In-app browsers (Paprika-style) provide a better import flow by keeping the user in one app | Compare: share extension requires 3 taps + app switch; in-app browser is 1 tap + browse + import | Raw |
| Feb 24 | M10.1.9 | Xcode/pbxproj | Removing an Xcode target requires deleting entries across ~9 pbxproj sections: PBXBuildFile, PBXContainerItemProxy, PBXCopyFilesBuildPhase, PBXFileReference, PBXFileSystemSynchronizedBuildFileExceptionSet/RootGroup, PBXNativeTarget, PBXTargetDependency, XCBuildConfiguration (x2), XCConfigurationList, plus references in PBXGroup children, Products group, project targets, and parent target buildPhases/dependencies. Missing any one causes cryptic build errors | Build after each removal batch; grep for target ID to verify zero remaining references | Raw |
| Feb 24 | M10.1.9 | WKWebView/Extraction | A user-facing WKWebView doesn't need the 2-second JS settle delay used by headless `WKWebViewExtractor`. By the time the user navigates to a recipe page and taps "Import", all JS has already executed and JSON-LD is injected. `evaluateJavaScript("document.documentElement.outerHTML")` returns immediately with fully-rendered content | Navigate to recipe site in browser → extract → verify JSON-LD present without settle delay | Raw |
| Feb 24 | M10.1.10 | CoreData/Categories | Hardcoded category name strings in code will drift from seeded Category entity names as defaults evolve. `categorizeIngredient()` returned "Meat & Seafood" but the seeded default is "Deli & Meat" — creating phantom category strings on templates. Fix: always assign categories via entity references (CategoryAssignmentModal), never by string matching | Import recipe → check IngredientTemplate.category values match actual Category entity names | Raw |
| Feb 24 | M10.1.10 | WKWebView/KVO | WKWebView properties (url, title, isLoading, estimatedProgress, canGoBack, canGoForward) are KVC-observable but NOT Combine publishers. Use `NSKeyValueObservation` (Swift KVO) to observe them. The observations must be stored in a collection to prevent deallocation — if the observation object is released, KVO stops silently | Set up KVO on webView.url → navigate → verify urlText updates; remove observation → verify updates stop | Raw |
| Feb 24 | M10.1 | CoreData/Replace | In-place recipe update via child context preserves `NSManagedObjectID` — which means PlannedMeal relationship references survive the update. Delete+recreate would orphan them. The child context pattern (`existingObject(with:)` → modify → save child → save parent) keeps atomicity while targeting a specific existing object | Replace a recipe that's in a meal plan → verify PlannedMeal still navigates to it | Raw |
| Feb 25 | M10.1.10 | Import/JSONLDTitle | JSON-LD `name` field is unreliable for recipe titles — some sites (NYT Cooking, others) use shortened names in structured data while the full title lives in `og:title` or `<title>`. NYT's "Spaghetti Carbonara" recipe has `"name": "Carbonara"` in JSON-LD. Fix: post-extraction title enhancement that cross-references HTML metadata, only upgrading when the metadata title is longer AND contains the JSON-LD name | Import NYT Cooking recipe → verify title shows "Spaghetti Carbonara" not "Carbonara" | Raw |
| Feb 25 | M10.1.10 | CoreData/ValidationLimits | Validation limits set for manual entry (100 chars) break on imported data. Real-world recipe ingredients like "3½ ounces of slab guanciale (see recipe), pancetta or bacon, sliced into pieces about ¼ inch thick by ⅓ inch square" are 100+ chars. Template names after parsing inherit notes and qualifiers that push past tight limits. Set limits for the import use case (250 for ingredients, 300 for titles), not just manual entry | Import verbose recipe (NYT Cooking) → save succeeds without validation errors | Raw |
| Feb 24 | M10.1 | SwiftUI/Wireframe | HTML wireframes with CSS classes map almost 1:1 to SwiftUI: `ingredient-row` → bordered HStack, `step-number` → Circle background Text, `meta-row` → HStack with dot separators, `section-header` → HStack with Spacer+Image. The CSS `dashed` border for empty fields maps to SwiftUI `StrokeStyle(dash:)` | Visual comparison: simulator screenshot vs wireframe HTML in browser | Raw |
| Feb 22 | M8.4 | Testing/SchemaEvolution | When Core Data validation rules are added to existing entities (e.g., Recipe gains `instructions` requirement), ALL tests that create those entities break — even tests that don't test validation. The validation runs on `context.save()` for the entire object graph, not just the object being tested. Test files must be updated in batch when validation rules evolve | Run full test suite after adding validation → find all tests creating that entity type | Raw |
| Feb 26 | M10.3 | Vision/ReadingOrder | `VNRecognizedTextObservation.boundingBox` uses normalized coordinates with bottom-left origin (0,0 at bottom-left). For top-to-bottom reading order, sort by `1 - boundingBox.origin.y`. Observations are NOT guaranteed in reading order from the API — explicit sorting is required | OCR a multi-line image → check line order matches visual reading order | Raw |
| Feb 26 | M10.3 | iOS/PhotosPicker | SwiftUI `PhotosPicker` (PhotosUI framework) runs out-of-process — no `NSPhotoLibraryUsageDescription` permission needed. Only `NSCameraUsageDescription` is required for `VNDocumentCameraViewController`. This is a significant privacy advantage over UIImagePickerController | Check Info.plist — only camera key needed; photo picker works without library permission | Raw |
| Feb 26 | M10.3 | Vision/DocumentScanner | `VNDocumentCameraViewController` returns already-processed images (deskewed, contrast-enhanced, perspective-corrected). No need for manual image preprocessing before OCR — Vision's text recognition on scanner output is already near-optimal quality | Scan a tilted document → verify output image is straightened | Raw |
| Feb 26 | M10.3 | Import/DualPath | Photo import uses FM as primary extraction (fast path) with heuristic classification as fallback, mirroring M10.2's text paste pattern. The split-screen review UI only appears on the heuristic path — FM produces a ready draft directly. `extractionMethod` tracks which path was taken: `"ocr_fm"` vs `"ocr_heuristic_reviewed"` | Import photo on FM-capable device → verify FM path skips review; on non-FM device → review appears | Raw |
| Feb 22 | M8.4 | Testing/TestHostLifecycle | When `xcodebuild test` runs, the app launches as a test host — `@main` struct's `init()` and `body` execute. If the app renders full SwiftUI views with `@FetchRequest` against a context with no loaded stores, `NSFetchedResultsController` crashes with "A fetch request must have an entity." Fix: detect test environment in `prepare()` and keep `isReady=false` so the app stays on a minimal loading screen | Run tests with `isReady=true` in test mode → crash; keep `isReady=false` → TEST SUCCEEDED | Raw |
| Feb 27 | M10.3 | UX/InlineAssignment | Moving category assignment inline into the import preview eliminates a separate modal step. The key architectural bridge: preview tracks `[Int: String]` (index → category), `saveWithCategories()` maps to `[String: String]` (parsed name → category), `applyCategoryAssignmentsAndFinish()` applies post-save to templates by name lookup. No service API changes needed — categories are applied as a post-save patch, falling back to CategoryAssignmentModal only for unassigned items | Import recipe with inline category picks → save → verify templates have categories, no modal appears | Raw |
| Mar 1 | M10.6.1 | iOS/MockURLProtocol | `MockURLProtocol` (subclass of `URLProtocol`) injected via `URLSessionConfiguration.ephemeral` with `protocolClasses = [MockURLProtocol.self]` intercepts ALL requests on that session — no live network calls. The static `requestHandler` closure pattern enables per-test response stubbing while keeping setup minimal. Essential for testing API adapters without hitting real endpoints | Create URLSession with mock config → make request → verify handler receives it and returns stubbed data | Raw |
| Mar 1 | M10.6.1 | Swift/CatchExhaustiveness | `do/catch let error as SpecificType` is NOT exhaustive in Swift — the compiler requires a fallback `catch` clause even if you believe only that type can be thrown. In tests, the fallback `catch { XCTFail("Unexpected: \(error)") }` pattern catches this while still failing loudly on unexpected error types | Compile test with only typed catch → error; add fallback catch → compiles | Raw |
| Mar 1 | M10.6.1 | Testing/BackoffTiming | Exponential backoff retry tests with real `Task.sleep` delays take wall-clock time (~3s for 1s+2s delays). This is intentional — it validates actual backoff timing. For large test suites, inject a clock abstraction to speed up, but for small suites (<20 tests) real delays are acceptable and more honest | Run rate-limit test → verify ~3s duration matches expected 1s+2s backoff | Raw |
| Mar 1 | M10.6.1 | Architecture/ProtocolSeparation | `LLMIngredientParser` is a separate protocol from `IngredientParser` because the contracts differ fundamentally: async vs sync, batch (`[String]`) vs per-line (`String`), network-dependent vs local-only. Sharing a protocol would force sync parsers into async wrappers or batch parsers into per-line loops. The `LLMParserResult.toParserResult()` bridge connects the two worlds at the boundary | Both protocols compile independently; bridge method tested in `testToParserResultBridge` | Raw |
| Mar 1 | M10.6.2 | iOS/DualStorage | Settings that combine a non-sensitive toggle with a sensitive credential benefit from dual storage: UserDefaults for the toggle (fast, `@Published` `didSet` for SwiftUI reactivity) and Keychain for the API key (encrypted at rest, survives reinstalls). The `activeParser()` factory checks both — downstream code calls one method instead of checking toggle + key separately | Toggle ON + save key → `activeParser()` returns parser; toggle OFF → returns nil; delete key → returns nil | Raw |
| Mar 1 | M10.6.4 | Architecture/LLMBypass | The LLM acts as a pipeline bypass in `RecipeImportService.saveImport()`, not a 4th tier inside `HybridIngredientParser`. When enabled, it parses ALL ingredient lines in one batch API call and skips regex→ML→NLP entirely. On any failure, the full pipeline runs unchanged. This avoids making `IngredientParser` protocol async, which would cascade through 11+ call sites | Disable LLM → pipeline used; enable LLM + mock error → pipeline used; enable LLM + success → pipeline skipped | Raw |
| Mar 1 | M10.6.4 | Swift/AsyncConversion | Making a sync method `async` has minimal blast radius when callers are already in SwiftUI button handlers — just wrap in `Task { await ... }`. The 3 call sites in `RecipeImportSheet` needed only `Task {}` wrappers. No protocol changes, no upstream cascading | Build succeeds; all 3 call sites compile with `Task { await }` wrappers | Raw |
| Mar 1 | M10.6.6 | Architecture/FacadeAPI | Adding `isLLMAvailable`, `parseSingleWithLLM()`, `parseBatchWithLLM()` to the existing `IngredientParsingService` facade means 5 views each need only 3 new lines to integrate LLM parsing. Views never import or reference `LLMIngredientParser` or `ClaudeIngredientParser` — the facade handles provider selection, telemetry, and error suppression | All 5 views compile with only `IngredientParsingService` import | Raw |
| Mar 1 | M10.6.6 | SwiftUI/ContextMenuAsync | SwiftUI `.contextMenu` button actions can't be `async` directly. Wrapping in `Task { await ... }` works cleanly for fire-and-forget operations like LLM parsing. The `@State` mutations from the async closure update the UI automatically since they're on `@MainActor` | Long-press → "AI Parse" → spinner appears → result updates row | Raw |
| Mar 1 | M10.6.6 | Pattern/GracefulFallback | For grocery views, the LLM sparkle button falls back to local parse on failure (`quickAddItem()` / `addItemToList()`). This means the user always gets a result — the worst case is the same quality as not having LLM. The early return pattern (`isLLMAdding = false; localMethod(); return`) keeps the fallback clean | Airplane mode → tap sparkle → item added via local parse | Raw |
| Feb 27 | M10.3.9 | SwiftUI/SheetDismiss | `.interactiveDismissDisabled()` must be applied at every call site, not just on the modal's own body. When a shared modal gains this requirement (e.g., CategoryAssignmentModal must not be swipe-dismissable), all callers need updating — the modifier is on the `.sheet` content, not inside the modal struct. Easy to miss when 4+ views present the same modal | Search for all callers of the modal → verify each `.sheet` has `.interactiveDismissDisabled()` | Raw |
| Feb 27 | M10.3.9 | SwiftUI/MenuVsNavigationLink | For category selection in modals, `Menu` (dropdown) is significantly better than `NavigationLink` because it doesn't push a new navigation stack inside a sheet. NavigationLink in sheets creates deep navigation that confuses users and breaks the sheet's dismiss gesture. Menu keeps the user on the same screen and provides the same selection capability | Present category picker via Menu in sheet → selection works inline; compare to NavigationLink → extra navigation layer | Raw |
| Feb 27 | M10.3.9 | Architecture/PatternReuse | The card-by-card review pattern (currentIndex + loadCurrentIngredient + saveAndAdvance + advance) from `IngredientReviewSheet` transferred directly to `CategoryAssignmentModal` with minimal adaptation. The key additions were: (1) re-parsing on name edit, (2) merge-on-rename logic, (3) "Skip All" for bulk dismiss. Pattern proves highly reusable for any sequential template review workflow | Compare IngredientReviewSheet and CategoryAssignmentModal side-by-side → same state machine, same UX flow | Raw |
| Feb 24 | M10.1.1 | Architecture/StrategyPattern | The `RecipeExtractor` protocol mirrors `IngredientParser` — both use the "return nil to pass" pattern. This is emerging as a Forager-wide convention: strategy protocols where implementations return nil when input isn't their format, allowing an orchestrator to try the next strategy. Consistent across HybridIngredientParser (3 parsers) and now import (5 extractors) | Verify both protocols use optional return + orchestrator fallthrough logic | Raw |
| Feb 24 | M10.1.1 | Architecture/DraftModel | Separating `ImportDraftRecipe` from `RecipeFormData` prevents bloating the manual creation flow with import-only concerns (confidence tracking, field source attribution, author/imageURL/cuisine). The `toRecipeFormData()` mapping at save time preserves the existing validation gate — import uses the exact same `RecipeFormData.validate()` as manual creation | Call `toRecipeFormData()` on a draft with missing required fields → validation returns errors | Raw |
| Feb 24 | M10.1.1 | Swift/Generics | `ImportField<T: Equatable>` wrapping enables per-field confidence without repeating confidence/source/wasEdited for every field declaration. The Equatable constraint allows the parent struct (`ImportDraftRecipe`) to derive Equatable automatically. `Comparable` on `ImportConfidence` (via `Int` raw value) enables sorting/comparison for UI dot colors | Create two fields with different confidence → compare works; create two drafts → Equatable synthesis works | Raw |
| Feb 24 | M10.1.2 | Architecture/ConfidenceFromContext | Spike's `MappingContext` returned diagnostic flags (hadHowToSections, hadUnusualYield) as a separate data path. Production inlines these flags to directly drive `ImportConfidence` levels — HowToSection nesting → `.medium`, unusual yield format → `.medium`. Eliminates an extra return value and co-locates confidence assignment with the parsing logic that knows why it matters | Map a dict with HowToSections → instructions get `.medium`; map with plain text → `.high` | Raw |
| Feb 24 | M10.1.2 | Performance/GuardBeforeWork | `HTMLEntityDecoder.containsEntities()` is a cheap 3-pattern string scan that avoids allocating new strings + regex replacements on the ~75% of instruction text with no HTML entities. Applied in `SchemaRecipeMapper.cleanInstructionText()` vs spike's unconditional decode | Profile instruction text cleaning with/without the guard — fewer allocations when entities absent | Raw |
| Feb 24 | M10.1.3 | CoreData/AtomicSave | `IngredientTemplateService.findOrCreateTemplate()` and `incrementUsage()` both call `context.save()` internally. For atomic import saves, use a child context: template saves push to parent in memory only, then `viewContext.save()` persists everything to disk in one write. If save fails, `viewContext.rollback()` discards all changes cleanly | Create child context → save template → verify viewContext.hasChanges is true but nothing on disk yet → save viewContext → verify persistence | Raw |
| Feb 24 | M10.1.3 | Architecture/OrchestratorPattern | Import extractors tried in priority order with error collection: `nil` return = "not my format" (pass to next), thrown `ImportError` = "my format but failed" (keep error). If all return nil, show last thrown error or generic "No recipe found". This mirrors HybridIngredientParser but adds error semantics on top of the nil-pass pattern | Test with HTML that has malformed JSON-LD → extractor throws → error shown; test with non-recipe HTML → nil → "No recipe found" | Raw |
| Feb 24 | M10.1.5 | WebKit/AsyncBridge | `WKNavigationDelegate` is callback-based but import service is async/await. `CheckedContinuation` bridges the gap. Key risk: multiple code paths can resume the continuation (didFinish+settle, timeout, didFail). Solution: nil-check continuation before resuming and nil it immediately — only the first path resumes, rest are no-ops | Test: load slow page, verify timeout fires and doesn't crash; load fast page, verify settle timer fires first | Raw |
| Feb 24 | M10.1.4 | Xcode/ManualPBXGroup | `forager/` directory uses manual `PBXGroup` — new view files need 4 entries: PBXFileReference, PBXBuildFile, PBXGroup children (alphabetical), and PBXSourcesBuildPhase. Each entry needs a unique 24-char hex ID. The `Services/` directory auto-detects via `PBXFileSystemSynchronizedRootGroup`. This asymmetry is a recurring friction point for import views vs services | Build with missing pbxproj entry → "file not found" or silent exclusion; build with correct entries → BUILD SUCCEEDED | Raw |
| Feb 22 | M8.4 | CoreData/CloudKitInMemory | `NSPersistentCloudKitContainer` creates `NSCloudKitMirroringDelegate` instances for every store with `cloudKitContainerOptions` set — including in-memory test stores. During test teardown, these delegates fire fetch requests against disappearing contexts, causing `NSFetchedResultsController` crashes. Fix: guard `cloudKitContainerOptions` assignment with `if !inMemory` in `createStoreDescription()` | Create `PersistenceController(inMemory: true)` with CloudKit options → teardown crash; without → clean exit | Raw |
| Feb 16 | M15 | Design/ButtonNaming | When two buttons perform related but distinct operations (create-new vs append-to-existing), using different verbs ("Generate" vs "Add to") while keeping the same noun ("Grocery List") communicates the distinction without requiring explanation. Using the same verb for both would hide the behavioral difference; using different nouns ("Shopping List" vs "Grocery List") creates terminology confusion | Compare "Generate Grocery List" and "Add to Grocery List" — users understand one creates, the other appends | → LN 33 |
| Feb 16 | M15 | Design/QuickSelectPills | Meal planning apps that require creating a "recipe" for non-cooking days (takeout, leftovers) add friction that discourages daily planning. Inline quick-select pills that mark a day as "planned" without a recipe entry cover ~90% of non-cooking scenarios (Takeout, Dining Out, Leftovers, No Meal) and keep the weekly completion dots accurate. The distinction between "unplanned" and "deliberately skipped" matters for grocery list generation | Tap "Takeout" pill on unplanned day — dot fills, day no longer contributes ingredients to grocery list generation | → LN 33 |
| Feb 21 | M8.4 | Process/ReviewConvergence | Five review passes showed monotonically decreasing severity (5 high → 2 → 1 → 2 → 0 high). When high-severity findings drop to zero and remaining items are typing/consistency level, the document has converged. Each pass found genuinely new things with zero overlap — validating multi-pass over single-pass review | Track high-severity count per pass — convergence = zero highs with only low/medium remaining | Raw |
| Feb 21 | M8.4 | Process/InternalVsExternal | External PRD review (Codex) checks internal logic and consistency. Internal code-audit review reads source files and verifies claims against ground truth. Pass 4 (internal) found things no PRD-reading would catch: 5 double-parse call sites (not 1), 11 unlogged parse operations, 204 tests (not 155). These are complementary, not redundant | Compare external findings (architectural, contract) vs internal findings (code cross-reference, stale counts) — different categories entirely | Raw |
| Feb 21 | M8.4 | Architecture/SemanticTyping | Semantic type distinctions (e.g., `ParsingSource` for parse-context vs `CorrectionSource` for edit-flow) only surface after use cases are fully specified. The distinction was invisible in passes 1-4 and only became clear in pass 5 when correction flows were concrete. This argues for iterative review of type design, not upfront design | Compare type definition at pass 2 (`ParsingSource?` reused) vs pass 5 (dedicated `CorrectionSource` enum) — the need for separation wasn't visible until edit flows were enumerated | Raw |
| Feb 21 | M8.4 | Telemetry/ProvenanceRules | When telemetry attribution depends on runtime state (is `parserUsed` still in memory?), define provenance rules per call site rather than assuming uniform availability. A 4-row table mapping edit flows to `parserUsed` availability made an underspecified metric precise and testable | Review provenance table — each flow explicitly states yes/no for parserUsed availability with rationale | Raw |
| Feb 21 | M8.4 | Process/IterativeReview | A second review pass on an updated PRD catches different issues than the first — pass 1 finds architectural gaps, pass 2 finds contract and migration gaps introduced by the fixes. The reviewer's fresh read of updated text catches inconsistencies the editor missed | Compare pass 1 findings (CRF parity, model I/O) vs pass 2 findings (non-existent fields, schema evolution) — no overlap | Raw |
| Feb 21 | M8.4 | Telemetry/SchemaMigration | Optional Codable fields (`String?`) give free JSON schema migration — `JSONDecoder` silently defaults missing optional fields to `nil`. No migration code, no version gating on load. This is a major advantage of JSON-based telemetry over Core Data schema evolution where every field change requires a migration step | Encode v3 event (with `parserUsed`), decode v2 event (without it) — both succeed, missing field is nil | Raw |
| Feb 21 | M8.4 | Architecture/GateRelaxation | When a quality gate depends on infrastructure that doesn't exist yet (e.g., "50 linked corrections" requiring a non-existent `parseEventId` field), relax the gate to match what's achievable rather than expanding scope to build the infrastructure. The gate's intent (minimum data volume) is preserved without a hidden prerequisite chain | Compare "50 linked corrections" (requires Core Data v7 migration) vs "50 corrections" (works today) — same retraining quality bar | Raw |
| Feb 21 | M8.4 | Telemetry/Attribution | Winner-only attribution (`parserUsed` = the parser that produced the returned result) is cleaner than router-level attribution (`"hybrid"`) for analytics. Per-parser accuracy measurement becomes a simple group-by query. A `"hybrid"` label would require additional metadata about which parsers were consulted vs which won — complexity with no current use case | Query corrections grouped by `parserUsed` — each group measures one parser's accuracy directly | Raw |
| Feb 21 | M9.5 | Swift/DI | Default parameters (`parser: IngredientParser = HybridIngredientParser()`) provide zero-blast-radius DI in Swift. All 11 existing call sites compile unchanged while tests can inject mocks. This avoids DI containers, Environment keys, or ServiceFactory patterns — the right tool for scoped DI where only a few constructors need injection | Grep for `IngredientParsingService(context:` — all existing call sites compile with no changes | Raw |
| Feb 26 | M10.5 | FoundationModels/Hallucination | Foundation Models systematically converts grams to kilograms uninstructed: `250g chickpeas` → `qty=0.25, unit=g` (should be qty=250). This is a silent 1000x error that corrupts grocery quantities. Also invents units (`1 cucumber` → `unit=g`) and assigns wrong units (`4 slices bread` → `unit=clove`). FM is unreliable for numeric extraction but useful for soft tasks (category suggestion, template dedup) | Run `RecipeCorpusFMComparisonTests` on device with Apple Intelligence — check FM results for `250g`, `200g`, `300g` inputs | Raw |
| Feb 26 | M10.5 | Parser/BulletPrefix | Leading bullet prefixes (`"- "`, `"• "`, `"* "`, `"1. "`) are the single root cause of ~70% of pipeline errors (206/295). Both `OCRLineClassifier.startsWithNumber()` and `RegexIngredientParser`'s `^`-anchored patterns fail because the prefix character is at position 0, not the actual content. Recipe lines from web extraction commonly retain list formatting. Fix: strip prefixes before scoring/parsing, preserve original text in output | Add `"- 2 cups flour"` to classifier tests — before fix: scores 0 ingredient; after fix: scores 0.6+ | Raw |
| Feb 26 | M10.5 | Parser/MetricNoSpace | Metric measurements without spaces (`400g`, `250ml`, `1.5L`) account for ~120 parsing failures. The existing pre-processor `^(\d+)\s*([a-zA-Z])` does separate digits from letters, but the real issue is that bullet prefixes (Fix 1) prevent the `^` anchor from matching. After bullet stripping, most metric patterns resolve naturally through the existing pre-processor | Test `"400g mushrooms"` directly (no prefix) — existing pre-processor handles it; test `"- 400g mushrooms"` — fails without bullet strip | Raw |
| Feb 26 | M10.5 | LLM/APIArchitecture | External LLM APIs (Claude, GPT, Gemini) with user-owned API keys stored in iOS Keychain provide a clean architecture: no server-side proxy, no data leaves device without opt-in, user controls costs. The fallback chain (LLM API → on-device FM → deterministic pipeline) ensures the app always works offline. Claude Haiku at ~$0.001/recipe makes LLM parsing nearly free for typical usage (5-20 recipes/month = $0.005-$0.02) | Estimate: 15 ingredients × ~35 tokens/line = 525 input tokens per recipe. Haiku input price: $0.80/MTok → $0.00042/recipe | Raw |
| Feb 26 | M10.5 | Process/LLMCorpusReview | Using 5 parallel Claude agents (one per corpus category) to review 50 recipes identified 295 errors and distilled them into 7 systematic patterns in ~12 minutes vs estimated 3-4 hours for human review. The aggregation step — going from 295 individual errors to 7 patterns — is where LLM review adds the most value over manual review. Individual error spotting is tedious; pattern recognition across hundreds of cases is the strategic capability | Compare time: 12 min LLM + 5 min human review of patterns vs 3-4 hours of line-by-line human review | Raw |
| Feb 26 | M10.5 | Regex/CharClassTrap | A regex character class `[\.\):\s]` intended to match "period, close-paren, or colon" also matches whitespace due to `\s`. This caused `"2 cups flour"` to be treated as a numbered list (digit + space). The same bug existed in TWO patterns: `stripBulletPrefix` AND `numberedStepPattern`. Both needed fixing to `[\.\):]`. Character classes should be reviewed for unintended `\s` inclusions — it's a silent, cascading failure mode | Test "2 cups flour" through classifier — before fix: instruction; after fix: ingredient | Raw |
| Feb 26 | M10.5 | Parser/CorpusImpact | After fixing 7 pipeline bugs + 2 cascading regex issues, corpus results improved: ingredients detected 442→477 (+7.9%), classification confidence 0.520→0.641 (+23.3%), parsing confidence 0.932→0.984 (+5.6%). Regex parser now handles 79.8% of lines (was 18.3%) — the ML parser is no longer compensating for regex failures. The biggest improvements came from no-headers (+27, +54%) and messy (+18, +55%) categories | Run `testRunFullCorpus()` — compare per-category ingredient counts to baseline | Raw |
| Feb 26 | M10.5 | Parser/PipelineVsFM | After pipeline fixes, the deterministic pipeline now beats Foundation Models on quantity extraction: 88.3% vs 63.5% (before fixes it was 65.2% vs 78.7%). FM disagreements dropped from 269 to 210, and FM "fixes pipeline gaps" dropped from 126 to 33. This validates investing in deterministic pipeline improvements over relying on FM — the pipeline is more reliable, faster, works offline, and now more accurate | Run `testFMvsPipelineComparison()` — compare qty extraction rates | Raw |
| Feb 21 | M9.5 | Swift/DI | Converting `private static let` properties to instance-level `let` properties assigned in `init` makes constants injectable while preserving immutability. The static → instance transition has zero performance cost (no synchronization needed) and enables test-time configuration (e.g., `regexConfidenceThreshold: 0.9`) without touching production defaults | Create HybridIngredientParser with custom threshold → verify routing changes | Raw |
| Feb 21 | M9.5 | Testing/Mock | A mock parser that tracks `parseCalls: [String]` enables negative assertions ("NLP should NOT be called when regex is confident") which are impossible with real implementations. Call-counting mocks catch routing bugs where both parsers fire when they shouldn't — saving NLP overhead on 85%+ of inputs | Run testHighConfidenceRegexSkipsNLP — verify `mockNLP.parseCalls.count == 0` | Raw |
| Feb 21 | M8.4 | ML/CRFTransitions | CRF transition matrix reveals learned grammar: QTY→UNIT has highest weight (1.28), NAME→NAME self-transition (1.32) captures multi-word ingredients, PREP→PREP (1.63) and COMMENT→COMMENT (1.69) model multi-word phrases. Start transitions favor QTY (0.59) reflecting that most ingredients begin with quantities | Inspect `transitions.json` — verify high-weight transitions match ingredient grammar intuition | Raw |
| Feb 21 | M8.4 | ML/ClassImbalance | MODIFIER label has lowest F1 (0.9261) due to severe class imbalance — only 1.1% of tokens vs NAME at 30.4%. The model still exceeds the 0.90 target but this is the weakest label. Future improvements: targeted augmentation or upsampling for MODIFIER examples | Check `dataset_statistics.json` label distribution — MODIFIER is smallest class | Raw |
| Feb 21 | M8.4 | ML/MPSTraining | Apple Silicon MPS (Metal Performance Shaders) is viable for small-to-medium model training — 1.35M parameter BiLSTM-CRF trained in ~39 min on MPS vs estimated 2+ hours on CPU. MPS device selection is automatic via `torch.backends.mps.is_available()` | Run `train_model.py` — observe MPS device selected and ~90s/epoch | Raw |
| Feb 21 | M8.4 | ML/EarlyStopping | Early stopping with patience=5 found optimal model at epoch 21 while training ran to epoch 26. The gap between train loss (0.17) and val loss (0.20) at stopping suggests mild overfitting — dropout 0.5 effectively controlled it. Patience=5 is a good default for this model size | Plot train vs val loss curves — divergence begins around epoch 13, best val at epoch 21 | Raw |
| Feb 21 | M8.4 | Python/StdoutBuffering | Python buffers stdout by default when not connected to a terminal (e.g., background execution). Training output was invisible until the process completed. Fix: `python -u` flag disables buffering, or `PYTHONUNBUFFERED=1` env var. Always use `-u` when running long training jobs in background | Run `python train.py` in background → no output; run `python -u train.py` → output streams live | Raw |
| Feb 21 | M8.4 | Python/EmDashEncoding | Python source files use ASCII `--` (double hyphen) but Markdown files often contain Unicode em dash `—` (U+2014). String replacement in `update_model_card()` silently failed because the search key didn't match. Fix: use `\u2014` escape in Python code to match the actual file content | Write replacement with `--` → replacement fails silently; use `\u2014` → replacement succeeds | Raw |
| Feb 16 | M15 | Design/ActionButtonProminence | Text-only action links (Done / Swap / Remove) inside cards disappear visually — they read as metadata, not interactive controls. Pill-shaped outlined buttons with icons create a distinct "action row" that users scan separately from card content. Equal-width buttons (`flex: 1`) reinforce that all three are peer-level actions | Compare text links vs pill buttons — users tap pill buttons faster due to clear affordance | → LN 33 |
| Feb 16 | M15 | Design/DestructiveConfirmation | A generic "Are you sure?" dialog is less useful than one that tells the user *why* removal matters. If ingredients from a meal's recipe are already on a grocery list, the confirmation says so — the user can decide whether to also clean up the list. "Informative confirmation" pattern vs the "speed bump" anti-pattern | Show alert with ingredient count ("3 ingredients from this recipe are on your grocery list") — user makes informed decision vs blind confirmation | → LN 33 |
| Feb 16 | M15 | Design/NavBarCentering | iOS nav bar titles must be centered relative to the full screen width, not the space remaining after back/trailing buttons. CSS approach: title with `position: absolute; left: 0; right: 0; text-align: center; pointer-events: none` floats in a center layer. Back and trailing buttons position independently at edges. This mirrors how `UINavigationBar` works internally — the `titleView` is centered in the full bar, `leftBarButtonItems`/`rightBarButtonItems` float at edges | Toggle dark mode — title stays centered regardless of back button text length ("‹ Lists" vs "‹ Settings") | → LN 33 |
| Feb 16 | M15 | Design/RecipeDetailNoNavTitle | Recipe detail screens benefit from omitting the nav bar title entirely when the hero section immediately below contains the same text. Showing "Honey Garlic Salmon" in the nav bar AND in the hero header is redundant — it wastes vertical space and creates a "stuttering" effect. The nav bar shows only "‹ Recipes" + Edit/••• actions; the recipe name appears once in the 28pt hero | Compare with/without nav title — single hero title reads cleaner and reclaims ~44pt of vertical space | → LN 33 |
| Feb 21 | M8.4 | ML/DocumentVsToken | Foundation Models and BiLSTM-CRF solve fundamentally different problems. Foundation Models excels at document-level understanding (which lines are ingredients vs. instructions). BiLSTM-CRF excels at token-level extraction (what is the quantity, unit, name within one ingredient line). The ideal recipe import pipeline uses Foundation Models for section detection and BiLSTM-CRF for ingredient parsing — complementary, not competing | Compare: LLM on "2 cups flour" (slow, overkill) vs BiLSTM-CRF on full recipe text (no section detection). Each tool where strongest | Raw |
| Feb 21 | M8.4 | iOS/ShareExtension | iOS Share Extensions are limited to ~120MB memory. Foundation Models loads a ~1.2GB model. This means Foundation Models CANNOT run in a Share Extension — recipe import via Share Sheet must extract URL/metadata only, with all AI processing deferred to the main app. This constraint reinforces minimal share extension architecture | Attempt FoundationModels import in share extension → memory crash | Raw |
| Feb 24 | M8.4.1 | Parser/Normalization | `IngredientTemplateService.removeVariations()` conflated identity qualifiers (ground, dried, frozen — change WHAT an ingredient IS) with preparation qualifiers (diced, chopped, sliced — describe what you DO to it). Stripping identity qualifiers caused "ground beef" → "beef", "unsalted butter" → "butter", etc. Fix: reduce strip list from 30+ to 9 pure preparation qualifiers | `service.normalize(name: "ground beef")` returns "ground beef", not "beef" | Raw |
| Feb 24 | M8.4.1 | Parser/DataDriven | Mining the strangetom training data (68,846 samples) for qualifier-as-NAME occurrences found 3,032 unique compound ingredients. "ground" appears as NAME 838 times (ground beef 46x, ground cinnamon 79x), "fresh" 4,523 times, "unsalted" 904 times. The training data already encodes which qualifiers are identity-forming — use data to drive normalization decisions, not hand-curated lists | Run `python3 -c` script against train.jsonl to count qualifier→NAME occurrences | Raw |
| Feb 24 | M8.4.1 | Parser/PipelineOrder | The 3-tier parser (regex→ML→NLP) correctly produces `name: "ground beef"`. The normalization layer (`IngredientTemplateService.normalize()`) was undoing the parser's correct work. Bug location was NOT where the symptoms appeared — always trace the full pipeline before fixing | Add logging at each normalize() phase to see where "ground" gets stripped | Raw |
| Feb 24 | M8.4.1 | Parser/CompoundPlural | `normalizePlural()`'s `preferPlural` map only matched single-word entries. Compound ingredients like "dried cranberries" need a last-word lookup — extract "cranberries" from the compound and check it against `preferPlural`. Without this, "dried cranberries" → "dried cranberry" | `service.normalize(name: "dried cranberries")` returns "dried cranberries", not "dried cranberry" | Raw |
| Feb 24 | M10 Spike | Web/FalsePositiveGuard | `__NEXT_DATA__` payloads can contain non-recipe objects with time fields (cookTime, prepTime) that look recipe-like. BBC Good Food's __NEXT_DATA__ had such an object, causing a false positive (recipeFound=true with only imageURL="Image"). Tightened `findObjectWithRecipeKeys()` to require `recipeIngredient` as mandatory key plus ≥1 secondary key. This eliminated the false positive with zero impact on legitimate extractions | Run spike → BBC Good Food correctly shows recipeFound=false; all 12 genuine recipes still extract | Raw |
| Feb 24 | M10 Spike | Codable/ComputedMetrics | Swift's auto-synthesized `Codable` conformance skips computed properties — only stored properties are encoded. `ExtractionReport` had 12+ computed metrics (fullExtractionCount, medianTime, etc.) that were invisible in the JSON output. Fix: custom `encode(to:)` with explicit `SummaryPayload` struct. Lesson: if a report needs to be self-contained, computed properties must be explicitly serialized | Decode extraction-report.json → check for `summary` key with all metrics present | Raw |
| Feb 24 | M10 Spike | Swift/TupleCapture | When a function sets `ctx.extractionMethod = "ld+json"` AFTER calling a strategy that returns `(recipe, context)`, the returned tuple captures the context BEFORE the method was set. Returning `result` (which contains the old context) instead of building a fresh tuple from `(result.recipe, ctx)` causes the method to always be "none". Tuple value capture is a subtle source of stale-state bugs | Check extraction-report.json extractionMethod values — should be ld+json/inline-script/none, never all-none | Raw |
| Feb 24 | M10 Spike | Architecture/AtomicImport | `RecipeService.createRecipe()` calls `save()` immediately (line 44), while `parseAndConnectIngredients()` creates Ingredient entities without saving. Calling both sequentially produces two separate commits, not one atomic transaction. Import requires a new `importRecipe(from:ingredientTexts:)` method that defers the save — create Recipe + connect ingredients + save once. The existing `createRecipe()` API stays unchanged for manual recipe creation | Check RecipeService.swift:44 save() call; IngredientParsingService.swift:149 creates but doesn't save | Raw |
| Feb 24 | M10 Spike | Codable/ClassificationDrift | When computed classification logic (`classifySuccess()` checking 3 core fields) is separate from display logic (`fieldsMissing` checking 6 fields), they can disagree. A recipe with title + ingredients + instructions but missing prepTime would be "FULL" in the report but "PARTIAL (prepTime)" in the console. Fix: single source of truth via `ExtractedRecipe.successLevel` used by both report and CLI | Run spike CLI → console labels match report classification for all 12 extracted recipes | Raw |
| Feb 24 | M10 Spike | Process/ReviewRegenerate | After fixing extraction code bugs, regenerating the report and re-running metric calculations is essential. The BBC false positive removal changed totals from 13/28 to 12/28, changed full/partial split from 9+4 to 12+0, and updated per-field percentages. All downstream documents (PRD, acceptance criteria, test matrix) needed corresponding updates — stale data in any one creates internal inconsistency | Diff extraction-report.json pre/post fix → verify BBC entry changed, summary metrics updated | Raw |
| Feb 21 | M8.4 | Architecture/ConvergencePoint | All three recipe import paths (URL, text paste, photo) converge at the same point: an array of ingredient strings flowing through `parseAndConnectIngredients()`. Improving this shared bottleneck (via M8.4's ML parser) benefits all import modes simultaneously with zero import-specific code | Trace URL import → JSON-LD extract → recipeIngredient[] → same function as manual recipe creation | Raw |
| Feb 21 | M8.4 | Parsing/MealieWarning | Mealie's open-source recipe manager documents the #1 parser UX pitfall: re-parsing destroys user edits. When users modify parsed ingredients, clicking "Parse" again overwrites corrections with fresh parse results. Forager's correction instrumentation (M8.4 Phase 7) avoids this — corrections are logged but never override source text | Review Mealie issue #5336 — data loss on re-parse. Forager logs corrections separately from parse results | Raw |
| Feb 26 | M10.6 | Architecture/SeparateProtocol | `LLMIngredientParser` is a separate protocol from `IngredientParser` because the API shapes are fundamentally different: sync+per-line vs async+batch. Forcing `IngredientParser` to become async would cascade through `HybridIngredientParser`, `IngredientParsingService`, and all 11+ call sites. The LLM lives at the `RecipeImportService` level (stage 2 bypass), not inside the hybrid router | Verify: `IngredientParser` stays sync, all 267+ tests pass unchanged after M10.6 implementation | Raw |
| Feb 26 | M10.6 | Architecture/BypassNotTier | The LLM integration acts as a pipeline **bypass** (skip all 3 tiers), not a 4th tier within the hybrid router. This is cleaner because: (1) LLM is batch while tiers are per-line, (2) LLM has network latency that would slow down the router for all lines, (3) the fallback semantics are different (LLM fails → use entire pipeline, not "try next tier") | Trace import flow: LLM success skips pipeline entirely; LLM failure → full regex→ML→NLP chain | Raw |
| Feb 26 | M10.6 | LLM/ToolUseStructuredOutput | Anthropic's `tool_use` feature guarantees structured JSON output matching a schema — no free-form text parsing needed. This eliminates the "LLM sometimes returns markdown" or "LLM adds commentary" failure modes. The tool definition schema maps directly to `LLMParserResult` fields, making the response→struct mapping trivial | Send parse request with tool_use → verify response contains exactly the schema fields, no extra text | Raw |
| Feb 26 | M10.6 | Architecture/FMExclusion | Foundation Models is intentionally excluded from the LLM fallback chain despite being "on-device AI." The M10.5 spike proved FM systematically converts g→kg (silent 1000x error), invents units for unitless items, and assigns wrong units. The fallback chain is `LLM API → deterministic pipeline` only. FM may have a future role in soft tasks (category suggestion) but not for quantity parsing | Run FM on `"250g chickpeas"` → observe qty=0.25 instead of qty=250; pipeline returns correct qty=250 | Raw |
| Feb 26 | M10.6 | Strategy/ValidateFirstProvider | M10.6 implements Claude-only despite the protocol supporting multiple providers. This "validate the pattern first" strategy avoids multi-provider testing and UI complexity before knowing if anyone uses the feature. Adding GPT/Gemini adapters is trivial once the protocol, settings, and integration point are proven — each adapter is ~4-6 hours | Implement M10.6 Claude-only → measure adoption → decide if M10.7 multi-provider is warranted | Raw |
| Feb 26 | M10.6 | Architecture/AvoidSchemaChange | LLM settings use UserDefaults + Keychain instead of Core Data to avoid a v7 migration. The trade-off: settings don't sync via CloudKit. This is acceptable because API keys are personal in M10.6 (household sharing deferred). The general principle: if a feature can avoid Core Data schema changes, it should — every migration adds risk to CloudKit's append-only Production schema | Check: `isLLMEnabled` in UserDefaults, API key in Keychain — no new Core Data entities, no `.xcdatamodeld` change | Raw |
| Feb 26 | M10.6 | Process/PRDExtraction | The "spike → extract PRD" pattern is proving repeatable: spike produces raw findings + rough design embedded in a research doc, then a separate session formalizes into a standalone implementation-ready PRD. The spike stays as historical record; the PRD is the actionable contract. This avoids implementers needing to cross-reference irrelevant spike context during development | Compare M10 spike→PRD (session 23) and M10.5 spike→M10.6 PRD (session 51) — same pattern, same benefit | Raw |
| Feb 26 | M10.6 | LLM/OAuthDead | As of Jan 2026, third-party OAuth is not viable for any major LLM provider. Anthropic banned it explicitly. OpenAI's OAuth is for ChatGPT→your-backend direction only. Google Gemini supports it but with consent screen review friction. API keys with UX mitigation (deep links, prefix validation, test connection) is the only universal approach — and turns a 5-min setup into 30 seconds | Check Anthropic policy docs (Feb 2026 update), OpenAI Apps SDK docs, Google AI Studio | Raw |
| Feb 21 | M8.4 | Architecture/PitfallOwnership | Implementation pitfalls in a multi-layer pipeline split cleanly by *where in the pipeline* they occur. Parser-layer pitfalls (tokenizer parity, CRF export, unit canonicalization, confidence calibration) belong to the parser milestone. Pre-parser pitfalls (schema.org group header stripping, double-parse avoidance, OCR error correction) belong to whichever feature feeds text into the parser. Categorizing by pipeline layer prevents scope creep in both directions | Map each pitfall to its pipeline layer — parser pitfalls are already in M8.4 PRD phases; pre-parser pitfalls are notes for future import PRD | Raw |
| Feb 16 | M15 | Design/EmptyStateHierarchy | Empty states have a visual hierarchy: large icon → title → subtitle → CTA. The icon size and container create the "emotional hook" (what this space could become), the title names the state, the subtitle gives context, and the CTA provides escape. Positioning at ~40% from top (not centered) avoids the "lost in space" feeling — content occupies the upper-third reading zone | Compare vertically-centered vs 40%-from-top placement — upper placement feels purposeful, centered feels like a loading spinner | → LN 33 |
| Feb 16 | M15 | Design/SearchHighlightPattern | Highlighting search matches in `--accent-primary` with `<strong>` tags inside result text creates a visual "connect the dots" effect — the user's eye jumps between highlighted fragments across result rows, confirming each result's relevance at a glance. This works because the accent color is already associated with "active/selected" throughout the app | Search "garlic" — highlighted matches in recipe names, ingredient names, and grocery items all use the same accent treatment | → LN 33 |
| Feb 16 | M15 | Design/SyncFeedbackLevels | Three-tier sync feedback (progress → error → unavailable) maps to three UX severities: informational (blue, auto-dismiss), warning (amber, user-actionable retry), and blocking (full-screen empty state, system-level fix). The key insight is that sync-in-progress and sync-error are non-blocking — the user can always interact with locally-saved data. Only "no iCloud" replaces the full content area | Toggle airplane mode during sync — warning banner appears, content remains interactive. Disable iCloud account — empty state replaces all content | → LN 33 |
| Feb 16 | M15 | Design/SwipeMockupTranslateX | Static mockups can show mid-swipe states using CSS `transform: translateX(±80px)` on the content layer over an absolutely-positioned background layer. The `overflow: hidden` on the container clips cleanly. This communicates the swipe interaction without requiring animation, and is immediately recognizable to iOS users from muscle memory | Visual: swipe mockups show revealed action areas at consistent 80px offset — matches iOS default leading/trailing swipe distance | → LN 33 |
| Feb 16 | M15 | Design/EditModeFocusIndicator | In inline edit mode, the focused field should be visually distinct from unfocused fields. Using `--accent-primary` for the active field's bottom border (instead of `--border-default`) plus a pipe cursor character creates a clear "I'm editing this one" signal without disrupting the overall form layout. The 2px border on household name editing (vs 1px on recipe fields) signals "primary editable content" vs "row-level fields" | Tap between ingredient fields in recipe edit — border color changes on focus, cursor appears at insertion point | → LN 33 |
| Feb 16 | M15 | iOS/NavBarAddButton | The trailing nav bar `+` is the only correct iOS pattern for "create new item" on list screens. FABs (floating action buttons) are Android-native and fight with iOS tab bars and `.safeAreaInset` bottom bars. Inline dashed "add" cards scroll off-screen on long lists and conflate "card" with "action". Apple Notes, Reminders, Calendar, and Files all use trailing `+` | Check Apple's first-party apps — all use trailing nav bar `+` for creation on list screens | → LN 33 |
| Feb 16 | M15 | CSS/SemanticTokensDarkMode | Using `var(--btn-primary-text)` instead of hardcoded `#FFFFFF` on accent backgrounds eliminates the need for separate `[data-theme="dark"]` overrides. Before: each element had two rules (base `#FFFFFF` + dark override `#1C1A14`). After: one rule using the semantic variable that flips automatically. Removed 4 redundant dark mode overrides. Every manual dark mode override is a maintenance liability; every semantic variable is one fewer thing that can go stale | Toggle dark mode theme — all accent-on-white elements maintain contrast without individual overrides | → LN 32 |
| Feb 16 | M15 | Design/TapTargetSeparation | CSS mockups should reflect *visual* sizes (28px `+` button, 24px pills) while the SwiftUI implementation handles *hit areas* (44pt minimum via `.contentShape(Rectangle())` or `.frame(minHeight: 44)`). Making elements visually 44pt would look clunky — the standard iOS practice is small visual elements with large invisible hit targets. Document this split in the PRD to prevent implementers from inflating visual sizes | Compare 28pt visual `+` with 44pt invisible hit area — tappability improves without visual bloat | → LN 33 |
| Feb 16 | M15 | Design/PRDSyncAudit | After a batch of mockup changes, grepping the PRD for stale terminology (old button names, removed CSS patterns, outdated layout descriptions) catches drift invisible when reading sequentially. A single session's changes can touch 6+ PRD sections — any one being stale during implementation causes confusion about which mockup is authoritative | After mockup changes: `grep -i "Shopping List\|Add All\|✕.*Remove\|icon-only"` the PRD — stale hits = drift | → LN 33 |
| Feb 16 | M15 | Design/TextTertiaryContrast | `--text-tertiary` at `#7A7067` on `#FDFBF7` canvas is ~4.1:1 contrast — passes WCAG AA for large text (18px+) but fails for normal text. At 12px (category headers, captions), this is a real accessibility failure. Darkening to `#6A6057` bumps to ~5.8:1, passing AA at all sizes. The visual difference is subtle but the accessibility impact is significant | Use WCAG contrast checker: `#6A6057` on `#FDFBF7` = 5.8:1 vs `#7A7067` = 4.1:1 | → LN 33 |

| Feb 16 | M15 | CSS/TypeScaleVariables | Defining `--font-*` CSS variables as the canonical type scale without refactoring existing rules creates a contract for implementation. Individual CSS rules still use raw `px` values, but the variable definitions are the single source of truth. During M15.2 (SwiftUI migration), developers reference the variables — not grep for `font-size` values scattered across 100+ rules. Define the scale early, migrate incrementally | Check `:root` for 8 `--font-*` variables matching the PRD 4.2.2 table exactly | → LN 32 |
| Feb 16 | M15 | CSS/OrphanedVariables | CSS custom property definitions have zero runtime cost but real maintenance cost — every unused variable is a false signal to developers scanning `:root` for the design system's actual tokens. `--r-xl: 20px` was defined but never referenced by any `var(--r-xl)` — removing it tightens the contract. Conversely, `--info-fg`/`--info-bg` are currently unused but will be needed for Phase 3 error states — keeping them is correct because their future use is planned and imminent | `grep 'var(--r-xl)' *.html` returns 0 results → safe to remove; `grep 'var(--info-' *.html` returns swatch reference → keep for Phase 3 | → LN 32 |
| Feb 16 | M15 | CSS/ColorMixFunction | `color-mix(in srgb, var(--cat-produce) 12%, transparent)` derives category chip backgrounds from their text color variable — one source of truth per category. Previous approach used hardcoded `rgba()` values that duplicated and could drift from the `--cat-*` variables. `color-mix()` has 96%+ browser support (2024+) and is the modern replacement for opacity hacks | Toggle dark mode — chip backgrounds automatically derive from dark-mode category colors without any override | → LN 32 |
| Feb 16 | M15 | Design/ProgressBarDarkMode | Translucent backgrounds (`rgba()`) that look correct in light mode can become invisible in dark mode. The progress bar track used `rgba(176,168,158, 0.3)` — fine on `#FDFBF7` canvas, but nearly invisible on `#1C1A14` dark canvas. Using `var(--bg-tertiary)` ensures the track is always distinguishable from the canvas because semantic tokens already have tested dark-mode values | Toggle dark mode — progress bar track is visible against both light and dark canvas | → LN 32 |
| Feb 16 | M15 | PRD/TabArchitectureReduction | When restructuring navigation (6 tabs → 5), document the "what moved where" mapping explicitly — not just the new structure. Developers familiar with the old layout need a migration path: "Ingredients → Settings > Categories" prevents them from searching for a deleted tab. The before/after table format in §6 makes the mapping scannable | Check PRD §6 "Tab Architecture Change" table — every old tab has a destination in the new structure | → ADR 011 |
| Feb 16 | M15 | PRD/CrossCuttingPatterns | UI patterns that appear across multiple screens (celebration, dialogs, edit modes, loading/error, swipe actions) belong in the shared components section (§5), not duplicated in each screen spec. Screen-specific sections (§6) reference the shared patterns. This prevents drift when a pattern changes — update one place, all screens inherit the change | Check that §6 screen specs reference §5 patterns by section number (e.g., "see §5.9 Celebration State") rather than inlining pattern details | → LN 33 |
| Feb 16 | M15 | Design/PillRadiusHierarchy | Three-tier border-radius creates a visual hierarchy without adding color or size differences: capsule (999px) for primary navigation controls (filter pills), rounded-rect (`--r-sm: 8px`) for content-level interactive pills (timing, scale), and compact (`--r-xs: 4px`) for informational badges (category chips). Users instinctively recognize capsule shapes as "filters" and rectangles as "content" — matching iOS system conventions | Compare filter pills (capsule) vs scale pills (rounded-rect) vs cat chips (compact) — visual hierarchy is clear without labels | → LN 33 |
| Feb 16 | M15 | Design/ButtonPaddingVsFontSize | Consistent padding (14px) across all full-width buttons ensures equal tap target height regardless of button style. Font-size variation (17px primary/secondary vs 15px outline/danger) creates intentional visual hierarchy — primary actions are louder, outline actions are quieter. This separates two concerns: ergonomics (padding = touch target) from hierarchy (font-size = visual weight) | Measure rendered height of `.btn-primary` vs `.btn-outline` — padding-based height is identical; visual weight differs | → LN 33 |

| Feb 16 | M15 | CSS/SpecificityBorderTrap | When a class uses `border` shorthand (`.ingredient-lib-card { border: 1px solid ... }`), it resets ALL four borders — including `border-left` set by an earlier class (`.cat-strip { border-left: 4px solid ... }`). Same specificity + later source order = shorthand wins. Compound selectors (`.ingredient-lib-card.cat-strip`) fix this by bumping to 2-class specificity. This explains why dairy (`.cat-strip.dairy` = 2 classes) showed its color but produce (`.cat-strip` = 1 class) didn't | Inspect element in browser — produce items get `border-left: 4px solid var(--cat-produce)` with compound selector override | → LN 32 |
| Feb 16 | M15 | Design/MockupFrameHeight | Design system mockups should use fixed `height` (not `min-height`) on phone frames so all screens appear identical. `min-height: 852px` causes detail screens with more content to grow taller than overview screens, making side-by-side comparisons misleading. Fixed `height: 852px` with `overflow-y: auto` on the content area lets users scroll within individual frames — matching real phone behavior | Compare overview vs detail screens — both are 852px tall, detail screen scrolls internally | → LN 33 |
| Feb 16 | M15 | CSS/HardcodedColorSweep | After a batch of mockup work, grep for literal hex values (`#FFFFFF`, `#000`, `white`, `black`) outside of `:root` variable definitions. Each hit is a dark mode liability — it won't flip when the theme changes. Replacing with semantic variables (`var(--surface-primary)`, `var(--btn-primary-text)`) eliminates redundant `[data-theme="dark"]` overrides. In this review: 5 inline `#FFFFFF` values in swipe demos, day-chip dot, and toggle thumb were all replaced, removing 1 manual dark mode override in the process | `grep '#FFFFFF' mockup.html` — only `:root` definitions should remain; zero hits outside variable declarations | → LN 32 |
| Feb 17 | M15 | Design/ParseConfidenceFeedback | Real-time parse confidence indicators on ingredient entry (green ✓ for high confidence, amber ● for low) give users immediate feedback about whether the parser understood their input — without requiring them to inspect parsed fields. The indicator auto-dismisses after 1.5s to avoid visual clutter. This maps directly to `IngredientParsingService`'s confidence score: ≥0.8 = green, <0.8 = amber | Type "2 cups flour" → green checkmark appears trailing; type "a handful of herbs" → amber dot appears | → LN 33 |
| Feb 17 | M15 | Design/OnboardingPagePattern | Onboarding screens use a consistent template (icon circle → title → subtitle → page dots → CTA) that varies only the content per screen. This template maps to a SwiftUI `TabView(.page)` where each page is identical structurally — only the icon, text, and CTA label change. The "Skip" button on every screen respects users who prefer to explore by doing. Screen 4 ("Get Started") replaces "Next" with a primary action that dismisses onboarding and sets `UserDefaults.hasCompletedOnboarding` | Swipe through 4 screens — layout is identical, only content changes; tap "Skip" on screen 1 — goes directly to main app | → LN 33 |
| Feb 17 | M15 | Design/InlineValidation | Inline validation (red border + caption below field) is less intrusive than modal alerts for form errors. The error state is visually anchored to the specific field that needs attention — the user's eye doesn't need to leave the form to read an alert and then find the problematic field. This pattern matches iOS system apps (Contacts, Settings) where validation is always inline | Tap Save with empty title → red border appears on title field, "Recipe name is required" caption below, no modal | → LN 33 |
| Feb 17 | M15 | PRD/FileToMockupMapping | Adding a "Swift File → Mockup Screen" mapping table to the PRD eliminates the mental translation layer during implementation. Without it, developers must search the PRD for screen names, then figure out which Swift file to modify. With it, they go directly from file to mockup to spec section. Particularly valuable for new contributors who don't know the codebase's file naming conventions | Check §8 mapping table — every mockup has a corresponding Swift file; `SearchView.swift` and `OnboardingView.swift` marked as new files | → LN 33 |

| Feb 17 | M15.3 | SwiftUI/List | `List` with `.listRowBackground(Color.clear)` + `.listRowSeparator(.hidden)` preserves `.swipeActions()` while enabling fully custom card-style row rendering. `ScrollView` + `LazyVStack` would break swipe actions entirely — `.swipeActions()` is a `List`-only modifier | Add `.swipeActions` to both List and ScrollView rows — only List row shows swipe | Raw |
| Feb 17 | M15.3 | SwiftUI/SafeAreaInset | `.safeAreaInset(edge: .bottom)` participates in the safe area calculation — List/ScrollView content automatically insets to avoid the sticky bar, unlike `.overlay()` which covers content. The bar also pushes up naturally when the keyboard appears | Show keyboard with `.safeAreaInset` bottom bar — bar rises above keyboard; compare with `.overlay` — content hidden behind bar | Raw |
| Feb 17 | M15.3 | SwiftUI/FilterPill | SwiftUI idiom: filter pills should be pure views (not buttons with action closures). Callers wrap in `Button` or `Menu` as needed. This keeps the component composable — a `Menu` wrapper with long-press options can't wrap a component that's already a `Button` | Wrap FilterPill in Menu vs Button — both work cleanly because FilterPill is a pure view | Raw |
| Feb 17 | M15.3 | CoreData/Relationships | `GroceryListItem.sourceRecipes` is an `NSSet?` relationship to Recipe — not a string property. Use `addToSourceRecipes(recipe)` to link items. The computed property `sourceRecipeNames` extracts display names from the set. Always check Core Data model for relationship types before assuming string fields | Read `GroceryListItem+CoreDataProperties.swift` — `sourceRecipes: NSSet?` with generated add/remove methods | Raw |

| Feb 17 | M15.4 | SwiftUI/Scaling | Inline scale pills outperform modal scaling sheets for recipes because the user stays in context while adjusting portions. `RecipeScalingService.scale(recipe:scaleFactor:)` returns pre-formatted `ScaledIngredient.displayText` strings — the view just swaps them in when `scaleFactor != 1.0`. Combined with `presentationDetents([.medium])` for the custom picker, the full scaling UX happens without ever leaving the detail view | Change scale from 1× to 2× — ingredient quantities update inline; tap custom gear — half-sheet appears without obscuring recipe | Raw |
| Feb 17 | M15.4 | SwiftUI/DisclosureGroup | `DisclosureGroup` is ideal for de-emphasizing optional content (usage analytics) that doesn't merit a full section header. It collapses by default and the user can expand if interested. Unlike `ForagerSectionHeader` (which requires manual `@State` for collapse), DisclosureGroup manages its own expansion state | Tap "Usage" label — section expands in-place with spring animation; tap again — collapses | Raw |
| Feb 17 | M15.4 | Swift/Regex | `cleanStepText()` using `#"^(?:Step\s+)?\d+[.):]?\s*"#` strips step prefixes ("1.", "Step 2:", "3)") while leaving content intact. The `^` anchor is critical — without it, "100g flour" would lose "100". Raw string literals (`#"..."#`) avoid double-escaping backslashes | Parse "Step 3: Preheat oven" → "Preheat oven"; parse "100g flour" → "100g flour" (unchanged) | Raw |
| Feb 17 | M15.4 | Design/IngredientDisplay | Splitting ingredient display into `displayText` (quantity+unit in secondary color with `.monospacedDigit()`) and `name` (ingredient name in primary color) creates a natural two-tone visual hierarchy where quantities align vertically without needing a table layout. The bullet color doubles as a confidence indicator (green vs amber) — no extra UI element needed | View ingredients at different scales — quantity column stays aligned due to monospaced digits; low-confidence ingredients show amber bullets | Raw |

| Feb 17 | M15.5 | CoreData/Migration | Core Data v6 lightweight migration adding an optional String attribute (`quickOption`) requires no mapping model — just create a new `.xcdatamodel` with the field, update `.xccurrentversion`, and add the `@NSManaged` property. However, Xcode silently reverts `.xccurrentversion` from its in-memory state during build — re-write the file after the build if edited externally | Add new model version, build, re-read `.xccurrentversion` to verify it wasn't reverted | Raw |
| Feb 17 | M15.5 | SwiftUI/ScrollViewReader | `ScrollViewReader` with `.scrollTo(id, anchor: .center)` in `.onAppear` auto-centers the horizontal day strip on today's date. The key insight: use the date itself (formatted as ISO string) as the scroll target ID, not an index. This avoids off-by-one errors when the plan starts on a non-today date | Open plan detail where today is mid-week — strip auto-scrolls to center today | Raw |
| Feb 17 | M15.5 | SwiftUI/Menu | `Menu` is the correct control for quick-select pills (Takeout, Dining Out, etc.) — not individual buttons. A single "Choose Recipe" menu button with recipe list AND quick options creates one tap target instead of 5+ separate buttons for unplanned days. This reduces visual noise on the most common card state (unplanned day) | Tap "Choose Recipe" on empty day — menu shows recipes + quick options in one sheet | Raw |
| Feb 17 | M15.5 | Design/ReviewTriage | One-at-a-time review flows (like the IngredientReviewSheet) should show *why* each item needs attention — a reason badge ("Contains parenthetical text", "Starts with a unit/measure") gives the user context to make a faster decision. Without the reason, users must visually parse the ingredient name to figure out what's wrong | Review sheet shows reason badge per ingredient — user understands issue before reading name | Raw |
| Feb 17 | M15.5 | Design/FilterPillSemantic | Using individual `FilterPill` buttons per category (with `ForagerTheme.categoryColor`) is superior to a dropdown `Menu` for category filtering because: (1) all options are visible without a tap, (2) the color dots preview what the filter will show, (3) the pill highlights which filter is active. The tradeoff is horizontal space — `ScrollView(.horizontal)` handles overflow cleanly | Compare dropdown menu filter vs horizontal pill row — pill row communicates more state with fewer taps | Raw |

| Feb 17 | M15.5b | Xcode/SourceKit | SourceKit diagnostics report false "Cannot find type in scope" errors for types from other files (ForagerTheme, HouseholdService, ShareParticipant) when editing in isolation. These are false positives — SourceKit doesn't have the full build graph. Always trust `xcodebuild` output over SourceKit squiggles when working with large projects | Run `xcodebuild build` — BUILD SUCCEEDED confirms the types resolve correctly at compile time | Raw |
| Feb 17 | M15.5b | CoreData/Versioning | `.xccurrentversion` can silently regress when Xcode overwrites it from in-memory state — even during routine builds. Before committing, always check `git diff` for this file. A regression from v6 to v5 would break lightweight migration for users who already have v6 data | `git diff forager/forager.xcdatamodeld/.xccurrentversion` before every commit touching Core Data | Raw |
| Feb 17 | M15.5b | SwiftUI/ViewExtraction | When extracting a view from a parent (SettingsView → HouseholdView), async CKShare data that was loaded in the parent's `.task` must be re-loaded in the child's own `.task`. Don't assume the parent passes down pre-loaded state — the child view may be navigated to independently. This is why HouseholdView has its own `loadHouseholdData()` async task | Navigate to HouseholdView — member list loads asynchronously via its own `.task` | → LN 35 |

| Feb 17 | M15.6 | iOS26/GlassAPI | `Glass` type in SwiftUI has 3 members: `.regular` (standard frosted glass), `.clear` (subtle translucency), `.identity` (no effect). There is NO `.prominent` variant despite WWDC documentation suggesting it. The "prominent" concept only exists for button styles (`.buttonStyle(.glassProminent)`). For emphasized cards, use `.regular` with a larger corner radius for visual distinction | `type 'Glass' has no member 'prominent'` — compiler error confirms; check SwiftUICore.swiftinterface for actual members | → LN 37 |
| Feb 17 | M15.6 | iOS26/GlassEffect | `.glassEffect()` replaces manual shadows — glass elements create depth through light refraction, so adding both `.shadow()` and `.glassEffect()` creates visual confusion (double depth cue). Always remove `.shadow()` calls when applying glass. The glass also handles dark mode elevation automatically (no manual rim light overlay needed) | Compare card with glass-only vs glass+shadow — shadow creates an uncanny "floating over floating" effect | → LN 37 |
| Feb 17 | M15.6 | iOS26/GlassButtons | `.buttonStyle(.glass)` completely replaces custom label content styling — it overrides backgrounds, foreground colors, and borders. This makes it unsuitable for buttons with semantic color states (green active, red danger, grey neutral). Glass buttons are best for standalone, single-purpose CTAs where the glass material itself is the visual language. For inline action buttons with distinct semantic states, keep explicit ForagerTheme-based styling | Apply `.buttonStyle(.glass)` to Done/Remove buttons — semantic colors disappear, all buttons look identical | → LN 37 |
| Feb 17 | M15.6 | iOS26/TabBarMinimize | `.tabBarMinimizeBehavior(.onScrollDown)` has 4 options: `.automatic`, `.onScrollDown`, `.onScrollUp`, `.never`. The `.onScrollDown` option collapses the Liquid Glass tab bar when the user scrolls content down, maximizing screen real estate for content consumption. The bar reappears on scroll-up. Works automatically with List/ScrollView content | Scroll a List down — tab bar minimizes; scroll up — tab bar reappears | → LN 37 |

| Feb 18 | M15 | Swift/Text | iOS 26 deprecated `Text("a") + Text("b")` concatenation. The new pattern uses `LocalizedStringKey` interpolation: `Text("\(Text("a"))\(Text("b").bold())")`. Each interpolated `Text` retains its individual styling. This affects any search-match highlighting or multi-style inline text | Build with iOS 26 SDK — `+` operator on `Text` produces deprecation warning; interpolation compiles cleanly | Raw |
| Feb 18 | M15 | Swift/StringIndex | `String.Index` from `lowercased()` is NOT safe to use on the original string — Unicode characters can change byte length under case mapping (e.g., Turkish dotted-I). Use `text.range(of: highlight, options: .caseInsensitive)` directly on the original string for safe, single-pass case-insensitive range finding | Search for Turkish characters ("İstanbul") — lowercased index approach crashes; .caseInsensitive works | Raw |
| Feb 18 | M15 | CoreData/AsyncDismiss | When using `performWrite` (async background context), calling `dismiss()` synchronously after the write call makes `onError` unreachable — the view is gone before the error callback fires. Move `dismiss()` into `onSuccess` callback so errors can still be shown to the user | Delete recipe → force Core Data error → verify alert appears before navigation pop | → LN 36 |
| Feb 18 | M15 | Swift/DateFormatter | `DateFormatter` is expensive to allocate. In row views called per-item (MealPlanRowView, WeeklyListRowView), creating a formatter on every render creates dozens of allocations per scroll frame. Static lets on a `DateFormatter` extension are initialized once and thread-safe (Swift guarantees lazy initialization of static lets is atomic) | Profile with Instruments Time Profiler — DateFormatter allocation drops from N-per-frame to 0-per-frame | Raw |
| Feb 18 | M15 | CoreData/RollbackState | When reverting a failed `viewContext.save()`, capture ALL original values before mutation — not just the primary field. `item.isCompleted.toggle()` + `item.dateCompleted = nil` on revert loses the original `dateCompleted` date. Pattern: `let orig = item.dateCompleted` before mutation, restore in catch | Toggle completed item off → force save error → verify dateCompleted is restored to original value | → LN 36 |

| Feb 20 | M15 | CoreData/FetchRequest | `@FetchRequest` observes direct attribute changes on its entity but NOT relationship-derived changes. `GroceryListItem.categoryName` reads through `ingredientTemplate?.category?.name` — when you recategorize a template, the item's category changes but the item entity itself is untouched. SwiftUI sees no change. Fix: `viewContext.refreshAllObjects()` forces Core Data to re-fault all objects, making relationships re-evaluate on next access | Recategorize item → navigate back to list overview → verify category chips update (requires refreshAllObjects) | → LN 35 |
| Feb 20 | M15 | Parser/TemplateSanitization | Template name corruption has three independent causes: (1) `normalize()` lacks leading-punctuation stripping ("/ black pepper"), (2) `extractCleanIngredientName()` regex uses `\d+` which splits fractions (1/4 → strips "1", leaves "/"), (3) missing count units (cloves, cans) in regex patterns. All three must be fixed independently — normalize() is the chokepoint for all template creation, but extractCleanIngredientName() runs in view-level matching code that bypasses the service | Add "1/4 tsp black pepper" → template should be "black pepper"; add "3 cloves garlic" → template should be "garlic" | Raw |
| Feb 20 | M15 | Swift/Dictionary | `Dictionary(uniqueKeysWithValues:)` is a fatal assertion — it crashes if ANY keys are duplicated. In multi-device CloudKit sync, `Category` entities can temporarily duplicate before `CategoryDeduplicator` converges. Using `Dictionary(_:uniquingKeysWith: { first, _ in first })` handles collisions gracefully by keeping the first value. Found in `AddIngredientsToListView.sortedCategoryNames` and `RecipeScalingView.sortedCategoryNames` | Crash report: `specialized _NativeDictionary.init<A>(uniqueKeysWithValues:)` in Thread 0 | Raw |
| Feb 20 | M15 | SwiftUI/ViewIdentity | `.id(stateVar)` + mutating `stateVar` in `.onAppear` creates an infinite loop: SwiftUI treats the new ID as a new view → destroys and recreates → `.onAppear` fires → new ID → repeat. This is a crash, not just a performance issue. If you need to force a view refresh, `refreshAllObjects()` or `@State` toggle without `.id()` is safe. Only use `.id()` with values that change from *outside* the view (parent state, model changes) | Add `.id(UUID())` in .onAppear → app freezes/crashes immediately on that view | → LN 35 |
| Feb 20 | M15 | SwiftUI/EditRecipe | `EditRecipeView.completeSave()` must mirror `CreateRecipeView` exactly for structured fields. When you delete and recreate Ingredient entities (the edit pattern), any fields not explicitly set are zero/nil — unlike updates where existing values persist. This makes edit-save a stricter code path than create-save | Edit recipe → verify displayText, numericValue, standardUnit all populated after save | → LN 36 |
| Feb 20 | M15 | CoreData/TemplateDuplication | Template deduplication requires TWO passes: (1) normalize names, (2) group by canonicalName and merge collisions. The original `migrateExistingTemplates()` only did pass 1 — renaming templates but never checking if two now share the same canonical. Also, `canonicalName` (simple lowercase) diverged from `normalize()` (4-phase pipeline), so templates created at different times could have matching names but different canonicals. Fix: always sync `canonicalName` from the normalized name | After migration, query `SELECT canonicalName, COUNT(*) FROM IngredientTemplate GROUP BY canonicalName HAVING COUNT(*) > 1` — should return 0 rows | → LN 36 |
| Feb 20 | M15 | CoreData/GroceryItemName | `GroceryListItem.name` is a display field, not an identifier. The original code stored template names ("garlic") which worked with a separate displayText column. After removing that column, `name` must contain the full ingredient text ("2 cloves garlic") for the quantity to be visible. Template name is used for matching/merging via a separate `cleanName` variable | Add ingredients from recipe → grocery items should show full text with quantities, not just ingredient names | → LN 36 |

| Feb 20 | M15 | UX/PatternReuse | The Ingredients tab's three-layer quality-check pattern (warning banner → filter pill → guided sheet) is a reusable framework. Duplicate detection was built on the same pattern as "needs review" with zero new interaction concepts — users already understand the flow. The only new verb is "merge" instead of "review". This pattern extends to any future quality check (orphaned templates, missing categories, etc.) | Add duplicates → verify banner appears → tap "Review Now" → guided merge sheet | Raw |
| Feb 20 | M15 | SwiftUI/CodeDuplication | Copy-pasted helper functions across views (like `categoryEmoji()` duplicated in 4 structs) create maintenance debt — when removing or changing behavior, every copy must be found via project-wide grep. Better pattern: single static method on theme struct or computed property on the entity. ForagerTheme.categoryColor(for:) already follows this pattern correctly. | `grep -r "categoryEmoji" forager/` should return 0 results | → ADR 012 |
| Feb 20 | M15 | UX/TwoStepCreation | Two-step creation flows (create entity with defaults → show dialog for user input) must backfill Step 1's entity with Step 2's data. Quick-add created GroceryListItem with `defaultCategory` (Produce), then showed template dialog where user picked real category — but never wrote it back. Fix: hold `lastAddedItem` reference, update in `saveToTemplates()`. | Quick-add "Fruit Snacks" → assign to "Snacks" in dialog → should appear in Snacks section, not Produce | → ADR 012 |
| Feb 20 | M15 | Parsing/ConfidenceSemantics | `parseConfidence` has three semantic levels: 0.0 = "no quantity attempted" (name-only, no warning), >0 && <0.7 = "parser tried but uncertain" (warning badge), >=0.8 = "user-validated" (recipe-sourced, autocomplete-selected). Floor confidence at 0.8 for any user-validated path to prevent false warnings. | Add "cucumber" via autocomplete → no warning. Add "2 clv garlic" → may warn. | → ADR 012 |

| Feb 20 | M8.4 | ML/BiLSTMvTransformer | BiLSTM-CRF is the right architecture for on-device ingredient parsing: 2-5 MB model size (vs 50-100 MB transformer), <5ms inference (vs 20-50ms), and 260k training sentences is excellent for BiLSTM but marginal for transformers. The tradeoff: BiLSTM has no attention mechanism, so very long inputs (>30 tokens) with distant dependencies may suffer — but ingredient lines rarely exceed 15 tokens | Compare model sizes and inference times after training both architectures on the same dataset | Raw |
| Feb 20 | M8.4 | ML/BIOTagging | BIO (Begin-Inside-Outside) sequence labeling is superior to flat classification ("2.0\|cup\|flour") for ingredient parsing because: (1) handles variable-length multi-word names naturally, (2) captures token-level boundaries between qty/unit/name/comment, (3) model learns from position context rather than memorizing formats. The original M8.4 plan's Text Classifier approach would fail on "1/4 tsp freshly ground black pepper" where "freshly ground black pepper" spans 4 tokens | Train both approaches on same dataset — BIO handles "freshly ground black pepper" as contiguous NAME span; classifier can't separate it | Raw |
| Feb 20 | M8.4 | ML/DatasetBootstrapping | Open-source datasets (NYT 180k + strangetom 81k = 260k labeled sentences) eliminate the cold-start problem for ML parsing. The original M8.4 plan required 100+ user corrections before training — meaning the model couldn't help until users had already suffered through hundreds of bad parses. With dataset bootstrapping, the model is production-ready from day one | Compare model accuracy at 0 user corrections (bootstrap only) vs requiring 100+ corrections | Raw |
| Feb 20 | M8.4 | Architecture/ParserProtocol | The M8.3 `IngredientParser` protocol was designed for exactly this ML integration. `ParserResult` already has `parserUsed: String` for telemetry, `HybridIngredientParser` already routes by confidence threshold, and `IngredientParsingService` delegates without knowing which parser runs. Adding `MLIngredientParser` requires zero protocol changes and zero changes to the public API — it slots into the router as a third option between regex and NLP | Build with MLIngredientParser — IngredientParsingService callers compile with no changes; telemetry shows "ml" in parserUsed field | Raw |

| Feb 20 | M7.5 | Architecture/ServiceDI | Services that accept `IngredientParsingService` as an init parameter (rather than creating it internally) become forward-compatible with M9.5-partial DI and M8.4 ML parser integration — when the parser swaps, the service layer doesn't change. This is a thin DI pattern: one init parameter, one stored property, zero new abstractions | Create RecipeService with injected parser → swap parser at init → verify service delegates to injected instance | Raw |
| Feb 20 | M7.5 | PRD/StaleAudit | PRD audits should verify entity names against actual Core Data model files, not trust the PRD's own code samples. The original M7.5 PRD referenced `RecipeIngredient` (doesn't exist), `displayName` on IngredientTemplate (correct is `name`), `cloudKitUserID` on HouseholdMember (doesn't exist), and `quantity`/`unit` on Ingredient (correct is `numericValue`/`standardUnit`). All 5 were wrong | `grep -r "RecipeIngredient\|\.displayName\|cloudKitUserID\|\.quantity\|\.unit" docs/prds/m7.5*.md` — should return 0 matches in corrected PRD | Raw |
| Feb 20 | M7.5 | Architecture/DescopeReasoning | Enum routing for views with 1-4 booleans adds pure ceremony (enum definition, Identifiable conformance, id computation) with no practical benefit. The threshold for enum routing is 6+ booleans, where the combinatorial explosion of boolean states makes reasoning about which sheets can co-present difficult. Below that threshold, booleans are fine — SwiftUI's `.sheet(isPresented:)` is simpler and more readable | Count booleans per view — views with 6+ get enum routing, views with 1-5 keep booleans | Raw |

| Feb 20 | M7.5 | Architecture/EstimateAccuracy | M7.5 estimated 14-19h but completed in ~5h. M15's visual refresh had already simplified navigation patterns (enum routing partially done) and eliminated many direct saves (views were rewritten cleaner), making the architecture hardening significantly cheaper. Lesson: doing a visual refresh first made the structural cleanup trivial — validates the decision to reorder M15 before M7.5 | Compare PRD estimates with actual hours in roadmap.md; check git diff stats for reduced scope vs original PRD expectations | Raw |

| Feb 21 | M9.0 | CloudKit/DeprecationNoReplacement | Apple deprecated `discoverUserIdentity` and `userIdentity(forUserRecordID:)` in iOS 17 with **no replacement API** — a rare "just remove it" deprecation motivated by privacy. The `nameComponents` property already returned nil for the current user on iOS 16+, making these APIs both deprecated AND unreliable. When the fallback path is the common path, the deprecated call is dead weight — remove it entirely rather than migrating to a "modern" version that doesn't exist | Build with deprecated calls removed → household creation still works; display name comes from user input during creation, not API | Raw |
| Feb 21 | M9.0 | Swift/MainActorAwait | When a class is `@MainActor` and calls a `@MainActor` closure, `await` is unnecessary — both are already on the same actor, so there's no actor hop. This commonly appears after refactoring to class-level actor isolation: existing `await` calls on MainActor closures become redundant warnings. The fix is simply removing `await`, not restructuring the concurrency model | Remove `await` from `onStatus()` calls in `@MainActor` class → warning resolves, behavior unchanged | Raw |
| Feb 21 | M9.0 | PRD/StalenessPattern | PRDs written >2 weeks before implementation are reliably stale in specific ways: (1) warning lists miss new warnings introduced by intervening milestones, (2) fix descriptions assume APIs that have since changed, (3) time estimates include research that's already been done during planning. The M9 PRD's Phase 0 missed 3 GroceryListDetailView warnings, listed MealPlanDetailView warnings that no longer existed, and estimated 2-3h for work that took <1h. Always do a fresh build audit before implementing from a stale PRD | Run `xcodebuild clean build | grep warning:` before starting PRD-guided work; compare with PRD's warning list | Raw |

| Feb 21 | M9.0.1 | SwiftUI/FocusState | `@FocusState` supports custom `Hashable` types, not just `Bool`. Using `@FocusState private var focusedSearchDate: Date?` with `.focused($focusedSearchDate, equals: date)` elegantly handles "only one field active at a time" across multiple instances of the same view in a `ForEach`. The `nil` state means nothing is focused. Much cleaner than tracking a separate `activeDate` state variable and keeping it synchronized | Create 3+ TextFields with Date-based focus — only one can be active; switching clears the other | Raw |
| Feb 21 | M9.0.1 | SwiftUI/InlineAutocomplete | Inline autocomplete in a `LazyVStack` inside `ScrollView` works naturally — search results expand the card's height and SwiftUI recalculates layout as results appear/disappear. The key design decision is limiting results (e.g., `.prefix(5)`) to prevent a single day card from dominating the scroll view. This is preferable to a modal sheet for quick-access patterns because the user stays in context | Type in inline search field → card expands smoothly as results appear; select result → card contracts back | Raw |

| Feb 21 | M9.1.2 | Architecture/StaticParser | `static let` on a class property is thread-safe in Swift (lazy atomic init via `dispatch_once`). When the underlying type holds only `let` properties and `parse()` creates no shared mutable state, a single static instance can safely serve all call sites without synchronization. This enabled `IngredientParsingService.extractCleanIngredientName(from:)` as a static utility that delegates to a shared `HybridIngredientParser` | Verify `HybridIngredientParser` has no `var` properties; call from multiple threads — no data races | Raw |
| Feb 21 | M9.1.2 | Parser/ViewLayerDivergence | View-layer reimplementations of parser logic diverge silently. AddIngredientsToListView had 40 lines of regex (7 pattern groups + qualifier stripping), while MealPlanDetailView had 18 lines (2 pattern groups, no qualifier stripping). The weaker version produced junk templates like "Salt To Taste" instead of "Salt". Centralizing to a single `static` method that delegates to the parser eliminates this class of bug permanently | Compare template names before/after: "salt to taste" → "Salt To Taste" (old) vs "Salt" (new) | Raw |
| Feb 21 | M9.5 | Architecture/DualParserInstances | `IngredientParsingService` has two independent `HybridIngredientParser` instances: instance-level (`private let parser`) used by `parseIngredient()`/`parseToStructured()`, and static-level (`private static let sharedParser`) used by `extractCleanIngredientName()`. DI for the instance parser doesn't affect the static one. The static method is a pure text utility — its shared parser doesn't need injection (testing via integration tests is sufficient), but this dual-instance pattern should be documented to prevent confusion during M8.4 | Grep for `HybridIngredientParser()` — should appear in exactly 2 places in IngredientParsingService | Raw |
| Feb 21 | M9.5 | Architecture/PRDPhaseConflict | M8.4 PRD Phase 5 shows `MLIngredientParser` hardcoded in `HybridIngredientParser.init()` — which contradicts M9.5's goal of injectable parsers. When prerequisites and features are planned by different sessions, their implementation details can conflict. Resolution: M9.5-partial makes existing parsers injectable; M8.4 adds the optional `mlParser` slot and routing changes. Cross-reference both PRDs before implementing either | Compare M8.4 Phase 5 code sample against M9.5 target state — should use init parameter, not hardcoded property | Raw |
| Feb 21 | M9.5 | Architecture/DefaultParamBackcompat | Swift default parameter values (`parser: IngredientParser = HybridIngredientParser()`) make DI refactoring zero-blast-radius: all 11 existing call sites compile unchanged, while tests gain the ability to inject mocks. This is the "thin DI" pattern — one init parameter, one stored property, zero new abstractions. Unlike full DI containers (ServiceFactory, Environment injection), thin DI adds testability without ceremony | Add `parser:` param with default to init → verify all callers compile without changes → write one mock test | Raw |

---

| Feb 21 | M8.4 | Process/ExternalReview | Using a separate AI agent (Codex) as a "principal engineering reviewer" on a PRD catches real engineering gaps (CRF decode parity, double-parsing, missing start/end transitions) that the author misses. The review found 11 issues — 5 high severity — that would have surfaced as bugs mid-implementation. The key was treating review findings as concrete PRD edits with acceptance criteria, not just acknowledgments | Compare PRD v1 vs v2 diff — every finding has a corresponding spec change and acceptance criterion | Raw |
| Feb 21 | M8.4 | ML/CRFDecoding | CRF Viterbi decoding requires THREE parameter sets: the 7×7 transition matrix, the 1×7 start-of-sequence transitions, and the 1×7 end-of-sequence transitions. Omitting start/end transitions produces different label sequences than the training-time CRF decoder, even when emission scores are identical. This is a common gap in "simplified" Viterbi implementations | Run Python CRF decode vs Swift decode on 1000 samples — with all 3 params, ≥99.9% agreement; without start/end, agreement drops significantly | Raw |
| Feb 21 | M8.4 | ML/WordVsChar | Character-level features (char CNN/LSTM) in ingredient parsing add dual-input CoreML conversion complexity and per-token character ID preprocessing for marginal accuracy gain. Ingredient vocabulary is distinctive at the word level — "cups", "tsp", "garlic" are unambiguous words that don't need character-level decomposition. Word-only BiLSTM halves the model I/O contract complexity | Train word-only vs word+char models on same dataset — compare accuracy difference vs conversion/preprocessing complexity | Raw |
| Feb 21 | M8.4 | Architecture/DoubleParse | `parseAndConnectIngredients()` calls `parseIngredient()` and `parseToStructured()` sequentially — both internally invoke `parser.parse()`. With regex-only this is microseconds wasted; with ML inference in the chain, it doubles the expensive operation. The fix is calling `parser.parse()` once and mapping the single `ParserResult` to both output formats. Always audit callsite multiplication before adding expensive inference to an existing pipeline | Grep for `parser.parse` call count inside `parseAndConnectIngredients` — should be exactly 1 after refactor | Raw |
| Feb 21 | M8.4 | Process/CorrectionDataGate | A retraining pipeline without production correction data flowing in is an empty pipeline. `logCorrection()` existed since M8.1 (with tests) but no production edit flow called it. Gate retraining claims on minimum correction corpus (≥50 linked corrections) to prevent shipping an unused capability. Wire the data collection before building the consumption pipeline | Export corrections via debug menu → count should be >0 after user has edited ingredients in production use | Raw |

| Feb 21 | M8.4 | CoreML/Warmup | CoreML performs JIT compilation on first `prediction()` call — 100-500ms spike. This can freeze the main thread if the ML parser is first invoked during user interaction. Fix: background warmup prediction during app initialization via `DispatchQueue.global(qos: .utility).async { _ = parse("warmup") }`. The `static let sharedParser` lazy init triggers the warmup on first access | Time first vs second `prediction()` call — first is 100-500ms, second is <5ms | Raw |
| Feb 21 | M8.4 | CoreML/MLMultiArray | `MLMultiArray` element access returns `NSNumber`. Neural Engine returns Float16, CPU returns Float32 — always use `.floatValue` accessor which handles both. Direct pointer access via `dataPointer` is faster but requires explicit stride calculation: `emissions.strides[dim].intValue` for each dimension. The subscript-based `[[0, t, j] as [NSNumber]]` is safer and sufficient for <64-token inputs | Compare `.floatValue` on Neural Engine vs CPU outputs — both return correct Float values. Profile subscript vs pointer access — negligible difference for short sequences | Raw |
| Feb 21 | M8.4 | CoreML/RangeDim | `ct.RangeDim(1, 64)` declares dynamic input shapes in CoreML — the model accepts variable-length inputs without padding. However, non-default shapes (anything ≠ the "default" size set during export) fall back from Neural Engine to CPU. Do NOT right-pad to a fixed size — pass actual token count and accept CPU execution for non-default lengths. Padding wastes compute and can introduce noise | Export with `RangeDim(1, 64)`, predict with length 5 vs length 64 — check MLComputeDevice in output; non-default lengths report CPU | Raw |
| Feb 21 | M8.4 | Swift/NFKD | `String.decomposedStringWithCanonicalMapping` gives NFD (canonical decomposition), NOT NFKD (compatibility decomposition). For tokenizer normalization that maps "½" → "1/2" and "ﬁ" → "fi", you need NFKD: `text.applyingTransform(.init("NFKD"), reverse: false)`. This is a common confusion — the property name sounds like it should work for all decomposition, but NFD only separates combining characters | Compare "½".decomposedStringWithCanonicalMapping (still "½") vs "½".applyingTransform(.init("NFKD"), reverse: false) (becomes "1⁄2") | Raw |
| Feb 21 | M8.4 | CoreML/SilentFailure | `MLIngredientParser.init?()` returns nil if the CoreML model bundle is missing or corrupted — no crash, no error message. When routed through `HybridIngredientParser` with `mlParser: IngredientParser? = nil`, all ML tests silently pass via fallback path. Fix: add `#if DEBUG` warning log on nil init + model presence guard test (`XCTAssertNotNil(MLIngredientParser())`) that fails loudly if the model is absent from the test bundle | Remove .mlpackage from bundle → run tests → without guard, all pass silently; with guard, one test fails explaining why | Raw |

| Feb 21 | M8.4 | ML/DatasetDeduplication | strangetom's 81,316 rows contain 12,470 exact duplicates (15.3%). Without deduplication before splitting, duplicates leak between train/val/test sets, inflating evaluation metrics. Deduplicate by sentence text before splitting — reduced to 68,846 unique samples. This is a standard ML hygiene step but easily overlooked when the source dataset doesn't document its duplicate rate | Count `SELECT sentence, COUNT(*) FROM en GROUP BY sentence HAVING COUNT(*) > 1` — should match dedup count. Verify zero sentence overlap between train/val/test after splitting | Raw |
| Feb 21 | M8.4 | ML/FractionDecoding | strangetom encodes fractions as `#num$den` (simple) and `whole#num$den` (mixed) — e.g., `#1$2` = 0.5, `1#1$2` = 1.5. Using `re.sub` with three ordered passes (mixed → prefixed → simple) handles these patterns inline, including when embedded in ranges like `1$2-#3$4` or suffixed tokens. The ordering matters: mixed fractions must match before simple fractions to prevent `1#1$2` from matching `1$2` first | Run decode_fraction on `"1#1$2"` → `"1.5"`, `"#3$4"` → `"0.75"`, `"1$2-3$4"` → `"0.5-0.75"` | Raw |
| Feb 21 | M8.4 | Architecture/SingleParseRefactor | `parseAndConnectIngredients()` previously called both `parseIngredient()` and `parseToStructured()`, each internally invoking `parser.parse()` — doubling the expensive operation. The `parseUnified()` refactor calls `parseCore()` once and maps the single `ParserResult` to both `ParsedIngredient` and `StructuredQuantity`. This is mandatory prep before adding ML inference — without it, every recipe import would run the model twice per ingredient | Grep for `parser.parse` calls inside IngredientParsingService — should be exactly 1 in `parseCore()` after refactor | Raw |
| Feb 21 | M8.4 | ML/StratifiedSplitting | When a dataset has multiple sources (NYT, AllRecipes, BBC, Cookstr, TC) with different distributions and writing styles, random splitting can over-represent one source in the test set. `train_test_split(stratify=sources)` ensures each split has proportional representation from every source. For 68,846 samples across 5 sources, this prevents evaluation bias from source-specific patterns (e.g., NYT uses more complex modifiers than AllRecipes) | Compare `Counter(s["source"] for s in test)` proportions against full dataset proportions — should be within 1% for each source | Raw |
| Feb 21 | M8.4 | ML/LabelMapping | Mapping 13 fine-grained labels to 7 coarser labels (e.g., B_NAME_TOK + I_NAME_TOK + NAME_VAR + NAME_SEP → NAME) is a one-way information loss. The mapping must be done at dataset preparation time, not training time, so the training data format matches the production inference format. Keeping the mapping as an explicit dict (`LABEL_MAP`) rather than regex-based rules makes it auditable and prevents silent misclassification when source labels change | Verify every strangetom label appears in LABEL_MAP keys — unknown labels should raise an error, not silently default | Raw |

| Feb 22 | M8.4 | CoreML/PackVsNoPack | `pack_padded_sequence` and direct LSTM forward produce identical results for batch_size=1 with no padding — max diff was 0.0 (exact match). The packing only helps when batches have variable-length sequences and you want to avoid computing on padding tokens. For CoreML single-input inference, dropping packing is a free simplification with zero accuracy loss | Compare `_get_emissions()` (packed) vs `EmissionScorer.forward()` (unpacked) on same input — diff should be 0.0 | Raw |
| Feb 22 | M8.4 | CoreML/ConversionPrecision | coremltools 9.0 achieves max emission difference of 4.77e-06 between PyTorch float32 and CoreML float32 across sequence lengths 3-20. This is well within the 0.01 threshold and causes zero label prediction differences in Viterbi decoding. FLOAT16 precision (`ct.precision.FLOAT16`) would halve model size but could increase this difference to a point where some boundary decisions flip | Convert with FLOAT32 → verify Viterbi parity is 100%; could test FLOAT16 in future for size optimization | Raw |
| Feb 22 | M8.4 | CoreML/ViterbiDeterminism | When emission scores are sufficiently accurate (4.77e-06 max diff), the Viterbi algorithm is fully deterministic — 100.0000% parity across 1,000 test samples (8,030 tokens). This is because Viterbi uses argmax at each step, and small emission differences don't change the argmax winner. The risk zone would be scores very close together where a tiny perturbation flips the max | Run 1000 samples through Python CRF decode vs Python Viterbi with PyTorch emissions — 100% match | Raw |
| Feb 22 | M8.4 | CoreML/RangeDimConversion | `ct.Shape(shape=(1, ct.RangeDim(1, 64)))` declares a variable-length input dimension in CoreML. The minimum (1) and maximum (64) bounds are enforced at runtime — inputs outside this range will fail. This is preferable to fixed-size with padding because it avoids wasted compute and prevents padding tokens from affecting LSTM hidden states | Export with RangeDim, predict with lengths 3-20 — all succeed; predict with length 65 — should fail | Raw |
| Feb 22 | M8.4 | CoreML/JSONKeyNaming | The `transitions.json` exported in Phase 2 uses `label_names` as the key (matching strangetom convention), not `labels` (as the PRD pseudocode showed). The Swift loader must match the actual JSON key, not the PRD — always verify JSON structure before writing deserialization code | `cat transitions.json \| python -c "import json,sys; print(json.load(sys.stdin).keys())"` — verify actual key names | Raw |
| Feb 22 | M8.4 | Python/ImportFromSibling | When `convert_to_coreml.py` needs to reuse model architecture from `train_model.py`, direct `from train_model import IngredientTagger` works because both files are in the same directory and Python adds CWD to sys.path. This avoids duplicating the model class definition. However, importing also executes module-level code — ensure the training script guards its main logic behind `if __name__ == "__main__"` | `cd Tools/ml-training && python -c "from train_model import IngredientTagger"` — should import without running training | Raw |

| Feb 22 | M8.4 | Swift/NFKD | `String.decomposedStringWithCanonicalMapping` gives NFD, not NFKD. For tokenizer normalization matching Python's `unicodedata.normalize('NFKD', text)`, must use `text.applyingTransform(StringTransform("NFKD"), reverse: false)`. NFKD decomposes ½ (U+00BD) to "1⁄2" (with U+2044 fraction slash), while NFD leaves it unchanged. This distinction is critical for tokenizer parity | Compare `"½".decomposedStringWithCanonicalMapping` (unchanged) vs `"½".applyingTransform(.init("NFKD"), ...)` (becomes "1⁄2") | Raw |
| Feb 22 | M8.4 | CoreML/MLMultiArray | `MLMultiArray` subscript `[[0, t, j] as [NSNumber]].floatValue` is the safe extraction pattern — handles both Float16 (Neural Engine) and Float32 (CPU) transparently. Direct pointer access via `dataPointer` is faster but requires manual stride calculation and type checking. For ingredient parsing (<64 tokens), subscript overhead is negligible | Profile both approaches on 20-token input — subscript adds <0.1ms total | Raw |
| Feb 22 | M8.4 | ML/ConfidenceGeometricMean | Geometric mean of max per-token softmax probability is a good confidence metric because it's sensitive to ANY uncertain token — even one low-confidence token drags down the entire score. Arithmetic mean would mask a single uncertain token in a long sequence. The formula `exp(mean(log(max_probs)))` is numerically stable via log-space computation | Parse "2 cups flour" (all confident) vs "xyzzy glorp" (uncertain) — first should have confidence >0.9, second <0.5 | Raw |
| Feb 22 | M8.4 | Swift/UnicodeSlash | After NFKD, Unicode fractions decompose to digit + U+2044 FRACTION SLASH + digit (not U+002F SOLIDUS). The quantity parser must handle both slash types when parsing fractions. Python's `split("/")` only splits on U+002F, but Swift's `split(separator:)` works with any Character — so explicitly checking both `"/"` and `"\u{2044}"` covers all cases | Parse "½" after NFKD → "1⁄2" → split on U+2044 → 1/2 = 0.5 | Raw |

| Feb 22 | M8.4 | Architecture/WinnerAttribution | "Hybrid" as a `parserUsed` value is a routing label, not a parser label — it says "regex won after NLP was consulted" but doesn't tell you which parser to improve. Winner-only attribution (`"regex"`, `"ml"`, `"nlp"`) makes telemetry directly actionable: group corrections by `parserUsed` and each group measures one parser's accuracy. The `parserName = "hybrid"` stays on the router for protocol conformance but never flows to telemetry | Query corrections grouped by `parserUsed` — each group maps to exactly one parser's accuracy | Raw |
| Feb 22 | M8.4 | Architecture/NLPGateThreshold | NLP's confidence cap (0.75) means it can't beat a regex or ML result above 0.75. Gating NLP behind both regex < 0.5 AND ML < 0.5 is conservative but correct: in the moderate band (0.5-0.9), ML outperforms NLP on structured ingredient text. NLP's value is for completely unstructured inputs where both primary parsers produce garbage. The NLP cap from ADR 010 interacts with the gate threshold — they form a coherent system | Run "salt to taste" through all 3 parsers — NLP wins only when regex and ML both < 0.5 | Raw |
| Feb 22 | M8.4 | CoreML/WarmupStrategy | CoreML JIT compiles on first `prediction()` — 100-500ms. The static `sharedParser` in `IngredientParsingService` lazily loads `MLIngredientParser` → `IngredientTaggerEmissions`. If first call happens during SwiftUI body evaluation, the main thread stalls. A `DispatchQueue.global(qos: .utility).async` warmup call in `foragerApp.init()` triggers this loading off the main thread — zero UI impact, ~200ms on background queue | Profile first `extractCleanIngredientName` call with/without warmup — without: 100-500ms main thread; with: <5ms because model already loaded | Raw |

| Feb 22 | M8.4 | Tokenizer/PunctuationContext | Naive punctuation splitting (split ALL `/` and `.`) breaks fraction tokens (`1/4` → `["1", "/", "4"]`) and decimal tokens (`14.5` → `["14", ".", "5"]`). The fix is context-aware splitting: only split when NOT between digits. This is a training-inference mismatch — the Python tokenizer preserved these tokens, but the Swift tokenizer split them, causing the model to receive wrong vocabulary IDs. Cross-validation test vectors are essential for catching this class of bug | Compare tokenize("1/4 tsp salt") output vs test vector — should be `["1/4", "tsp", "salt"]` not `["1", "/", "4", "tsp", "salt"]` | Raw |
| Feb 22 | M8.4 | Unicode/NFKDCombining | NFKD normalization decomposes `ñ` to `n` + combining tilde (U+0303), but the combining mark is still a character. Python's `encode('ascii', 'ignore')` strips non-ASCII bytes after NFKD, effectively removing combining marks. Swift needs explicit `CharacterSet.nonBaseCharacters` filtering after NFKD to match Python's behavior. Without this, `jalapeño` stays as `jalapeño` in Swift but becomes `jalapeno` in Python — a silent tokenizer mismatch | `parser.tokenize("1 jalapeño") → ["1", "jalapeno"]` after fix | Raw |
| Feb 22 | M8.4 | Testing/MLvsAlgorithm | Testing deterministic algorithms (Viterbi) vs ML model outputs requires fundamentally different assertion strategies. Viterbi tests can assert exact equality because given the same inputs the output is deterministic. ML model tests should assert *invariants* (confidence > 0, name contains keyword, parserUsed = "ml") rather than exact field values, because model accuracy is probabilistic (95.4% sentence accuracy means ~5% of sentences differ from expected). The hybrid router ensures uncertain ML results are caught by regex or NLP | ViterbiDecoder tests: exact equality. MLIngredientParser tests: invariant-based assertions with `contains()` and range checks | Raw |

| Feb 22 | M8.4 | Telemetry/CorrectionSources | `CorrectionSource` is semantically distinct from `ParsingSource` — `ParsingSource` tracks where *parsing* happened (recipe, grocery list), while `CorrectionSource` tracks where the *correction* happened (editing recipe, renaming template). A correction in `editRecipe` may have originated from a `recipeIngredient` parse. Keeping them as separate enums prevents conflation | Verify CorrectionSource has 4 cases matching edit UI locations, not parsing entry points | Raw |
| Feb 22 | M8.4 | SwiftUI/parseUnified | Replacing `parseToStructured()` with `parseUnified()` in save flows gives both `ParsedIngredient` (for correction detection via `.name`) and `StructuredQuantity` (for entity field population) from a single parse call. This avoids the double-parse anti-pattern that Phase 0c eliminated from `parseAndConnectIngredients()` | Compare telemetry event counts before/after: same number of events per save operation | Raw |
| Feb 22 | M8.4 | Codable/OptionalBackCompat | Optional `Codable` fields give free JSON schema evolution — `JSONDecoder` silently defaults missing optionals to `nil` with no migration code. This enabled schema v2→v3 upgrade with zero migration: existing correction events just decode with `parserUsed: nil` and `source: nil`. Confirmed by TEST-TEL-027 which decodes actual v2 JSON | Encode v3 event, decode v2 JSON → both succeed, nil for missing fields | → insight at line 70 (cluster) |

| Feb 22 | M8.4 | ML/TokenizerExtraction | Extracting a stateless tokenizer from a class into a free function enables sharing without instantiation overhead. `MLIngredientParser.tokenize()` used instance method syntax but only referenced `Self.punctuation` (a static set). Making it a free function `foragerTokenize()` eliminates the need to instantiate `MLIngredientParser` (which requires CoreML model) just to tokenize text for export. The key design constraint: the tokenizer is the *contract boundary* between training and inference, so there must be exactly one implementation | `foragerTokenize("2 cups flour") == MLIngredientParser().tokenize("2 cups flour")` — delegates, so always identical | Raw |
| Feb 22 | M8.4 | ML/SyntheticReconstruction | Correction export uses synthetic reconstruction (build tokens from corrected fields) rather than raw input alignment. This is more reliable because: (1) corrections don't carry rawInput, (2) corrected fields are already clean/normalized, (3) reconstructed form matches the clean tokenized distribution the model was trained on. Trade-off: we lose MODIFIER/PREP/COMMENT labels since corrections only capture QTY/UNIT/NAME — but these 3 labels cover the core parsing task that corrections address | Export a correction with corrected name "bread flour", qty 2.0, unit "cup" → tokens: ["2", "cup", "bread", "flour"], labels: ["QTY", "UNIT", "NAME", "NAME"] | Raw |
| Feb 22 | M8.4 | ML/FineTuneOversampling | Fine-tuning on a tiny correction corpus (50-200 samples) against a 55k base dataset requires oversampling to be effective. Without oversampling, corrections are <0.4% of training data — the model barely sees them during an epoch. Auto-oversampling up to 50x puts corrections at ~4.5% of the merged set, giving them meaningful gradient influence without overwhelming base knowledge. Combined with lower LR (0.0005 vs 0.001), this prevents catastrophic forgetting while allowing the model to learn from user corrections | Train with/without oversampling: without, per-correction metrics unchanged; with 50x, correction-similar examples improve | Raw |

| Feb 22 | M8.4 | Testing/IntegrationVsUnit | Integration tests for ML parsing should assert *pipeline invariants* (name extracted, template created, sort order preserved) rather than exact parser output. The ML model isn't available in the test bundle (CoreML .mlpackage only in app bundle), so `HybridIngredientParser` falls back to regex → NLP in tests. This is fine — integration tests validate the pipeline works end-to-end regardless of which parser tier won. Parser-specific tests belong in unit test files (MLIngredientParserTests, ViterbiDecoderTests) | Run integration tests — all pass with regex tier, proving pipeline correctness is parser-independent | Raw |

| Feb 23 | Bugfix | Parsing/ConcatenatedQtyUnit | Regex parser requires `\s+` between quantity and unit, so "16oz baby carrots" fails to parse. Rather than adding new patterns for every concatenated format, a single pre-processing regex `^(\d+(?:\.\d+)?)\s*([a-zA-Z])` → `$1 $2` normalizes the input so all existing patterns work. This is more maintainable than N separate patterns | `parser.parse("16oz baby carrots")` → qty=16, unit=oz, name="baby carrots" | Raw |
| Feb 23 | Bugfix | Normalization/BabyQualifier | "baby" in food context is NOT a strippable size descriptor like "large" or "small" — it denotes a distinct grocery product (baby carrots, baby spinach, baby corn, baby bella). The normalization pipeline was treating it the same as "diced" or "fresh", permanently losing the "baby" prefix. Fix: remove from qualifier lists + add compound forms to preferPlural map | `normalize(name: "Baby Carrots")` → "baby carrots" (not "carrots") | Raw |
| Feb 23 | Bugfix | CloudKit/SchemaDeployment | `initializeCloudKitSchema(options: [])` force-pushes the complete Core Data schema to CloudKit Development by creating dummy records for all entities. Without it, CloudKit only registers fields when records with those fields are actually synced. If a field is added (like `quickOption` on `PlannedMeal` in v6) but no records are synced after, the Development schema stays stale and there's nothing to deploy to Production | Run in Debug on device with iCloud → check CloudKit Dashboard → new fields appear → deploy to Production | Raw |

| Feb 24 | M10 Spike | Web/JSONLDRendering | ~90% of major recipe sites embed JSON-LD for Google Rich Results, but ~30% render it via client-side JavaScript (WordPress WPRM/Tasty plugins inject `<script type="application/ld+json">` after page load). `URLSession` only gets server-rendered HTML, yielding 43% extraction rate vs the expected 90%. WKWebView fallback is a Phase 1 requirement, not a nice-to-have — estimated to raise extraction rate to 75-80% | Run spike CLI with URLSession-only on 28 sites → 12/28 (43%); compare with browser DevTools confirming JSON-LD present after JS load on Sally's, Half Baked, etc. | Raw |
| Feb 24 | M10 Spike | Web/ExtractionStrategies | Recipe JSON-LD appears in 4 distinct HTML patterns in the wild: (1) standard `<script type="application/ld+json">` tags, (2) `@graph` wrapper arrays with mixed types, (3) inline JSON-LD in regular `<script>` blocks without the ld+json type (Marmiton), (4) `__NEXT_DATA__` Next.js SSR payloads (BBC Good Food). A single regex-based extractor misses 30%+ of extractable recipes — the strategy pattern with multiple extraction methods is architecturally necessary, not just nice-to-have | Test each strategy in isolation against the 28-site matrix — standard ld+json: 10/28, +inline scripts: 12/28 (BBC Good Food __NEXT_DATA__ was false positive, eliminated by tightened rules) | Raw |
| Feb 24 | M10 Spike | Web/SchemaOrgEdgeCases | Real-world JSON-LD deviates from spec in predictable ways: `@type` as array (`["Recipe", "CreativeWork"]`) in 11% of sites, full URL types (`"https://schema.org/Recipe"`) instead of bare `"Recipe"`, `@graph` nesting in 18%, HTML entities in JSON string values in 25%. Each edge case is individually rare but collectively they affect >50% of sites. The `findRecipe()` function must handle all four deviations | Run extractor against spike matrix — sites like Allrecipes (array @type), Budget Bytes (@graph), Cookie and Kate (HTML entities) all extract correctly | Raw |
| Feb 24 | M10 Spike | Vision/OCRClassification | VNRecognizeTextRequest `.accurate` mode achieves ~100% character accuracy on clean printed recipe text (22/22 lines at full confidence). The challenge is not OCR accuracy but line *classification* — heuristic scoring alone achieves ~80% accuracy because ingredient lines containing time words ("until golden, about 10 minutes") score as both ingredient and instruction. Section-aware context (tracking which header preceded each line) raises classification to ~90%+ | Run `--image` on test recipe PNG → 22 lines recognized, 100% confidence; check classified types match expected sections | Raw |
| Feb 24 | M10 Spike | Vision/SectionContext | Line-by-line heuristic classification fails on ambiguous lines (short instructions like "Mix in the flour" score as ingredients due to length). Tracking section headers ("Ingredients:", "Instructions:") and applying their context to subsequent low-confidence lines dramatically improves accuracy — lines after "Instructions:" default to instruction type when their standalone classification score is < 0.5. This mirrors how humans read recipes: section headers set expectations for what follows | Toggle section-aware classification on/off in spike CLI → without context, steps 5-6 misclassified; with context, all 8 steps correct | Raw |
| Feb 24 | M10 Spike | Process/SpikeValue | Running a data extraction spike against 28 real sites before writing the PRD changed 3 major architectural decisions: (1) WKWebView is Phase 1 not Phase 2, (2) multiple extraction strategies are required not optional, (3) acceptance criteria use measured rates not guesses (43% → ≥65% vs the research doc's assumed 90%). The spike took ~4h but prevented ~10h+ of wrong-direction implementation work. Spikes should always precede PRDs for features with external dependencies | Compare PRD targets with spike data — every percentage and latency target traces to a measured value, not an assumption | Raw |
| Feb 24 | M10 Spike | Swift/SPMConcurrency | Swift 6.2 SPM packages with `@main` attribute conflict with top-level code execution — can't have both. The simpler pattern for CLI tools is top-level code (`let args = CommandLine.arguments`, `switch` on args) without the `@main struct` wrapper. Also, `URLSession` + `DispatchSemaphore` for synchronous HTTP in CLI tools avoids async/await complexity in a non-async context | Build `ImportSpike` CLI with both patterns — `@main` fails with top-level code; top-level code alone compiles cleanly | Raw |
| Feb 24 | M10 Spike | Web/HTMLEntityDecoding | 25% of sites (7/28) embed HTML entities in JSON-LD string values (`&amp;`, `&frac14;`, `&#8217;`). Standard `JSONSerialization` parses these as literal strings, not decoded characters. Recipe titles like "Homemade Pizza &amp; Pizza Dough" need entity decoding post-extraction. The spike's entity decoder handles named entities + numeric entities (`&#\d+;` pattern) | Extract Simply Recipes pizza → title contains `&amp;` → after decoding: "Homemade Pizza & Pizza Dough" | Raw |
| Feb 24 | M10 Spike | Architecture/DraftFirst | The spike validated that `RecipeFormData` (existing create/edit staging model) is the right target for import preview — NOT a new `ImportDraftRecipe` entity. All imported data maps naturally to `RecipeFormData` fields: title→name, recipeIngredient→ingredients[].fullText, recipeInstructions→instructions. The only gap is field-level confidence (not currently on RecipeFormData), which can be added as optional properties without disrupting existing create/edit flows | Map extracted recipe fields to RecipeFormData struct — all fields have natural targets; confidence can be parallel `[String: Double]` dict | Raw |

| Feb 24 | M10.1.8 | SwiftUI/GenericViewBuilder | A generic `@ViewBuilder` layout template (`errorLayout<Actions: View>`) lets multiple error presentations share identical visual structure (icon-in-circle → title → body → actions) while varying the action buttons per error type. This is the SwiftUI equivalent of "template method" — shared chrome, variant-specific content — without inheritance or type erasure | All 4 error variants render with same spacing/sizing; change circle size in one place → all variants update | Raw |
| Feb 24 | M10.1.8 | Architecture/FailFastURLCheck | Checking unsupported sources (Pinterest, TikTok, Instagram) before the network fetch saves 1-2s of wasted round-trip time AND produces a more helpful error message. Without this, Pinterest URLs would succeed the fetch, fail extraction, and show generic "No recipe found" — confusing because the user sees a valid recipe on Pinterest. Domain-specific messaging ("Pinterest pins don't contain recipe data. Try the original recipe link.") tells the user exactly what to do | Enter pinterest.com/pin/123 → instant fail with Pinterest-specific message (no network spinner) | Raw |
| Feb 24 | M10.1.8 | Swift/EnumComputedUIMetadata | Putting UI metadata (`errorTitle`, `errorIcon`) as computed properties on the error enum centralizes the error→presentation mapping. The alternative — switch statements in the view — scatters the same mapping across multiple methods. With enum properties, adding a new error case triggers a compiler error in the switch, forcing you to define its title and icon. The view just reads `error.errorTitle` and `error.errorIcon` | Add new `ImportError` case → compiler warns on all incomplete switches including `errorTitle` and `errorIcon` | Raw |

| Feb 24 | M10.1.7 | iOS/ShareExtension | Share extensions run in a separate process — they cannot access `UIApplication.shared`. Cross-process communication to the main app requires a registered URL scheme (`forager://import`) + `extensionContext.open(url:)`. The fallback path (URL stays in App Group UserDefaults until next app activation) handles the rare case where `open()` fails | Share from Safari → Forager opens and starts import; kill main app → share → relaunch → import triggers on `scenePhase` change | Raw |
| Feb 24 | M10.1.7 | iOS/ShareExtensionActivation | `TRUEPREDICATE` as `NSExtensionActivationRule` shows the extension for ALL share sheet content (images, text, files). Apple rejects this for production. `NSExtensionActivationSupportsWebURLWithMaxCount: 1` restricts activation to URLs only — the extension only appears in Safari's share sheet (and other apps sharing URLs), not when sharing photos or text | Share a URL in Safari → Forager appears; share a photo → Forager absent from share sheet | Raw |
| Feb 24 | M10.1.7 | iOS/AppGroupDefaults | `UserDefaults(suiteName:)` creates a shared container accessible by both the main app and extensions with the same App Group. The key insight: write-then-clear must happen atomically from the reader's side (main app reads + removes immediately before processing) to prevent duplicate consumption. If two URLs are shared rapidly, the second overwrites the first — acceptable for v1 single-URL queue | Write to shared defaults from extension → read from main app → key is nil after read | Raw |

| Feb 25 | M10.2 | iOS/FoundationModels | `SystemLanguageModel.default.isAvailable` is the correct availability check (not `LanguageModelSession.isSupported` as assumed in the PRD). The `.availability` property returns a detailed enum: `.available`, `.unavailable(.deviceNotEligible)`, `.unavailable(.appleIntelligenceNotEnabled)`, `.unavailable(.modelNotReady)` — each maps to a different user-facing message | Check `SystemLanguageModel.default.availability` on Pro vs non-Pro device | Raw |
| Feb 25 | M10.2 | iOS/FoundationModels | `LanguageModelSession.GenerationError` has 6 cases: `assetsUnavailable` (model not downloaded — trigger heuristic fallback silently), `guardrailViolation` (content filtered — actual failure), `decodingFailure` (@Generable schema mismatch), `exceededContextWindowSize`, `unsupportedGuide`, `rateLimited`. For recipe extraction, `assetsUnavailable` should fall through; others are terminal | Test on device with model not downloaded → `assetsUnavailable` thrown; test with valid recipe text → structured output | Raw |
| Feb 25 | M10.2 | Heuristic/Classification | Numbered instruction steps (e.g., "1. Mix ingredients") score highly as both ingredient (0.6 via `startsWithNumber`) and instruction (0.5 via `isNumberedStep`). Without a penalty, the ingredient score wins. Fix: deduct 0.4 from ingredient score when `isNumberedStep` matches. This is a common pitfall in multi-category scoring — signals can double-count across categories | Test case: "1. Mix dry ingredients" → classified as `.instruction`, not `.ingredient` | Raw |

| Feb 25 | M10.2.5 | SwiftUI/BindingFromEnum | Creating a `Binding<T>` that reads from and writes to an enum associated value (e.g., `TextPastePhase.reviewing([EditableClassifiedLine])`) requires a computed get/set closure — `get` pattern-matches the current enum case, `set` re-wraps in the enum. This keeps the review state local to `TextPasteImportView` without promoting it to a global state machine or passing it through an `ObservableObject` | Change a line type in SectionHighlightView → `TextPastePhase.reviewing(lines)` updates correctly via binding | Raw |

| Feb 25 | M10.5 | Corpus/MetricNoSpace | Metric no-space quantities (`400g`, `750ml`, `2L`, `200ml`) account for ~120 parsing failures across international + unusual-metadata categories. The regex parser expects `\d+\s+unit` — no whitespace between number and unit causes the whole line to be treated as a name with no quantity. Fix: add lookahead pattern `(\d+)(g|kg|ml|L|cl|dl)\b` before the standard quantity-unit split | Parse "400g mushrooms" → qty=400 unit=g name=mushrooms (currently returns qty=nil name="400g mushrooms") | Raw |
| Feb 25 | M10.5 | Corpus/UnitlessIngredient | Unit-less ingredients with quantities (`1 egg`, `2 bay leaves`, `3 cloves garlic`) are classified as instruction by OCRLineClassifier when no recognized unit keyword is present. The classifier's ingredient score relies heavily on unit detection — without a unit, short numeric-prefixed lines default to instruction. Fix: add count-noun patterns (egg, leaves, cloves, heads, sprigs, stalks, pinch, handful) to classifier vocabulary | "2 bay leaves" classified as `.ingredient` not `.instruction` | Raw |
| Feb 25 | M10.5 | Corpus/BareIngredientName | Bare ingredient names without quantities (`salt and pepper`, `celery`, `sugar`, `passata`) score ~0.10 confidence and fall to `.unknown`. The classifier requires either a number or a unit to give ingredient score. Fix: add a known-ingredient-name lookup (even a small list of 200 common ingredients) as a fallback signal when no quantity pattern matches | "salt and pepper" classified as `.ingredient` at ≥0.5 confidence | Raw |
| Feb 25 | M10.5 | Corpus/UnusualMetadata | Unusual metadata formats (`Difficulty: Easy`, `Oven: 180°C`, `Active time: 15 mins`, `Wine Match:`, `Serves: 6-8`) are classified as `.unknown` instead of `.metadata`. The classifier only recognizes `Prep Time:`, `Cook Time:`, `Servings:`, `Yield:`. Fix: expand metadata keyword list with cooking-specific patterns (difficulty, oven, temperature, wine, active/passive time, source, cuisine) | "Difficulty: Easy" classified as `.metadata` not `.unknown` | Raw |
| Feb 25 | M10.5 | Corpus/UnitAbbreviations | `tbs`, `tablespoons`, `teaspoons` (full words and British abbreviations) are not in the regex parser's unit recognition list, causing ~15 parsing failures. The parser recognizes `tbsp`/`tsp` but not variants. Fix: expand unit alias map to include `tbs`→`tbsp`, `tablespoon(s)`→`tbsp`, `teaspoon(s)`→`tsp`, `dessertspoon`→`dsp` | Parse "1 tbs tomato puree" → qty=1 unit=tbsp name="tomato puree" | Raw |
| Feb 25 | M10.5 | Corpus/MixedFractions | Mixed fractions written as `2-1/2`, `1-1/2` (hyphen-separated) are not parsed — the parser handles `2 1/2` (space) and `2½` (unicode) but not the hyphenated form. Fix: add pattern `(\d+)-(\d+/\d+)` that converts to decimal before standard parsing | Parse "2-1/2 cups flour" → qty=2.5 unit=cups name=flour | Raw |
| Feb 25 | M10.5 | Corpus/LLMCorpusReview | LLM (Claude Opus) review of 50-recipe corpus found 295 errors in 12 minutes across 5 parallel agents — a task that would take a human reviewer 3-4 hours. The LLM identified systematic patterns (metric no-space, unit-less ingredients, bare names) that individual recipe review might miss. Key trade-off: LLM review has ~5% false positive rate on ambiguous lines, but the systematic pattern identification is far more valuable than individual corrections | Compare LLM correction count (295) with human spot-check — LLM catches patterns humans would recognize individually but not aggregate | Raw |
| Feb 25 | M10.5 | Architecture/LLMUniversalBackend | The corpus review revealed that Foundation Models can perform classification AND parsing in a single pass — extracting structured `{qty, unit, name, category_suggestion}` from raw text with higher accuracy than the 3-tier regex→ML→NLP pipeline. Architecture implication: Foundation Models should be the primary ingredient processor for ALL input paths (manual entry, URL import, text paste, photo OCR), with the existing pipeline as offline fallback | Compare FM extraction accuracy on messy-01 ingredients vs pipeline output — FM handles "600g beef shin", "750ml red wine" correctly where pipeline fails | Raw |

| Feb 26 | M10.5.4 | LLM/OAuth | Anthropic banned third-party OAuth for API access (Jan 9 2026 enforcement, Feb 19 2026 policy update). OpenAI OAuth is for ChatGPT actions only (their app calling your backend), not for your app calling their API. Only Google Gemini supports proper OAuth via Google Sign-In. Conclusion: API keys are the universal auth approach for external LLM integration. UX mitigation via deep links to provider key pages, clipboard auto-detect, test connection button | Check Anthropic docs for OAuth policy; check OpenAI docs for Apps SDK | Raw |
| Feb 26 | M10.5.4 | Parser/DescriptorGaps | Bare descriptors without the "a" prefix (`"Dash of olive oil"` vs `"a dash of olive oil"`) need a separate regex pattern. The "a" prefix pattern catches `"a pinch of salt"` but `"Bunch of spring onions"` (capitalized, no article) falls through to ML parser. Both patterns must include the full descriptor vocabulary | Parse "Bunch of spring onions" → qty=1 unit=nil name="spring onions" | Raw |
| Feb 26 | M10.5.4 | Parser/InvertedStructure | "Juice of 1/2 lemon" and "Zest of 1 lemon" use a prefix-before-quantity structure that no existing pattern handles. Standard patterns expect qty first, then unit, then name. A dedicated `tryPrefixQuantityPattern()` catches these by matching `^(juice\|zest)\s+of\s+\d+` and swapping the parse order | Parse "Juice of 1/2 lemon" → qty=0.5 name="lemon" notes="juice of" | Raw |
| Feb 26 | M10.5.4 | Parser/ConfidenceRouting | The hybrid parser's 0.90 confidence threshold causes a critical routing issue: regex patterns returning valid quantities at 0.60-0.85 confidence get overridden by ML parser, which returns qty=nil for descriptive amounts (treating "bunch", "dash" as units). This was the single biggest accuracy limiter — fixing confidence levels from 0.60→0.95 for descriptor patterns and 0.75-0.85→0.92-0.95 for others boosted qty extraction from 88.4% to 94.1% | Run corpus test before/after confidence change — check regex usage % and nil-qty count | Raw |
| Feb 26 | M10.5.4 | Testing/ValidationCorpus | A second 50-recipe corpus (unseen during fix development) achieved 92.9% qty extraction vs corpus 1's 94.1% — only 1.2% degradation. This validates that regex pattern fixes generalize well and aren't overfitting to specific corpus 1 phrasings. The messy category (prose-embedded ingredients) shows the biggest gap (47/~170 ingredients detected) — this is the scenario where LLM integration will provide the most value | Compare corpus 1 vs corpus 2 qty extraction rates per category | Raw |

| Feb 26 | M10.3 | SwiftUI/Equatable | Custom `Equatable` on `@State` enums can silently break SwiftUI reactivity. `PhotoImportPhase` returned `true` for all `.reviewing` states, so modifying classified lines via a binding triggered `phase = .reviewing(newLines)` but SwiftUI's diff saw no change and skipped re-rendering. Fix: return `false` for cases with mutable associated values, or remove Equatable entirely | Tap a line to reclassify in SectionHighlightView → verify count updates and view re-renders | Raw |
| Feb 26 | M10.3 | SwiftUI/SheetSequencing | When a save operation sets `state = .saved` (showing a success view) AND then sets `showingCategoryAssignment = true` in the same sync block, the success view takes over before the user notices the category modal. Sheets attached to the outer container still present, but the UX is confusing — the user sees "Recipe Saved!" then a modal on top. Fix: remove the intermediate success view and dismiss directly, showing the category modal as the only post-save interaction | Save imported recipe with new ingredients → CategoryAssignmentModal appears, then sheet dismisses | Raw |
| Feb 26 | M10.3 | SwiftUI/AppLifecycle | `HouseholdService.init()` runs during `foragerApp.init()` before `PersistenceController.prepare()` loads stores. Any Core Data fetch in `init()` finds nothing. The `.task` modifier on the TabView runs AFTER `isReady = true`, making it the correct place for the first real `loadCurrentHousehold()` call. The init-time call was a silent no-op that caused a 4+ second blank grocery list screen on cold launch | Force-close app → reopen → grocery list should show data immediately (no empty state flash) | Raw |

| Feb 27 | M10.3 | SwiftUI/Menu | `Menu` with `Label("Name", systemImage: "circle.fill").foregroundStyle(color)` works inside Menu buttons for colored category icons. The `circle.fill` SF Symbol with per-item `.foregroundStyle()` gives quick visual association between category names and their theme colors without custom views | Visual: import preview category dropdown shows colored dots per category | Raw |
| Feb 27 | M10.3 | UX/InlineEditing | Full-line ingredient editing (single TextField for entire line) is superior to split editing (qty Text + name TextField). Users expect to fix any part of the OCR result — splitting forces them to mentally decompose the ingredient, which is the app's job. After editing, re-parse on submit to extract the updated ingredient name and auto-match category | Edit "2 cups chiken breast" → submit → parses to "chicken breast" and auto-fills category | Raw |
| Feb 27 | M10.3 | UX/ParsedFeedback | Showing the parsed ingredient name below the editable line gives users confidence the system understood their edit. Accent-colored parsed name acts as a feedback loop: edit → submit → see parsed name update → confirms correct parsing. Without this, users have no way to verify the system interpreted their correction correctly | Edit ingredient line → parsed name updates on submit → matches expected ingredient | Raw |
| Feb 27 | M10.3 | SwiftUI/PostSavePatch | Applying category assignments after `saveImport()` via template name matching avoids modifying the service API. The `pendingCategoryAssignments: [String: String]` dictionary flows as view state through the save pipeline, then `applyCategoryAssignmentsAndFinish()` matches against `IngredientTemplate.name`. This is cleaner than threading category hints through the service layer | Import recipe with category selections → templates have correct categories after save | Raw |

| Feb 28 | M10.8 | SwiftUI/UUIDvsIndex | When porting a display/edit toggle pattern from a read-only preview (import) to an editable list (recipe editing), use UUID-based tracking (`@State editingIngredientId: UUID?`) instead of index-based (`@State editingIndex: Int?`). Lists with reorder/delete support shift indices on mutation — a `@FocusState` tied to an invalidated index causes focus to jump to wrong rows or crash. UUIDs are stable across mutations. | Reorder ingredients while one is in edit mode → focus stays on correct ingredient | Raw |
| Feb 28 | M10.8 | SwiftUI/LazyParsing | Parsing ingredients only on edit-exit (commit) rather than on every keystroke is both a performance win and a better UX. Users fix OCR/typo errors holistically before submitting — parsing mid-edit shows confusing intermediate states ("chick" → parsed as "chick" before they finish typing "chicken"). The import preview validates this: `reMatchIngredient()` fires on `.onSubmit` and `.onChange(of: editingIndex)`, never on text binding changes | Type partial ingredient name → parsed display stays stable until submit | Raw |

| Mar 1 | M10.6.5 | Parsing/Normalization | `preferPlural` dictionary caused template names like "avocados", "peppers", "tomatoes" — wrong because quantity handles plurality ("2 avocado" not "2 avocados"). Removed entirely. Only genuinely always-plural items (beans, oats, peas, chips) stay plural via `alwaysPlural`/`alwaysPluralSuffixes`. Also updated Claude API system prompt to instruct singular names, creating a dual-layer defense: LLM returns singular + normalization enforces singular | Parse "2 avocados" → template name should be "avocado"; "black beans" stays "black beans" | Raw |
| Mar 2 | M10.6.8 | Architecture/ServiceExtraction | Extracting 4x-duplicated `IngredientMatchInfo` + `computeIngredientMatches()` + `formattedIngredientText()` into a shared `IngredientMatchService` + `IngredientMatchRow` removed ~400 lines from views and centralized the parse→template→status pipeline. The key insight: input is always `String` (views provide raw text regardless of data model), output is a value type `IngredientMatchResult` that views store however they want. No view state in the service | Build succeeds; all 4 views (Import, Create, Edit, RecipeList) use shared service with identical behavior | Raw |
| Mar 2 | M10.6.8 | Testing/InputDesign | Test inputs must match actual parser behavior, not assumed behavior. `testWithCategoryFromNeedsCategoryState` used "1 large onion" expecting parsed name "onion", but the parser preserves "large" as an identity qualifier → "large onion" ≠ template "onion" → wrong status. Fix: use "2 cups flour" where parser strips unit and returns name "flour" matching the template exactly. Tests that assert downstream behavior must understand the upstream pipeline | Run failing test with debug print on parsedName → reveals actual parser output vs assumed output | Raw |
| Mar 2 | M10.6.8 | Parsing/SingularConsistency | The normalizer should consistently singularize nouns, with the `alwaysPlural` list as the only exception. Identity qualifiers (baby, dried, large, ground, frozen) are preserved while the noun singularizes: "baby carrots" → "baby carrot", "large eggs" → "large egg". Adding more pluralization exceptions creates a maintenance burden and ambiguity. AI adds value in categories, typo correction, and abbreviation expansion — not pluralization decisions | Run 363 tests — all pass with consistent singular expectations | Raw |
| Mar 2 | M10.6.8 | Swift/ProtocolCascade | When a protocol's required method signature changes (e.g., adding `categories: [String]` to `LLMIngredientParser.parseBatch()`), every conformer AND every test mock must be updated simultaneously. In Swift, `let` properties with optionals (like `let category: String?`) don't get automatic `nil` defaults in memberwise inits — callers must pass `category: nil` explicitly. `var` properties do get the default. This distinction caused compilation failures in tests | Build test target — compilation errors point to every non-conforming mock/test; fix all before tests can run | Raw |
| Mar 2 | M10.6.8 | CodeReview/SilentFailures | `try?` in production-facing error paths is a code smell — it swallows errors that callers need for debugging. Found 3 instances: `buildURLRequest` silently dropped JSON serialization failures, `SettingsView.saveAPIKey` hid CloudKit sync errors, `compactMap` silently dropped malformed AI results. Replacing with `do/catch` + specific error messages made failures visible. The pattern: `try?` is fine for "don't care" paths, dangerous for "should care but forgot" paths | Search for `try?` in service/view code — each should have a documented reason or be replaced with do/catch | Raw |

**Last Updated**: March 1, 2026 (session 56 — M10.6.5 singular normalization fix)
