# Insights Log

**Purpose**: Lightweight capture of technical insights discovered during development sessions. Acts as a triage inbox — when 3+ insights cluster around a topic, promote to a Learning Note or ADR.

**Promotion rules**:
- **3+ related insights** → Learning Note (implementation journey)
- **Architectural decision with trade-offs** → ADR
- **Recurring pattern or gotcha** → Add to CLAUDE.md or development-guidelines.md

Historical insights archived — see git history before March 2026.

---

## Insights

| Date | Milestone | Topic | Insight | Verification | Status |
|------|-----------|-------|---------|--------------|--------|
| Jul 3 | reskin-provisions-press | Design/StoredHexTrumpsThemeFallback | `ForagerTheme.categoryColor(for:)` **prefers the Category's persisted hex** and only falls back to name-based theme defaults — so a token-level reskin does NOT restyle categories. The colors a fresh install (= the App Store reviewer) sees come from THREE places that must all be updated together: (1) `Category.defaultCategories` seed hexes in `Models/Category+CoreDataClass.swift`, (2) the theme's name-based fallbacks, (3) picker palettes in AddCategoryView/AddStoreView. Same trap with the **asset-catalog `AccentColor`** — it drives the global tint (tab bar, toggles) and lives outside ForagerTheme entirely; the dashboard screenshot showed a green Home tab on an otherwise-tomato app until the .colorset was updated. **Rule: a "centralized" token system has satellites — persisted seeds, asset catalogs, and picker palettes. Grep for hex literals AND check Assets.xcassets on any reskin.** | Simulator screenshots pre/post `f951f26`; leak sweep found 9 rounded fonts + 6 raw color literal sites + Material seed hexes despite M15.1's "no hardcoded colors" rule. | Raw |
| Jul 3 | reskin-provisions-press | Testing/WorktreeBaselineForPreExisting | When the suite fails during an unrelated change, the fastest attribution test is a **throwaway `git worktree` at `main`** running the exact same `-only-testing:` set — identical failure profile (49 tests / 8 failures, CategoryDeduplicator + StoreSchema isolation issues) proved all failures pre-existing in ~3 min without stashing or branch-switching the working tree. Complements Git/CheckDiffVsMainBeforeCommittingOffStaleBranch: diff-vs-main validates *content*, worktree-run-vs-main validates *behavior*. | `git worktree add <scratch> main` + isolated suite run, both sides 8/49 failures (2026-07-03). | Raw |
| Jul 3 | reskin-provisions-press | AppStore/LiveMeetingNamedTheAxis | The "Meet with Apple" appointment did what three written rounds could not: it surfaced the **actual axis** of the 4.3(a) determination. The rep's feedback was that the **UI, color scheme, and overall design read as over-saturated / already-used** — explicitly *not* code duplication or functionality similarity. This validates the Session-130 category-saturation diagnosis AND converts it from a perception problem into an actionable design brief. **Rule: when written appeals return conclusion-only boilerplate, live engagement is worth it precisely because a human in real time will name the axis the boilerplate hides.** The four-factor engineering defense effectively landed; the design surface was the unaddressed remainder. | Meet with Apple 2026-07-02 (appointment from ticket APL466617). Outcome drove `reskin-provisions-press` change. | Raw |
| Jul 3 | reskin-provisions-press | Design/CategoryDefaultIsTheRejection | forager's M15.1 identity (warm cream `#EDE8DF` + forest-green accents + SF Pro Rounded + soft cards) is simultaneously (a) the grocery/recipe **category default** — 2026 roundups describe the whole space as "clean minimal, green and muddy brown, warm family-focused" — and (b) the single most common **AI-generated design cluster** (warm cream + earthy accent + rounded type). A reviewer triaging screenshots literally cannot distinguish it. **Rule: escaping a saturated design space requires departing on temperature, hue, shape, AND typography simultaneously — tweaking the accent color keeps you in the cluster.** Second rule: the replacement must be grounded in **subject vernacular** (grocery world: crate labels, butcher paper, price tags), not in a counter-trend (neo-brutalism has its own recognizable clichés — acid yellow/pink/lime, thick black borders — and reads as a different template). Subject-specific vernacular is what's defensible as original work. | Direction research 2026-07-02 (category roundups + 2026 design-trend sources, cited in session); 3-direction mockup `docs/mockups/ui-overhaul-directions.html`; chosen direction specs in `openspec/changes/reskin-provisions-press/`. | Raw |
| Jul 3 | reskin-provisions-press | Design/ChromeContentLayerSplit | A full visual-identity replacement does NOT require discarding Liquid Glass: glass is the **chrome layer** (tab bar, nav, sheets, CTAs — floats above content, adaptively picks up tint from beneath), while identity lives in the **content layer** (rows, tags, headers, type). Splitting on that boundary preserves all FUI-1 glass work, follows the HIG ("glass floats above content, no glass-on-glass"), and produces a signature pairing — matte print under glossy glass — that a flat-web reskin or a glass-everything approach both miss. Bonus: because the identity lives in content, it fully survives Reduce Transparency (glass falls back to tinted opaque; the print layer is untouched). | Layer split specified in `openspec/changes/reskin-provisions-press/design.md` Decision 1; mockup `docs/mockups/provisions-press-liquid-glass.html` incl. Reduce Transparency panel. | Raw |
| Jun 23 | escalate-43a-to-app-review-board | AppStore/WrittenAppealCeiling | The App Review Board (the top written-appeal tier) **upheld** forager's 4.3(a) rejection while naming **no specific app** and **not engaging** the point-by-point refutation of Apple's four stated spam factors. That combination is diagnostic: when escalating tiers (reviewer → Board) on **one unchanged submission** returns the same unsupported determination, the operative driver is an **unstated category-saturation perception** ("another grocery+recipe+meal app"), not the written duplication criteria — and you've hit the **ceiling of written argument**. A correct argument can't win because the argument was never the thing being weighed. **Rule: once the Board upholds with no named app, stop writing. The only remaining levers are (a) live human engagement ("Meet with Apple" appointment) to force the "name the app" question, and (b) repositioning the product's first-impression surface (name/icon/subtitle/first screenshot) so a fast-triage reviewer files it under a less-saturated genus, then withdraw-and-refile-fresh on a new submission ID.** Builds on AppStore/BoilerplateReReviewIsNonEngagement: tier-escalation without changing the submission cannot break a non-engagement loop. | Board response 2026-06-23, ticket APL466617 (reviewer "Leo"), verbatim in `docs/app-store-rejection-43a-response.md` § 11.7; outcome plan § 11.5/§ 11.8. | Raw |
| Jun 6 | escalate-43a-to-app-review-board | AppStore/BoilerplateReReviewIsNonEngagement | When an App Store guideline rejection (here 4.3(a) "Design - Spam") returns **verbatim-identical boilerplate** on re-review — same template, opening "the issues we previously identified still need your attention," naming no specific app and referencing none of your changes — that is the signature of a **non-engagement loop**: the response wasn't individually evaluated (likely a category-saturation pattern-match or automated pass). You cannot copy-edit out of it: forager fully executed a metadata-only repositioning (new subtitle, description, keywords, 5 screenshots, competitor-named reply) and drew identical boilerplate on two re-reviews. **Rule: identical boilerplate on re-review means the lever is no longer the artifact being rejected — it's getting a different human in the loop (App Review Board appeal, or the "Meet with Apple" appointment). Stop iterating the metadata.** Compounding factor: all 3 determinations were on one submission ID, so a fresh reviewer was never reached. | 3× 4.3(a) on submission `e5e960e5…` (2026-04-21 build 134, late-Apr re-review, 2026-05-13 build 140), all iPad Air 11" M3. May 13 text recorded verbatim in `docs/app-store-rejection-43a-response.md` § 11.1. | Raw |
| Jun 6 | escalate-43a-to-app-review-board | AppStore/RefuteStatedCriteriaNotUniqueness | A 4.3(a) spam appeal lands harder when it refutes Apple's **own stated spam factors** point by point (shared source code / repackaged template / purchased template / multiple accounts) than when it argues "my app is unique." The four factors are objective and demonstrably inapplicable to an original single-developer native app, which reframes the appeal from a subjective judgment the Board can dismiss with boilerplate to an objective checklist it must engage with. Pair it with named-competitor differentiation (axis of comparison per competitor), not adjective prose. In a formal **Board appeal** (unlike a Resolution Center reply) it's appropriate to ask them to name the specific app they believe you duplicate — it shifts the burden and reads as confidence. | Appeal letter drafted in `docs/app-store-rejection-43a-response.md` § 11.3, grounded in the 12-app competitor matrix from `reposition-app-store-listing/design.md`. Outcome pending Board response. | Raw |
| Jun 6 | escalate-43a-to-app-review-board | Git/CheckDiffVsMainBeforeCommittingOffStaleBranch | When scaffolding a change off a branch other than current `main`, check `git diff --stat main` BEFORE committing — a never-merged feature branch can be weeks stale, and basing new work on it silently drags its regressions (older `current-story.md`, removed journal/insights lines, reverted service code) toward main in the eventual PR. Here the appeal work was docs-only but the host branch (`reposition-app-store-listing`) was ~5 weeks behind main. Fix: commit on the stale base, then cherry-pick just the docs commit onto a fresh `main`-based branch. The response doc was new to main, so the cherry-pick threw an expected modify/delete conflict — resolved by `git add`-ing the full file left in tree. **Rule: a clean `git diff main --stat` is the acceptance test that a branch carries only intended changes.** | `feature/escalate-43a-board-appeal` diff vs main = exactly the appeal doc + OpenSpec change (commit `00f3765`); the stale-branch reversions were left behind. | Raw |
| Apr 30 | fix-meal-plan-household-observer | SwiftUI/EagerReloadVsAsyncDependency | A one-shot eager reload synchronously called after wiring an async dependency is **not** a fix — it's a faster race condition. `HouseholdService.init()` kicks off `Task { await loadCurrentHousehold() }`, then `foragerApp.init()` synchronously calls `MealPlanService.shared.loadActiveMealPlan()`. At the moment of the synchronous call, `currentHousehold` is still nil → predicate matches `householdKey == nil` → user's plan filtered out → cached for the rest of cold start. Replaced with a Combine subscription on `service.$currentHousehold`: `@Published` delivers the current value on subscribe AND fires again when the async load resolves, so one subscription replaces "eager call + observer." **Rule: when an init-time call depends on async state, subscribe to the signal that async-completion will publish — don't synchronously read it.** Applies to `@Published` properties, `NotificationCenter`, or anything Task-backed. | DashboardView showed empty Tonight's Meal / Meal Plan / Tomorrow's Meal cards on cold start until user navigated to Meals tab (whose `onAppear` triggered `updateActivePlanStatus → loadActiveMealPlan`). Grocery card worked because it uses `@FetchRequest` (reactive) — the asymmetry was the diagnostic shortcut. Fix verified by code review + clean build; simulator verification pending. | Raw |
| Apr 30 | fix-meal-plan-household-observer | SwiftUI/MainActorSinkHop | `MealPlanService` is `@MainActor`. Combine `.sink` closures aren't actor-isolated — they're `@Sendable` escaping. `.receive(on: DispatchQueue.main)` puts the closure on the main thread but Swift's strict-concurrency checker still requires an explicit hop into the actor before calling main-actor methods. Pattern: `.sink { [weak self] _ in Task { @MainActor [weak self] in self?.method() } }`. The redundant `[weak self]` on both the sink and the Task is intentional — the sink captures may outlive the actor hop. | Pattern matches `Services/HouseholdService.swift:251-258` (`scheduleMemberDepartureCheck` Timer callback uses the same `Task { @MainActor [weak self] in await … }` shape for the same reason). | Raw |
| Apr 30 | fix-meal-plan-household-observer | Diagnostic/AsymmetryAsShortcut | When one feature works and a sibling-shaped feature doesn't, the difference between them IS the bug. Dashboard's grocery list card populated on cold start; meal-plan cards didn't. The visible difference: grocery uses `@FetchRequest` (reactive Core Data binding), meal-plan reads from a singleton's `@Published` property. That single asymmetry pinpoints "data source isn't reactive to the late-arriving signal," which is exactly the bug. **Rule: in symmetric UI surfaces, check the data-source symmetry first — it's a faster path than tracing through method calls.** | Diagnosis from view file took two reads (`DashboardView.swift` to identify data sources, then `MealPlanService.swift` to confirm singleton's reload mechanism). No agent or runtime instrumentation needed; root cause was knowable from code structure. | Raw |
| Apr 21 | fix-groceryitem-multi-zone-assignment | CloudKit/EventObserverNSErrorsAreSanitized | Apple's `NSPersistentCloudKitContainer.eventChangedNotification` delivers `NSError` instances with mostly-empty `userInfo` for mirroring-delegate failures. Build 138's diagnostic found `NSLocalizedFailureReasonErrorKey` populated on setup-time errors but absent on export-path errors. Build 139's beefed diagnostic dumped everything — `userInfoKeys=[]`, `underlying=nil`, only `desc="A Core Data error occurred."`. The rich detail visible in UI alerts (persistent IDs, zone names) comes from a different Apple code path (alert presentation) that observers cannot reach. **Rule: don't chase detail from the CloudKit event observer — you've hit Apple's sanitization ceiling. Use observer for DETECTION ("X conflict exists, X conflict resolved") not IDENTIFICATION ("which object").** For identification, rely on the UI alert path or escalate to device console logs via Xcode. | Build 139 diagnostic log showed completely empty userInfo on real zone-conflict event; verified Apple's API path rather than our observer code was the ceiling. | Raw |
| Apr 21 | fix-groceryitem-multi-zone-assignment | CloudKit/AbsenceOfErrorIsValidationSignal | "No new error entries after the remediation step" is sufficient validation for data-corruption fixes even when we can't identify the specific corrupted record. If the bug was persistent every session and stops firing the moment the bad payload is removed, the fix is validated by absence. Applies to stuck-delegate class bugs where the error is unambiguous when it fires. Don't block on "identify the record" — validate on "does it stop firing." | Build 139 post-nuke session showed zero 🚨 entries across the full reinstall + new-household-create flow; pre-nuke every session had at least one 🚨 entry. | Raw |
| Apr 21 | fix-groceryitem-multi-zone-assignment | CoreData/LocalDeletesSurviveStuckDelegate | `viewContext.delete(obj)` + `context.save()` succeed locally even when the `NSPersistentCloudKitContainer` mirroring delegate is stuck in a failed state. Local Core Data operations don't route through the CloudKit delegate — they hit SQLite directly. CloudKit propagation happens async via the delegate and can silently fail, leaving orphan CKRecords in the user's private zone (post-delete zombie data). Important for designing delete/cleanup flows that MUST work during broken-sync recovery. Corollary: when designing user-facing "delete household" / "leave household" flows, don't assume the CloudKit-side cleanup completes just because the UI says "Deleted" — design the local flow to be correct independently of CloudKit round-trip. | Post-delete session on build 138 logged `Marked household as deleted in Keychain` locally; build 139 reinstall pulled "Your Household" back from CloudKit's private zone (CKShare was gone but records were orphaned). Discovery polled for 60s before timing out cleanly. | Raw |
| Apr 21 | fix-groceryitem-multi-zone-assignment | CloudKit/ImplicitStoreInferenceUnreliable | Core Data's relationship-based store inference (pick the child's store from its parent's via `objectID.persistentStore`) runs lazily at save time and does NOT prevent the NSPersistentCloudKitContainer mirroring delegate from routing a CKRecord into a different zone than the parent's. Explicit `context.assign(child, to: parent.objectID.persistentStore)` BEFORE the first save is required under CloudKit dual-store mirroring. ADR 014's "Child HouseholdScoped Entities" M9.15 pattern + M9.19 CRITICAL rule assumed implicit inference was enough — 18 production sites followed the pattern faithfully and all had the same latent defect, surfacing as CoreData 134040 "Object graph corruption detected — objects assigned to multiple zones." **Rule: when an ADR says "parent.store must equal child.store," the ADR must also specify the mechanism (`context.assign`) that guarantees equality.** | Device bug evidence: `docs/bugs/investigation-assets/2026-04-21-groceryitem-multi-zone-error.png`. Regression tests verify the new pattern in foragerTests/Services/WeeklyListServiceTests.swift + RecipeServiceTests.swift (4 tests, all pass). | Raw |
| Apr 21 | fix-groceryitem-multi-zone-assignment | Tooling/HookMatchesSpecDisjunctionNotSubset | Enforcement hooks should match the spec's actual rule (often a disjunction like "X OR Y"), not a strict-interpretation subset. The original architecture-guard.sh enforced "always use ManagedObjectFactory.make()" (DataScope.swift:98-103 strict reading) and blocked all direct `Entity(context:)` inits. ADR 014 actually permits "factory OR child-inheritance-with-assign." The hook's over-strict rule created false positives the moment the correct fix pattern tried to land — only discovered when the hook blocked the PR. Fixed: hook now uses awk to check for `context.assign(...)` within 10 lines of `Entity(context:)`; passes if factory-route OR if assign-route is present. **Rule: write enforcement against the spec's disjunction, not a proper subset.** | `.claude/hooks/architecture-guard.sh` post-fix — tested on the 18 fix sites (all pass), one intentional bypass attempt (correctly blocked). | Raw |
| Apr 21 | reposition-app-store-listing | AppStore/43aMetadataNotBinary | 4.3(a) "Design - Spam" rejections in saturated categories (grocery/recipe/meal-planner, astrology, VPN, habit-tracking, dating per 9to5Mac Nov 2025) are won in the Resolution Center with noun-phrase functional differentiation, not with code changes. Apple rep (Forum 772135) defined "metadata" = screenshots + title + subtitle + keywords + description; Forum 771794 documents a dev who redesigned UI, added features, renamed the app, and still got rejected because the listing positioning stayed generic. **Rule: pure binary changes with unchanged listings fail; metadata rewrites (especially screenshots — the dominant signal per Apple rep) succeed in 1-3 rounds.** Name competitors by name in the reply with concrete axis-of-comparison; don't ask Apple which app they think you resemble (they won't disclose). | Precedent research (2 parallel agents, 12 competitor analysis, Forum 772135 / 771794 / 769912), 4.3(a) plan drafted. Apple re-review verdict pending on build 138. | Raw |
| Apr 19 | session-wrap | Git/PRStackingReparentBeforeMerge | `gh pr edit <N> --base main` preserves a stacked PR when its original base branch is about to be deleted. Do this BEFORE merging the base PR. Once the base branch is deleted, GitHub auto-closes the stacked PR and reopening requires recreation. Adds a mitigation to Session 117's PR-stacking insight: the escape hatch isn't "rebase + open new PR" if you catch it early — it's "re-parent to main before the base merges." Generalizable: when you know you'll merge a stack, re-parent each downstream PR to main FIRST, then merge base PRs with --delete-branch. | Session 124: #149 was stacked on #147. Ran `gh pr edit 149 --base main` before `gh pr merge 147 --squash --delete-branch`. #149 survived, rebased cleanly onto new main, merged without re-creating the PR. | Raw |
| Apr 19 | session-wrap | Process/ConflictMarkerRegexForAppendOnlyDocs | When both sides of a merge conflict added new content to the TOP of a reverse-chronological log (journal, insights-log, current-story), the resolution is trivially "keep both" — not "pick one side." A 5-line Python script `re.sub(r'<<<<<<< HEAD\n', '', content); re.sub(r'=======\n', '', content); re.sub(r'>>>>>>> [a-z0-9]+ \(.*?\)\n', '', content)` strips conflict markers while preserving both blocks in-place. Won't work for substantive code merges — shines for append-only docs that every change touches. | Session 124 rebased #146/#148/#149, each producing 1-3 doc conflicts. Python helper resolved all 9 conflicts in ~30 seconds total; manual inspection after confirmed both sides preserved. | Raw |
| Apr 19 | session-wrap | Process/SessionWrapIsOwnHygiene | When context is running low ("let's call it quits"), the wrap is its own hygiene — not an afterthought. Capture into the repo before the session ends: (1) saved agent plans from `~/.claude/plans/` → `docs/prds/active/` (plans/ is session-local, repo survives), (2) current-story.md reflects reality (all COMPLETE, no stale in-flight entries), (3) next-prompt.md has a "Recommended Next" section so the next session can pick up directly, (4) journal entry for the wrap itself so the decision trail is preserved. "What's next?" needs a concrete written answer, not handwaving. | Session 124 wrap: copied test-first plan to `docs/prds/active/establish-test-planning-workflow.md`; next-prompt.md gained "Recommended Next" list; current-story.md shows 4 PRs COMPLETE + build 137; journal entry documents the wrap. All survives session end. | Raw |
| Apr 19 | investigate-import-and-store-test-failures | CoreData/ChildContextVsViewContextIdentity | When passing a managed object across context boundaries (e.g., child→parent save), you're passing one specific NSManagedObject instance. The same logical entity in another context is a DIFFERENT NSManagedObject with the same ObjectID. Both `===` and `Set<NSManagedObject>.contains()` use identity, not ObjectID. First attempt at fixing `persistAndFinish`'s refresh-loop-discards-edits bug used `preserveUpdated.contains(obj)` — silently no-op'd because the caller passed child-context recipe but the loop iterated viewContext's updated objects. Fix: compare by `objectID`. Generalizable: if a Set/Dict key is NSManagedObject, it's probably wrong — use `NSManagedObjectID` instead. | Pre-fix: `testReplaceExistingRecipeUpdatesFields` still failed same as before the `preserveUpdated` parameter added. Post-fix (objectID comparison): test passes. | Raw |
| Apr 19 | investigate-import-and-store-test-failures | CoreData/FreshlySavedObjectsAreFaulted | `context.existingObject(with: result.recipeObjectID)` returns the entity but accessing attributes immediately after save may return nil until the fault fires. Two robust alternatives: (1) fetch by unique-property predicate (forces fault); (2) `context.refresh(obj, mergeChanges: true)` explicitly. Predicate-fetch is simpler AND tolerates state leakage — if your draft uses a UUID-suffixed title, the fetch finds the right object even among leaked recipes from other tests. | Pre-fix: `recipe?.title` returned nil on freshly-saved recipes. Post-fix (predicate fetch by unique title): test passes. | Raw |
| Apr 19 | investigate-import-and-store-test-failures | Process/OverCountingFailuresFromInferredStatus | PR #147 description claimed 6 failures; agent's investigation showed reality was 3 failures + 3 tests that actually pass. Over-count came from inferring "not green" as "failing" while crash-loops masked per-test status. When a test file is crash-looping on setUp, no test inside it can be classified accurately. Generalizable: when a suite has any crash-loops, the failure count is unknown until the crashes are fixed. "Everything in the file failed" is the default xctest output, not necessarily the truth. | PR #147 description: 4 crashing files contributing N failures each + 2 stale assertions. Post-PR-#147 xcresulttool run: 3 real failures + 3 pass + rest were crash-loop artifacts. | Raw |
| Apr 19 | investigate-import-and-store-test-failures | AI/AgentExplorationShapesHighLeverageWhenOutputIsActionable | Second instance of "fire investigation agent in background, return with a plan I can execute verbatim." Agent produced 2300 words with triage table, root causes, code refs, and 3-option fix breakdown per failure. Picking the right option took seconds because the agent had already compared them. Generalizable: investigation agents pay off strongest when the problem is bounded (specific failures to triage) and the expected output shape is specified (plan file with options + recommendations). | Session 121 agent report → PR #147 in ~1h. Session 123 agent report → PR #149 in ~2h. Both executable verbatim. | Raw |
| Apr 19 | fix-test-harness-and-stale-assertions | Testing/CrashLoopsHideRealFailures | A test file that crashes on setUp never reaches its assertions — xctest relaunches and tries the next test method, often repeatedly. The forager suite had 4 files with setUp crashes producing 51 relaunches per full-suite run, ~15 minutes of lost CI time AND 51 hidden opportunities to see real behavior drift. Fixing the crash-loops dropped runtime for those files from ~15 minutes to 2.8 seconds AND exposed 6 legitimate assertion failures that had been invisible. Generalizable rule: when `TEST FAILED` shows up alongside "Restarting after unexpected exit" markers, the restarts are almost always the bigger signal than the assertion failures — fix them first. | Before: 1495s elapsed, 480+2 executed, 51 "Restarting after unexpected exit" events. After: 6 affected files run in 2.8s, no crash-loops, 6 real assertion failures now visible and investigable. | Raw |
| Apr 19 | fix-test-harness-and-stale-assertions | Testing/FactoryEnforcementPropagatesToTestSetup | When a service declares `private(set) var factory: ManagedObjectFactory!` (implicit-unwrap), calling any method that uses `factory.make(...)` before `configure(factory:)` crashes. This is an intentional ADR 014 contract — misconfiguration at app startup should fail loudly. The COST is that test setUps must match production setup precisely: instantiate a `ManagedObjectFactory(context:, scopeProvider: nil, persistence: testPersistence)` and call `service.configure(factory: factory)` before any creation-path test method runs. RecipeServiceTests/WeeklyListServiceTests/StoreServiceTests predated M9.13/M19 factory enforcement and never got that setUp update. Checklist addition: when adding/modifying a service with `factory: ManagedObjectFactory!`, also update its test file's setUp. | 3 service-test setUps needed 3 lines each (factory construction + 1-2 configure calls) to go from crash-looping to fully running. | Raw |
| Apr 19 | fix-test-harness-and-stale-assertions | Testing/SingletonReachThroughBreaksTestIsolation | A test can LOOK isolated (creates its own `NSPersistentContainer(name: "forager")` with an in-memory store) but still share state with the process's production singleton if the code under test reaches through that singleton internally. `RecipeImportService.persistAndFinish` calls `PersistenceController.shared.privateStore` — a separate controller from the test's one. `.shared` lazy-initializes the default (production) controller which has no loaded stores in the test process; the getter's force-unwrap crashes. The pragmatic fix: make `shared` a `static var` so the test can swap it for the test's lifetime. The architectural fix: inject PersistenceController via init instead of reaching through a singleton. Deferred to `establish-test-planning-workflow`. | `PersistenceController.shared = persistence` in setUp + restore in tearDown resolved 5 crashing tests; change was 1 line of production code (`let` → `var`) with a comment documenting the test-only intent. | Raw |
| Apr 19 | fix-test-harness-and-stale-assertions | Testing/KeepTestsAboutOneThing | `HybridParserRoutingTests.testParsersReceiveCorrectInput` used `"½ cup butter"` as input to verify that all three parsers receive the same string. When `IngredientPreprocessor` started normalizing Unicode fractions to ASCII before parser dispatch, the test broke — even though parser routing (what the test is about) still worked correctly. Lesson: test one thing at a time. If the test is about parser dispatch, use input that doesn't exercise preprocessing. Changing the input to `"1 cup butter"` decoupled the test from future preprocessor evolution without weakening its assertion about routing. Generalizable: test inputs should use the MINIMAL features needed to exercise the unit under test. Coupling a routing test to Unicode-normalization rules guarantees a failed assertion every time those rules evolve. | Pre-fix: test asserts parsers receive `["½ cup butter"]` — fails after preprocessor normalizes. Post-fix: test asserts parsers receive `["1 cup butter"]` — stable against future preprocessor changes, same routing verification. | Raw |
| Apr 19 | fix-test-harness-and-stale-assertions | Testing/InMemoryStoreURLsNeedCanonicalFilenames | `PersistenceController.privateStore`/`sharedStore` getters match persistent stores by `url.lastPathComponent` (looking for `"forager.sqlite"` / `"forager_shared.sqlite"`). In-memory test mode used `/dev/null` and `/dev/null-shared` as sentinel URLs — so `lastPathComponent` returned `"null"` and `"null-shared"`, failing the filename match. Any test code path that reaches those getters via an in-memory PersistenceController would fatalError. Fix: use `/dev/null/forager.sqlite` and `/dev/null/forager_shared.sqlite` — Core Data treats URLs as opaque identifiers for in-memory stores, so the filesystem path doesn't need to be real but the `lastPathComponent` DOES need to match production. Latent for months because no test previously reached those getters through an in-memory controller. | Pre-fix: `persistence.privateStore` crashes with "Private store not found" on an in-memory controller. Post-fix: same call resolves the in-memory store identically to how it would resolve a real file-backed one. | Raw |
| Apr 19 | architecture-compliance-sweep | Verification/GrepExistenceIsNotGrepContents | My initial exploration claimed `GroceryListItemService.resolveCategory` and `.resolveStore` needed `householdKey` predicates added (ADR 013 Item 1 of the narrowed plan). Ultraplan's re-verification caught the error: both predicates already scope by householdKey (lines 291-298, 330-337), plus `lookupDefaultStore` at 344. My mistake: I grep-matched the file's existence and inferred "this needs work" from the PRD's claim, rather than opening the file and reading the predicate contents. Generalizable rule for claims of "X is missing in file F": always open F and verify, never trust PRD-level claims about file contents without grounding. | Ultraplan output correctly noted Phase 1 Item 1 was a non-issue; post-finding grep of `Services/GroceryListItemService.swift` for `householdKey` confirms 3 scoped fetches, all using the standard nil-branch fallback | Raw |
| Apr 19 | architecture-compliance-sweep | SpecDrift/SpecsCanOverSpecifyBeyondADRs | `openspec/specs/architecture/spec.md` (lines 13, 19-21 pre-fix) mandated `householdKey` on `@FetchRequest` in views. ADR 013 doesn't say that — it explicitly targets service-layer fetches only and notes the view-layer in-memory filter as "existing pattern" without ruling on it. The spec drifted past the ADR. This is a new drift-risk category: not "ADR-to-code drift" (the familiar one) but **spec-to-ADR drift** where the authoritative spec over-specifies relative to the ADR it claims to codify. Consequence: readers who notice the contradiction don't know which is truth, weakening both documents. Fix: always narrow the spec to what the ADR actually says; if the spec needs to say more, update the ADR first (or explicitly mark the additional content as non-normative). | Pre-fix: spec says "@FetchRequest MUST include householdKey"; post-fix: spec says "services/repositories MUST include householdKey; view-layer @FetchRequest is out of scope pending future change." ADR 013 now has a "Scope of this ADR" section that matches the spec's boundary. | Raw |
| Apr 19 | architecture-compliance-sweep | Testing/SwiftUIPreviewsAreLegitimateSaveEnvironment | `grep 'context\.save()' forager/Views/` returns 6 matches but only 3 are production code. The other 3 live inside `#Preview { }` macro blocks and `PreviewProvider` extensions — which are a standalone rendering environment where `context.save()` is the normal way to stage preview data. The fix for the `/architecture-audit` Check 4 false positives is two-layer: `--exclude='*Preview*'` on the grep (catches dedicated preview files) plus prose instruction to discount in-body `#Preview` and `PreviewProvider` hits (catches the non-preview-named case where the macro lives inside a regular view file). Regex-based in-body bracket-matching would be brittle because preview blocks contain nested braces, closures, and multi-line content; human-readable prose is the pragmatic right answer until a Swift-AST-aware audit skill is worth building. | Pre-fix: Check 4 reports 6 matches (4 false positives after AddCategoryView was fixed, would be 6 before); post-fix: Check 4 reports 3 matches, all confirmed preview-block uses by manual inspection of MealPlanDetailView:703, RecipePIckerSheet (line ~340), SelectMealPlanSheet (line 340) | Raw |
| Apr 19 | architecture-compliance-sweep | Process/EmergentVsDesignedIsWorthPreservingInWriting | Earlier in the session I wrote an auto-memory entry framing the view-layer in-memory filter as "intentional design per ADR 016." Commit archaeology (first appearance `f263730` 2026-01-18, spread by copy-paste across 24 views with no coordinating commit/journal/ADR, ADR 013 acknowledges it post-hoc as "existing pattern" without ruling on it) proved it's emergent-not-designed. Corrected the memory to say that explicitly. Behavioral consequence: future Claude sessions loading the memory will now be LESS confident about extending the pattern and MORE willing to revisit it, vs. if the memory still called it "intentional." The emergent-vs-designed distinction has direct effect on how AI agents reason about whether to preserve a pattern, so the honesty in writing matters. | Memory file `project_scope_safety_three_layers.md` renamed from "Three-Layer Architecture" to "Two Deliberate Layers + One Emergent Pattern"; MEMORY.md pointer updated to flag the emergent half; correction itself captured in this insight so the correction-workflow is visible | Raw |
| Apr 19 | fix-dashboard-meal-plan-cold-start | Architecture/LazySingletonInitOrderHazard | `MealPlanService.shared` demonstrates a common-but-subtle pattern: `static let shared = Self()` + `private init()` that eagerly calls `loadActiveMealPlan()`. The init runs on first `.shared` access — which happens during `foragerApp.init()` BEFORE `householdKeyProvider` is wired. The fetch runs with the fallback nil-key predicate and populates `activeMealPlan = nil`. Nothing re-triggers the load when the provider becomes available. Dashboard renders; card is blank. The pattern to watch for anywhere this shape appears: `private init` doing work that depends on external state yet to be configured. Fixes come in three flavors, cheapest to most correct: (1) manual re-invoke the load after wiring (one line), (2) observe upstream state changes and reload automatically, (3) proper DI so the service can't be constructed without its dependencies. We chose (1) — the init-order is deterministic in app-init and a one-line completion is honest about the shape. (2) and (3) are queued in `establish-test-planning-workflow`. | Pre-fix: Dashboard Meal Plan card ghost-state on cold launch, populates only after visiting Meals tab. Post-fix: card renders active plan on first render. Reproducer: cold-launch app with active meal plan; observe Dashboard first render. | Raw |
| Apr 19 | fix-dashboard-meal-plan-cold-start | Architecture/DashboardLandingExposesLatentBugs | FUI-1 made Dashboard the landing tab. Every domain card on Dashboard now binds to state that previously only got `.onAppear`-refreshed on first-tab-visit. Bugs that were always present in the service-init order — but hidden because users had to navigate to a tab to see the symptom — are now visible on cold launch. The meal plan init-order race is the first; there may be others (need an audit of every Dashboard-card data source against their init-time vs. onAppear-time state). Generalizable: changing the app's landing surface is a visibility-increase event. Schedule an audit of all Dashboard data sources after any landing-tab change. | Hypothesis validated by Rich's observation + code trace. Additional audit: confirm `nextMeal`, `tomorrowMeal`, `activeGroceryList`, `planDaysFilled`, `planDayIndicators` (all Dashboard data accessors) have correct first-render behavior. Active grocery list uses @FetchRequest so safe; meal-plan data sources all go through `MealPlanService.shared` which this fix's reload covers. | Raw |
| Apr 19 | fix-dashboard-meal-plan-cold-start | Process/CodeReadingBeatsLogRequest | Rich offered to provide the diagnostic log when asking how the meal plan loads. I resisted and traced the init order in code instead. The code told the full story: singleton eager-init, provider wired after, fallback-to-nil-key in the predicate helper, `activeMealPlan` @Published but not re-triggered. The log would only have confirmed what the code already made deterministic. Generalizable rule: when the bug mechanism can be derived from static analysis (init order, predicate composition, control flow), don't ask for runtime data. Save the log request for bugs where runtime state (content of the store, timing of async operations, user actions) actually matters. | Arrived at correct root cause + one-line fix in ~15 minutes of code reading. Log would have added a confirmation step, not a diagnostic step. | Raw |
| Apr 18 | sync-status-line-with-focus | StatusLine/BranchKeyedFilesMakeSwitchingFree | The forager status line is already half-auto-updating: the polling script (`.claude/statusline.sh`, ~300ms) reads `~/.claude/forager-status-<branch-slug>.txt`, so branch switches auto-refresh via the filename. Only the orthogonal half — focus changes *within* a branch — needs explicit writes. This decomposition is the design win: the polling script stays dumb, the write-logic lives in skills that know when focus shifts, and the intersection (`forager-status-<slug>.txt`) is a plain file, not a protocol. | Switching branches during testing showed the bar flip labels without any additional logic; writing to the branch-keyed file mid-session updated the bar within one poll interval | Raw |
| Apr 18 | sync-status-line-with-focus | Process/RewriteDontDeleteStateFiles | The original `/milestone-complete` did `rm -f` on the branch-keyed status file as cleanup, which caused the bar to regress to raw-branch-name parsing (`.claude/statusline.sh` fallback Option 2) and lose the focus label at the moment work finished. Rewriting the file to `"[<id>] COMPLETE — awaiting merge"` instead preserves the narrative for zero cost. Generalizable rule: when a state file gets consumed by a polling reader and has fallback behavior, prefer rewriting over deleting — deletion triggers fallback, which is usually a lossy regression. | Pre-change: raw-branch bar label post-completion; post-change: "COMPLETE — awaiting merge" persists visibly | Raw |
| Apr 18 | sync-status-line-with-focus | Process/DogfoodValidatesEnforcement | Shipping `harden-pr-skill-doc-freshness` in Session 118, then starting `sync-status-line-with-focus` in Session 119 and having the gate fire immediately on the new change (stale journal + stale insights + missing PRD) was an unintentional but perfect validation test. The gate caught exactly the drift it was built to prevent, and on the *next* change after the gate shipped. Lesson: landing mechanical enforcement and then continuing work without special-casing is the cheapest way to confirm the enforcement actually works in production. If it doesn't fire, the enforcement isn't tight enough; if it fires and you bypass, it's dead. | `bash .claude/skills/_shared/doc-freshness.sh --mode=warn` on the new branch flagged 3 families as STALE; fixing all 3 made the report clean | Raw |
| Apr 18 | harden-pr-skill-doc-freshness | Process/MechanicalEnforcementBeatsAdvisoryReminders | Advisory reminders in skill prose ("Verify insights-log is current") do not prevent drift — PRs ship with stale docs because the advisory is easy to skim past. Installing a mechanical gate (`doc-freshness.sh --mode=block` in `/pr`) that exits non-zero on any stale family converts "remember to update the journal" from a rule-you-must-recall into a tool-surface constraint. Same pattern that made `ADR 014 factory enforcement` stick while `ADR 013 scope-aware-fetch` drifted to 45 violations without enforcement. | `/pr` in this change now fails fast with a table listing each stale family + remediation commands; the check reports FRESH/STALE/SKIP with per-family reasons | Raw |
| Apr 18 | harden-pr-skill-doc-freshness | Bash/PureCheckFunctionsEnableTestIsolation | Refactoring `check_journal`, `check_insights`, `check_prd`, `check_openspec` to pure functions that take `(identifier, diff_list)` as args and echo `STATUS\|reason` (rather than reading global state) made the `--test` block trivial: synthetic diff strings + a temp-dir fixture exercise every branch. The `DOC_FRESHNESS_ROOT_OVERRIDE` environment variable parameterizes the repo root for tests, so real `git diff` stays out of the fixture path. Same isolation principle as unit-tested pure functions in higher-level languages, just bash-flavored. | 12 self-test fixtures pass; no git invocation inside the test harness; all four families exercised in FRESH/STALE/SKIP states | Raw |
| Apr 18 | harden-pr-skill-doc-freshness | Shell/RemediationHintsMustBePaths | Initial implementation printed the full check-reason string in remediation hints (e.g., `Update PRD at docs/prds/.../foo.md not modified in branch diff` — the reason text got concatenated). Fix was trivial (strip the `" not modified in branch diff"` suffix) but it's a reminder: when data flows status+reason through a pipe-delimited string, remediation messages need their own assembly logic, not raw reason concatenation. | Before fix: "Update PRD at <path> not modified in branch diff"; after: "Update PRD at <path>" | Raw |
| Apr 18 | expand-claude-context-infrastructure | Git/PRStackingVsDeleteBranch | When stacked PR B targets PR A's branch as base, merging PR A with `--delete-branch` auto-closes PR B. GitHub refuses to reopen a closed PR whose base branch is deleted — the only path is to rebase PR B onto updated main and open a new PR. Rebase: `git rebase --onto main HEAD~<N>` where N is the count of PR-B-only commits. Merge downstream stacked PRs BEFORE merging the base PR, OR use `--no-delete-branch` on the base merge. | Merge #141 with --delete-branch → #142 auto-closes; GraphQL refuses reopen; must create new PR #143 after rebase | Raw |
| Apr 18 | expand-claude-context-infrastructure | Git/AccidentalMainCommitsRescue | Commits can land on main without a feature branch. Recovery pattern: `git branch <feature-branch>` FIRST (creates pointer at HEAD, preserves commits), THEN `git reset --hard origin/main` (moves main back safely). Order matters — reset-first loses the commits. `/session-start`'s "not on main" red-flag should catch this at session start; running it mid-session would have caught the drift. | Session 117 made 4 commits on main; rescued via branch-first pattern without data loss | Raw |
| Apr 18 | expand-claude-context-infrastructure | Shell/SelfTestBlockInUtility | Shell utilities benefit from a built-in `--test` flag that invokes a self-test harness within the same file. Pattern: check `$1 == --test` BEFORE main arg validation, run test suite, exit 0/non-zero. Keeps coverage collocated with implementation, survives relocation, is documentation-by-example. Caught a bare-`M9` edge case missed in manual review (regex was `{1,3}` when it should have been `{0,3}`). | `.claude/skills/_shared/milestone-format.sh --test` → 8/8 cases pass after fix | Raw |
| Apr 18 | expand-claude-context-infrastructure | MCP/SmokeTestWithoutRuntime | MCP tool functions can be smoke-tested without the full Claude Desktop runtime by importing the server module directly: `uv run python -c 'from src import server; print(server.get_capabilities())'`. Bypasses the MCP protocol layer but exercises all business logic (filesystem scans, parsing, BM25 queries). Saves the cost of a Claude Desktop restart cycle during development. Found a pre-existing repo artifact (leading-space filename in `Services/ RecipeScalingService.swift`) via this path. | 4 tool smoke tests in ~10s each; surfaced real repo artifact | Raw |
| Apr 18 | expand-claude-context-infrastructure | OpenSpec/PromotionDuringApply | When `/opsx:apply` writes delta-spec content DIRECTLY into the living capability spec file during apply (rather than only writing the delta under `specs/` in the change dir), the subsequent `/opsx:archive` promotion step becomes a no-op verification. Simplifies workflow: one write, not two. Delta spec files in archived change dir become historical/audit records; live content lives in `openspec/specs/*/spec.md`. | `openspec/specs/architecture/spec.md` (111 lines) written during apply; archive just moved the change dir | Raw |
| Apr 18 | seed-operating-model-foundations | OperatingModel/EnforcementGapsDriveDrift | Architectural patterns without mechanical enforcement accumulate drift proportional to time-since-adoption. ADR 013 (scope-aware fetch) drifted to 45 `@FetchRequest` occurrences across 28 view files because there was no `/architecture-audit` check for it, while ADR 014 (factory enforcement) stayed clean because the skill blocks violations. Every new architectural rule must ship with an enforcement mechanism (skill check, compile-time, or pre-commit hook) or it will drift. | `grep -rn '@FetchRequest' forager/` vs `grep -rn 'Entity(context:)' Services/` — ratio of violations tracks enforcement presence | Raw |
| Apr 18 | seed-operating-model-foundations | Planning/UmbrellaPRDsFailFocusedMilestonesShip | The January 2026 M9 umbrella PRD (47 FR-TD items, 137-176h estimate) never drove work; what shipped were small focused sub-milestones (M9.0, M9.5, M9.13, M9.16, M9.28, M9.30-M9.36) averaging 1-5h each. Umbrellas are optimistic trivia; focused milestones are real. Applying this: the post-launch-integrated-cleanup plan treats 130h of remaining debt as a living backlog, not a commitment — individual milestones get their own PRDs when scoped. | FR-TD numbering from the M9 umbrella is absent from every shipping artifact; archived M9.x changes use their own naming | Raw |
| Apr 18 | seed-operating-model-foundations | Tooling/UltraplanSandboxIsolation | Claude Code on the web runs in a sandboxed `/root/.claude/` filesystem that doesn't sync back to the local repo. Ultraplan can produce a refined plan but the artifact must be copied via the UI (or regenerated locally from pasted content) to land in the repo. Treat ultraplan's audit findings as authoritative — it re-checked ground truth in a fresh sandbox and caught drift between my local state and committed state (e.g., m9.37 PRD existed locally but not in the plan's view). | `ls /root/.claude/plans/` fails locally; `ls ~/.claude/plans/` succeeds — namespaces are distinct | Raw |
| Apr 18 | seed-operating-model-foundations | NamingMigration/ForwardOnlyResolvesChurn | Retroactive naming migrations are expensive churn. Forward-only policy (historical artifacts keep old convention, new work uses new convention) resolves the tension at near-zero cost. Historical commits, PRDs, ADR references, journal entries, in-flight legacy branches all stay `M#.#.#`; only new branches/changes/PRDs use OpenSpec change-id form. Shared dual-format utility in skills bridges tools that parse identifiers. | `grep -r 'M#.#.#' docs/prds/complete/` returns many hits (history preserved); `ls docs/prds/active/` shows architecture-compliance-sweep.md after rename | Raw |
| Apr 18 | seed-operating-model-foundations | CapabilityDesign/CrossCuttingConcernsNeedOwnSpec | OpenSpec specs are organized by product domain (grocery-lists, meal-planning, recipes), but cross-cutting architectural rules (scope-aware fetch, factory enforcement, service-save ownership, parser confidence thresholds) have no natural domain home. Creating `architecture` and `developer-tooling` as their own capabilities lets delta specs codify enforcement without duplicating rules across domain specs. Same pattern applies to any cross-cutting concern (observability, security, performance budgets) as the project grows. | `openspec/specs/architecture/spec.md` has 6 requirements derived from ADRs 007/010/012/013/014/service-layer; architecture-compliance-sweep adds delta specs against it | Raw |
| Apr 17 | M7.7 | AppStore/MetadataVsBinaryRejection | Apple's Resolution Center handles metadata rejections and binary/guideline rejections differently. For metadata rejections (e.g., 2.3.6 Age Rating), updating the metadata in ASC + replying in Resolution Center is sufficient — Apple continues the review automatically, no Resubmit click required. For binary/guideline rejections, an explicit Resubmit is required (and may be gated behind clicking "Cancel Submission" to unlock the button). Easy to conflate and give wrong advice. Hit during M7.7 rejection round 2 (Unrestricted Web Access missing due to recipe URL import). | Check Apple developer docs "Reply to App Review messages" + forum thread 743567 | Raw |
| Apr 17 | M7.7 | AppStore/UnrestrictedWebAccessTrigger | Any feature that fetches user-supplied URLs (forager's recipe import pipeline) qualifies as "unrestricted web access" for App Store age-rating purposes, even without an embedded WKWebView. Selecting Yes auto-bumps the age rating to 17+, which is mandatory and not negotiable. Only way to avoid 17+ would be to allowlist recipe domains — significant code change for no user benefit. | Check App Information > Age Rating after toggle — rating shows 17+ automatically | Raw |
| Apr 12 | M7.7 | Platform/CloudKitPrivacyExemption | Apple explicitly exempts CloudKit data from App Privacy nutrition label disclosure when: (1) data is stored solely in the user's own iCloud account, (2) the developer does not access it server-side, and (3) the purpose is to sync the user's own data. A CloudKit-only app with no analytics can potentially declare "Data Not Collected" — only non-CloudKit data flows (e.g., optional third-party API calls) need disclosure. forager declares one item (User Content) solely for the optional Claude API feature. | Check Apple's App Store Connect privacy documentation for CloudKit exemption language | Raw |
| Apr 12 | M7.7 | SwiftUI/ButtonInsideNavigationLink | The common ZStack pattern (`NavigationLink { EmptyView() }.opacity(0)` overlaid on card content) causes buttons inside the card to fire BOTH their action AND the NavigationLink. A tap on the button triggers navigation + the button action simultaneously. Fix: add `.buttonStyle(.borderless)` to buttons inside the card — this tells SwiftUI the button owns its own tap region and prevents the NavigationLink from intercepting it. Hit this with the Generate Grocery List button on MealPlanSummaryCard. | Remove `.buttonStyle(.borderless)` from a Button inside ZStack+NavigationLink → tapping button also navigates | Raw |
| Apr 12 | M19 | Architecture/OptionalFactoryRootCause | The root cause of ~15 factory bypass bugs was a single pattern: `factory: ManagedObjectFactory?` in services, forcing `if let factory` / `else` at every call site. The `else` branch silently creates unscoped entities in the wrong store. Making the type non-optional (`ManagedObjectFactory!`) eliminates the entire bug class at once — fixing the type is more effective than fixing individual sites. When an optional property should never be nil in production, the optionality IS the bug. | Grep for `if let factory` after fix — zero matches in production code | Raw |
| Apr 12 | M19 | CoreData/InMemoryStoreResolution | `PersistenceController(inMemory: true)` creates stores at `/dev/null` — `store(for: .private)` resolves by filename (`forager.sqlite`), which in-memory stores don't have. This means `ManagedObjectFactory.make()` crashes in test environments. Factory tests must validate scope resolution and household assignment without triggering `persistence.store(for:)`. Full factory integration tests require on-disk dual-store configuration. | Call `factory.make()` in test with in-memory persistence → fatalError on `store(for:)` | Raw |
| Apr 12 | M19 | Architecture/ChildEntityStoreInheritance | Core Data relationship store inheritance: when you set `item.weeklyList = parentList`, the child object inherits the parent's persistent store. Child entities (Ingredient, GroceryListItem) don't need factory — they get correct store placement from their parent relationship. This is documented in the M9.19 journal entry but was incorrectly flagged as a P1 factory violation during the initial audit. Ultraplan correctly downgraded these. | Create GroceryListItem, set `.weeklyList = list` where list is in shared store → item is in shared store | Raw |
| Apr 12 | M15.2 | Design/ColorTemperatureContrast | Warm-vs-cool color temperature contrast (parchment canvas #EDE8DF vs cool-white cards #FAFBFC) creates perceptual surface separation even at modest luminance ratios (1.18:1). The eye registers warm-on-cool as more different than warm-on-warm at the same luminance gap. More effective than simply darkening the canvas while keeping everything in the same temperature family. | Compare Option A (all-warm) vs Option C (warm-cool split) mockups at same luminance — C reads as more separated | Raw |
| Apr 12 | FUI-1.10.2 | SwiftUI/InsetGroupedHeaders | When using `.listStyle(.insetGrouped)`, Section headers render on the canvas background OUTSIDE the rounded section cards. Custom headers (ForagerSectionHeader) that replace system headers inherit this placement. To put headers inside the cards, move the header view from `Section(header:)` into the section content as the first row with matching `.listRowBackground()`. This gives headers the card surface background for proper contrast. | Move ForagerSectionHeader from header: parameter to first content row — header renders inside card | Raw |
| Apr 12 | FUI-1.9.5 | CoreData/MealPlanOverlapPrevention | `MealPlanService.createMealPlan()` had no overlap validation — it trusted callers to validate. The `CreateMealPlanSheet` UI validated, but `assignRecipeToToday()` (dashboard) bypassed it, creating overlapping plans. Fix: add `validatePlanDates()` call inside `createMealPlan()` itself so no caller can create overlaps regardless of entry point. Defense in depth — validate at the service level, not just the UI level. | Remove `validatePlanDates()` from `createMealPlan()` — overlapping plans can be created from any new caller | Raw |
| Apr 10 | FUI-1.9 | SwiftUI/GroupWrapperForBranches | SwiftUI `if/else` blocks inside `ForEach` can't have view modifiers (`.listRowBackground()`, `.listRowSeparator()`, etc.) applied to the branch result directly — the compiler sees the `if/else` as producing different types. Wrap in `Group {}` to erase the type difference, then apply modifiers to the Group. Hit this when adding `onSelect` conditional in RecipeListView grid/list rendering. | Add `if/else` inside ForEach without Group → compiler error on `.listRowBackground()` | Raw |
| Apr 10 | FUI-1.9 | DarkMode/WCAGContrast | `.white` text on `ForagerTheme.accentPrimary` (#7BC08A in dark mode, light green) has contrast ratio 2.5:1 — fails WCAG AA (4.5:1 required). Light mode is fine (white on #2D5016 dark green = 7.8:1). Always use `ForagerTheme.buttonPrimaryText` for text on accent backgrounds — it adapts (#FFFFFF light, #1C1A14 dark). Found in MealPlanDetailView TODAY circle, Add to Grocery button, and MealPlanListView day dots. | Switch to dark mode → check text on green accent buttons/circles for readability | Raw |
| Apr 10 | FUI-1.9.1 | SwiftUI/HiddenNavigationLink | To remove the disclosure chevron (>) from NavigationLink rows in a List, use a ZStack with the NavigationLink wrapping EmptyView at opacity(0) layered behind the visible content. The NavigationLink still handles navigation but the system chevron is hidden since the label is invisible. Applied to 10 instances across RecipeListView, MealPlanListView, WeeklyListsView, SettingsView, and HouseholdView. | Remove ZStack wrapper → chevron reappears in List row | Raw |
| Apr 10 | FUI-1.9 | UX/GhostCardPattern | Ghost card pattern (dashed outline, no background fill, actionable text like "No recipe for today. Tap to pick one.") is effective for teaching users what empty dashboard cards can do while maintaining visual hierarchy between populated and empty states. Better than hiding cards entirely (dashboard feels empty) or showing filled cards with "empty" content (looks broken). The dashed border is the key visual cue — instantly reads as "placeholder, not error." | Compare dashboard with 0 data: ghost cards vs hidden cards (git diff) | Raw |
| Apr 7 | INFRA-1 | Framework/SkillsAreJustMarkdown | Clauductor's 28 skills were functionally just markdown prompt templates in `.claude/skills/` — they never needed a framework to function. The Go binary only existed for the orchestration layer (SQLite worker coordination, file locking, HUD). Removing orchestration made the "framework" dissolve into standalone skill files. When evaluating whether to build a framework vs. use convention, check if the value is in the templates or the runtime. If templates, you don't need a binary. | List `.claude/skills/` after removing clauductor — all 17 skills work identically | Raw |
| Apr 7 | INFRA-1 | Framework/PlanningVsExecution | OpenSpec and Clauductor solve different problems with partial overlap. OpenSpec = planning layer (living specs, change proposals, delta tracking). Clauductor = execution layer (multi-worker coordination, file locking, build automation). The overlap is only in the planning artifacts (PRDs vs specs, next-prompts vs tasks). Recognizing this decomposition enabled a clean migration: adopt OpenSpec for planning, keep skills for execution, drop orchestration entirely. | Compare `openspec/` (planning) vs `.claude/skills/` (execution) — orthogonal concerns | Raw |
| Apr 7 | INFRA-1 | OpenSpec/BootstrapFromPRDs | Converting existing PRDs to OpenSpec living specs is a natural bootstrap path. Each PRD maps to a domain spec (`openspec/specs/<domain>/spec.md`). The key transformation: PRDs describe what to *build* (implementation-focused), specs describe what the system *does* (behavior-focused, with MUST/SHALL requirements and Given/When/Then scenarios). Active work maps to `openspec/changes/` with proposal + design + tasks. | Read any spec in `openspec/specs/` — requirements describe current behavior, not planned features | Raw |
| Apr 7 | INFRA-1 | Git/GitignoreExclusion | `.gitignore` pattern `.claude/*` with `!.claude/skills/` excludes everything except skills. OpenSpec adds `.claude/commands/` which was also gitignored. Must add `!.claude/commands/` and `!.claude/hooks/` exclusions to version-control tool-generated files. Easy to miss because the files exist locally (created by `openspec init`) but silently won't be committed. | Run `git status` after `openspec init` — commands should appear as untracked, not ignored | Raw |
| Apr 7 | INFRA-1 | Hooks/GracefulDegradation | Clauductor hooks all started with `test -d "$PROJECT_DIR/orchestration" || exit 0` — graceful no-op without orchestration. This meant deleting the `orchestration/` directory effectively disabled all orchestration hooks without touching settings.json. Design hooks with an existence check on their dependency so removal is always safe. | Delete orchestration/ → all hooks silently no-op | Raw |
| Apr 2 | M9.36 | CloudKit/PublicPermission | CloudKit public-link participants are NOT permanent. Their access depends on `publicPermission` remaining `.readWrite` — setting it to `.none` instantly removes them from `CKShare.participants`. Unlike Google Docs where "anyone with link" participants persist after link is disabled, CloudKit's `publicPermission` is the ongoing authorization mechanism, not a one-time gate. Any code that reverts `publicPermission` must first check for non-owner participants. Discovered when M9.30 auto-expiry kicked an accepted member on every app restart. | Create household, invite member, member accepts. Revert `publicPermission` to `.none` → member disappears from `share.participants` | Raw |
| Apr 2 | M9.36 | CloudKit/DiagnosticDiscovery | Diagnostic log from TestFlight device revealed the bug without needing a crash log or Xcode debugger. The `DiagnosticLogger` captured the exact sequence: CKShare participant count before (2) and after (1) the permission revert, with timestamps. In-app diagnostic logging is invaluable for debugging CloudKit issues on real devices where Xcode isn't attached. | Check `rich.log` from TestFlight device — full startup sequence visible | Raw |
| Apr 1 | M18.1.4 | Architecture/TestableGrouping | View-layer grouping logic (Dictionary grouping + sorting) can be extracted to a static service method for direct unit testing. `StoreService.groupByStore(items:stores:)` takes pure data in, returns tuples out — no SwiftUI view instantiation needed. Same pattern could retrofit category grouping if tests are ever needed there. | Run `StoreGroupingTests` — all 9 tests pass without any SwiftUI dependency | Raw |
| Apr 1 | M18.1.4 | SwiftUI/OrthogonalCollapse | When a view has two independent section-collapsing modes (category vs store), use separate `@State` sets (`collapsedCategories` vs `collapsedSections`). Sharing one set causes cross-mode interference — collapsing "Dairy" in category mode would also collapse a store named "Dairy". | Toggle between group modes → collapse state is independent per mode | Raw |
| Apr 1 | FUI-1.6 | SwiftUI/GridVsList | `LazyVGrid` does not support `.swipeActions` — that modifier only works on `List` rows. When implementing grid/list toggle layouts, grid items need `.contextMenu` for equivalent actions (delete, add to meal plan). Easy gotcha when porting a List-based view to support both layouts. | Add `.swipeActions` to a `LazyVGrid` item → compiler accepts it but it never renders; switch to `.contextMenu` | Raw |
| Apr 1 | M10.4.0 | Architecture/DualSavePath | `RecipeImportService` has two independent save paths (`saveImport` and `replaceExistingRecipe`) that both write Recipe fields from `ImportDraftRecipe`. When adding new Recipe attributes, both paths must be wired — they're 100+ lines apart and easy to miss. Will recur when deferred fields (description, cuisine, category) are persisted. | Add a new optional field to Recipe → grep for both `saveImport` and `replaceExistingRecipe` to confirm both are updated | Raw |
| Mar 31 | FRMWK-2.5 | Hooks/JSONProtocol | Claude Code hooks must output JSON via `hookSpecificOutput`, not plain text echo. Plain text output from hook scripts is silently discarded — the hook runs but its message/decision never reaches Claude. All hooks must format output as `{"hookSpecificOutput": {"message": "...", "permissionDecision": "..."}}`. | Run architecture-guard hook → verify JSON output appears in Claude response | Raw |
| Mar 31 | FRMWK-2.5 | Hooks/DenyVsAsk | `permissionDecision: "deny"` blocks tool calls and cannot be bypassed, even with "allow all edits during session". `permissionDecision: "ask"` prompts but can be bypassed. For architectural guards that must enforce invariants, always use `deny`. | Trigger architecture guard → verify no bypass option offered | Raw |
| Mar 31 | FRMWK-2.5 | Skills/FrontmatterPrecedence | Claude reads the skill description from YAML frontmatter first. If frontmatter says "degrades gracefully" but body says "always register", Claude may follow the frontmatter description. Frontmatter descriptions are behavioral directives, not just metadata — they must align with body instructions. | Compare skill frontmatter description with body CRITICAL markers | Raw |
| Mar 31 | FRMWK-2.5 | Skills/ChainingSkip | When a skill body says "Run /claim", Claude may skip it entirely. Critical commands must be inlined directly in the skill body rather than referenced as other skill invocations. Add CRITICAL markers to prevent skipping. | Check start-work SKILL.md for inline claim commands vs /claim references | Raw |
| Mar 31 | FRMWK-2.5 | Git/BranchAgnosticFiles | `.claude/settings.json` is gitignored (persists across branches) but `.claude/hooks/` is tracked (branch-specific). When switching branches, hooks may reference paths or configs that don't exist on the target branch, causing hook errors. Settings and hooks have different lifecycle scopes. | Switch between branches with different hook sets → check for errors | Raw |
| Mar 31 | FRMWK-2.5 | Go/TTYPassthrough | `exec.Command` in Go defaults Stdin to `os.DevNull`. Commands that need a TTY (like `tmux attach`) will fail silently or error. Must explicitly set `cmd.Stdin = os.Stdin` for TTY passthrough. | Check clauductor source for Stdin assignment on tmux commands | Raw |
| Mar 29 | FRMWK-1 | Framework/SettingsTier | Clauductor classifies settings.json as `tierFramework` (always overwrite, not merge). Projects with extensive permissions in settings.json or settings.local.json should manually merge or skip the overwrite. Forager's was identical by coincidence. | Check `classifyFile` in clauductor install.go | Raw |
| Mar 29 | FRMWK-1 | Framework/SkillCollision | When renaming domain-specific skills to match framework names (e.g., forager-architecture-audit → architecture-audit), the framework install may have already created a blank template at that path. Must replace the template with the project's production version, not the other way around. | Check architecture-audit/SKILL.md has 4 hardcoded checks, not "CONFIGURE FIRST" | Raw |
| Mar 29 | FRMWK-1 | Documentation/Staleness | development-guidelines.md drifted 5 months (Oct 2025 → Mar 2026) because it's not in the 7 core docs list. Files outside the mandatory update cycle will silently go stale. Consider adding a staleness check to `/session-start` for non-core docs. | Read docs/development-guidelines.md — should show v4.0, March 2026 | Raw |
| Mar 28 | M16.9 | Testing/SchemeTestables | Xcode scheme's `<Testables>` must explicitly include the unit test target for `xcodebuild test` to work, even when a `.xctestplan` references it. The test plan alone is NOT sufficient. Root cause of months of broken CLI test runner. | Run `xcodebuild test -only-testing:foragerTests` → should succeed | Raw |
| Mar 28 | M16.9 | Parsing/PreprocessorInteraction | Multi-tier pipeline interaction: `convertLeadingNumberWords("one")` → `"1"` broke regex compound phrase pattern expecting word-number. Fix: skip conversion when followed by "and a half/quarter/third". **Always run full test suite after preprocessing changes.** | Parse "one and a half cups milk" → qty=1.5, unit=cup, name=milk | Raw |
| Mar 28 | M16.9 | Testing/MLAssertions | ML model tests need looser assertions than regex tests. Regex is deterministic (`assertEqual`), ML is probabilistic (`contains`, `greaterThan`). v2 model labels tokens differently on some edge cases — expected statistical variation, not bugs. | Check MLIngredientParserTests use `contains` not `assertEqual` for names | Raw |
| Mar 28 | M18 | CoreData/Schema | Batch schema changes across milestones into a single migration when both are on the launch path. M18 (Store entity) + M10.4 (Recipe.imageURL/author) combined into v10→v11. Avoids two version bumps and CloudKit append-only overhead. The import pipeline already extracted imageURL/author but `toRecipeFormData()` explicitly dropped them (line 133 comment). Storing optional strings now is zero-cost future-proofing — no migration needed when image rendering ships later. **Key: review upcoming milestones for schema overlap before committing to a version bump.** | Check forager 11.xcdatamodel has both Store entity and Recipe.imageURL/author | Raw |
| Mar 26 | M16 | Parsing/NameOnlyPattern | No-quantity ingredients ("Salt and pepper", "Extra virgin olive oil") fell to NLP at 0.30 confidence because all regex patterns expect a leading number. Adding a "name-only" Pattern 8 at 0.85 confidence as the LAST pattern in the chain dropped NLP fallback from 7% to 0.5%. **Key: the pattern must run last and use a confidence below the standard patterns (0.92-1.0) but above NLP cap (0.75).** | Parse "freshly ground black pepper" → should be regex at 0.85, not NLP at 0.30 | Raw |
| Mar 26 | M16 | Testing/FreshValidation | Running fixes against the same test set risks overfitting. Fresh recipe validation (50 new recipes from replenished seed list) confirmed parser fixes generalized — confidence held at 0.97, regex coverage held at 100%. BUT core agreement dropped from 82% to 74%, revealing new patterns (leading decimals, metric range parens) the original 42 recipes didn't have. **Always validate with unseen data before declaring victory.** | Compare metrics on rerun-last vs fresh run | Raw |
| Mar 26 | M16 | ML/TrainingDataAsByproduct | Every AI-enabled harness run produces labeled training data for free. 9 runs × ~500 ingredients = 1,440 entries across 19 sites. The dedup-by-rawText strategy means re-running same recipes updates labels rather than duplicating. **Dual-purpose tool: immediate parser debugging + long-term ML training data accumulation.** | Check Results/training-data.json entry count after runs | Raw |
| Mar 26 | M16 | Testing/ComparisonLogic | Automated parser comparison (local vs AI) showed 37% agreement on 560 ingredients — alarming until analysis revealed the metric was fundamentally flawed. ~284 "name mismatches" were actually **design differences**: local keeps descriptors ("small onion, diced") while AI normalizes to canonical ("onion"). Neither is wrong. The faulty exact-match comparison buried 7 real parser bugs under 284 false positives. **Lesson: comparison metrics must account for intentional design differences, not just string equality.** Two-tier scoring (core match + full match) separates signal from noise. Before: 37% agreement. After (projected): ~85% core agreement with real issues clearly visible. | Run harness, check "Core agreement" vs "Full agreement" in report | Raw |
| Mar 26 | M16 | Testing/HarnessROI | First automated harness run (42 recipes, 560 ingredients, ~8 min with AI) found 7 real parser bugs that would have taken hours of manual device testing to discover. The bugs were systemic (hyphenated can sizes, number words, "about" prefix) affecting dozens of recipes each — the kind of bugs you'd only catch by testing at scale. The harness also accumulated 514 ML training data entries as a byproduct. **Standalone CLI testing of parsing code (no simulator, no device) is transformatively faster than in-app testing.** | Compare time: 8min harness run vs estimated 2+ hours manual testing for equivalent coverage | Raw |
| Mar 26 | M16 | Architecture/SymlinkVsCopy | For experimental code refinement, copying files into a harness (not symlinking) was the right call. It creates a safe sandbox: parser fixes are validated in the harness before touching the app. The trade-off is divergence — you must port fixes back. But the safety is worth it: a bad regex change in the harness doesn't ship to TestFlight. | Check that Services/Parsing/ files are unchanged after harness runs | Raw |
| Mar 25 | M9.35.2 | Parsing/RegexBacktracking | Greedy regex `([a-zA-Z]+)?\s*(.+)$` splits short words at character boundaries — "egg" becomes unit="eg", name="g" because the greedy group backtracks just enough for `.+` to match ≥1 char. The recombination then produces "eg g" with a space. **Fix: reject when captured unit is unknown AND remaining name is ≤1 char**, letting the count-noun pattern handle it. General rule: when a greedy group feeds into `.+`, test with 3-4 letter inputs. | Parse "1 egg" → name should be "egg", not "eg g" | Raw |
| Mar 25 | M9.35.2 | Parsing/ConfidenceRouting | Confidence thresholds in multi-tier parser routing are load-bearing architectural decisions. A correct regex parse at 0.70 loses to a wrong ML parse at 0.75 when the hybrid router's auto-accept threshold is 0.9 — both fall through, and the ML result wins on raw confidence. **The confidence value IS the routing decision.** Raising comma-qualifier confidence from 0.70 to 0.92 fixed ~145 prep-in-name instances. | Parse "garlic, crushed" → name "garlic" + notes "crushed" (not "garlic crushed") | Raw |
| Mar 25 | M9.35.2 | Parsing/IEEE754Floats | Recipe sites (AllRecipes) store Unicode fractions like ⅓ as IEEE 754 floats in JSON-LD: `0.33333334326744`. Detection: 8+ digits after decimal (user input never has this precision). Conversion: closest-fraction lookup against 13 common cooking fractions by minimum distance — more robust than exact matching since different platforms produce different float artifacts. | Import AllRecipes recipe with ⅓ cup → displays "1/3 cup" not "0.33333334326744 cup" | Raw |
| Mar 22 | M9.30 | CloudKit/ShareCorruption | Third-party share acceptance via public URL can corrupt the CKShare record on the server, destroying the household for all participants. Joe's device never found the household (discovery timeout x30), but the act of processing the share URL via `container.accept(metadata)` corrupted the CKShare — the owner's device immediately started getting "Household does not have a CloudKit share record" errors, and on next launch the household was ghost-detected and deleted. The public link acceptance flow needs resilience: error recovery, CKShare integrity validation, and protection against cascading share corruption to the owner's household. | Invite 3rd person to household → verify household survives on owner device | Raw |
| Mar 22 | M9.30 | SwiftUI/ButtonStyleGlassCard | Multiple buttons/links inside a VStack wrapped in `.foragerGlassCard()` absorb all taps and route them to whichever element SwiftUI picks first. Root cause of 3 bugs: Settings Data section (Ingredients/Categories/Restore all triggered together), AI section (Test Connection opened Get API Key link instead), About section (Replay Onboarding opened Privacy Policy). **Fix: `.buttonStyle(.borderless)` on every button/link inside a glass card VStack.** | Tap each button independently in a glass card with 3+ buttons | Raw |
| Mar 22 | M9.30 | SwiftUI/DiagnosticLoggerCrash | Calling `DiagnosticLogger.shared` (an `@MainActor` singleton with `@Published` properties) from a computed property that SwiftUI evaluates during view layout causes a re-entrancy crash. The view body evaluation triggers `objectWillChange` on the logger, which triggers another view update mid-layout. **Rule: never call `@MainActor` singletons with `@Published` from computed properties in the SwiftUI render path. Use `#if DEBUG print()` instead.** | Add DiagnosticLogger call to a computed property read by SwiftUI → crash | Raw |
| Mar 22 | M9.30 | Security/KeychainWriteRace | Keychain delete-then-add pattern (`SecItemDelete` + `SecItemAdd`) can race — if another thread re-adds between delete and add, `SecItemAdd` fails with `errSecDuplicateItem` and the write is silently lost. **Fix: update-first pattern — `SecItemUpdate` (atomic, no race window), fallback to `SecItemAdd` only if `errSecItemNotFound`.** | Rapid save/save of same Keychain key → verify both succeed | Raw |
| Mar 21 | M9.27 | Onboarding/SampleDataRemoval | Removing ~700 lines of SampleDataSeeder eliminated an entire class of ADR 014 violations (direct Entity(context:) for HouseholdScoped types) and household-scoping edge cases. Sample data created during first launch had no householdKey, causing confusion when users created households. Empty states with ContentUnavailableView + action buttons are sufficient for teaching features. **Rule: prefer contextual empty states over pre-populated sample data — they're always accurate and never create scoping bugs.** | Fresh install → empty tabs with action buttons → user understands what to do | Raw |
| Mar 21 | M9.24 | CloudKit/MemberStoreRouting | On member devices, `persistAndFinish` hardcoded `viewContext.assign(obj, to: privateStore)` — correct for the owner (zone routing via relationships) but wrong for members. Member's private store mirrors their personal CloudKit database, invisible to the owner. Fix: scope-aware store assignment using `HouseholdScopeProvider.activeScope` — returns `.shared` on member devices, `.private` on owner. Same pattern `ManagedObjectFactory.make()` already uses. **Rule: never hardcode store assignment — always resolve via scope provider.** | Import recipe on member device → recipe visible to owner | Raw |
| Mar 19 | M9.23 | CoreData/InverseRelationshipDirtying | Setting `template.categoryEntity = privateCategory` dirties the Category via its inverse relationship (`ingredientTemplates` set changed). Core Data then validates ALL relationships on that dirty Category during save — including pre-existing cross-store template refs that have nothing to do with the import. On member devices, private-store Categories have inverse refs to shared-store templates (from CloudKit sync), so even though the Category itself is in the correct store, its dirty state triggers cross-store validation (NSCocoaErrorDomain 134040). **Fix: refresh ALL updated non-inserted objects before save, not just shared-store ones.** This is a subtle variant of the cross-store relationship problem — the relationship isn't on the object you're creating, it's on a pre-existing object dirtied by an inverse. | Import recipe on member device → save succeeds; check diagnostic log for "refreshing updated obj" entries covering both stores | Raw |
| Mar 16 | M9.21 | CloudKit/DualStoreAsymmetry | On the **owner's** phone, the shared store (`forager_shared.sqlite`) mirrors the shared CloudKit *database* — which contains data shared **by other users with the owner**, NOT the owner's own shared data. The owner's shared zones live in the private CloudKit database, mirrored by `forager.sqlite`. M9.20's `assignToShared()` sent copies to a store that mirrors the wrong database entirely. **Rule: on the owner's phone, ALL their data (including shared household data) belongs in the private store. The shared store is only populated by CloudKit when other users share data with you.** | Create household → copies stay in forager.sqlite → participant sees them in forager_shared.sqlite | Raw |
| Mar 16 | M9.21 | CloudKit/ZoneRoutingViaRelationships | The CloudKit mirroring delegate uses **Core Data relationships** (not string attributes) to determine which shared zone a new CKRecord belongs to. `householdKey` (String) is invisible to zone routing — it's opaque data to CloudKit. When `container.share([household])` creates a shared zone, subsequent objects need a Core Data relationship back to the household for the mirroring delegate to place their CKRecords in that zone. M9.19 removed this relationship (to avoid cross-store issues), but at copy time both household and copies are in the private store — the relationship is same-store and safe. **Rule: copied entities need `new.household = household` for CloudKit zone assignment, AND `householdKey` for fetch predicates. Different concerns, both required.** | Create household → copy data with relationship → participant sees all data | Raw |
| Mar 16 | M9.21 | CloudKit/ThreeSessionsOneInsight | M9.19, M9.20, and M9.21 each fixed the "data not visible to household members" bug — and each was locally logical but globally wrong. M9.19 correctly identified cross-store relationships as dangerous, but removed a safe one. M9.20 correctly identified "wrong store" as the problem, but targeted the wrong store. It took 3 sessions to build the complete mental model of how NSPersistentCloudKitContainer routes data between stores, databases, and zones. **Rule: when a fix doesn't work, question the mental model — not just the implementation.** | N/A — process insight | Raw |
| Mar 14 | M9.19 | CloudKit/CrossStoreRelationship | `copyPersonalDataToHousehold()` set `new.household = household` on all 10 entity types. But after `container.share([household])` + `save()`, the Household moves to the **shared store**. Every copied entity in the private store then had a cross-store relationship that the CloudKit mirroring delegate **silently refused to export**. Data saved locally but never uploaded to CloudKit — permanently lost on reinstall. Fix: remove all `household` relationship assignments; use `householdKey` (String) only. **Rule: after `container.share()`, NEVER set relationships from private-store objects to the now-shared-store Household.** | Delete app → reinstall → verify all recipes, categories, templates sync back from CloudKit | Raw |
| Mar 14 | M9.19 | CloudKit/SilentMirroringFailure | Cross-store Core Data relationships cause NSPersistentCloudKitContainer's mirroring delegate to silently skip records during export. No crash, no error callback, no console warning. The only symptom is data that exists locally but never appears after reinstall. **This is the most dangerous failure mode in dual-store CloudKit.** | Force-quit → data present; delete+reinstall → data absent (before fix); data present (after fix) | Raw |
| Mar 14 | M9.19 | CloudKit/HouseholdKeyOverRelationship | `householdKey` (String attribute) is strictly more reliable than `household` (Core Data relationship) for scoping. String is store-independent, all fetches use it per ADR 013, not all creation paths set the relationship. **Rule: for newly created household-scoped entities, set `householdKey` only. Set `household` relationship only when both objects are known to be in the same store.** | `grep 'household = household' HouseholdService.swift` → zero results in copyPersonalDataToHousehold | Raw |
| Mar 14 | M9.18 | CloudKit/GhostDetectionFalsePositive | `isCurrentUserParticipant()` treated `HouseholdError.noShareRecord` as "user was removed from share" unconditionally. But a Household in the **private store** has no CKShare by design — it only gets one after `container.share()` migrates it. Fix: check `household.objectID.persistentStore == privateStore` before treating as ghost. **Rule: private-store Households are normal during CloudKit migration; only shared-store Households without a CKShare are genuine ghosts.** | Create household → immediately check → not deleted; stale shared-store household → correctly identified as ghost | Raw |
| Mar 14 | M9.19 | CloudKit/ArchitecturalOrigin | Pre-M7, Forager was entirely local — single-store Core Data with no CloudKit. M7 introduced dual stores, Household entity, and `householdKey` scoping. Every CloudKit bug since stems from the transition from single-store to dual-store. The pre-M7 codebase assumed one store; M7+ requires every creation path, fetch predicate, and relationship assignment to be store-aware. | N/A — architectural context | Raw |
| Mar 14 | M9.17 | LLM/BatchBlastRadius | `parseBatchWithLLM()` returned `nil` (complete failure) when AI returned fewer results than inputs. Fix: pad shortfall with local-parse fallback. **Rule: batch operations should degrade gracefully on partial failure — never discard N-1 good results because 1 failed.** | Import recipe where AI drops 1 ingredient → remaining N-1 still have AI categories | Raw |
| Mar 14 | M9.16 | Service/UnifiedPipeline | Consolidating 6 independent GroceryListItem creation paths into one service eliminated a recurring bug fixed 4 separate times. **Rule: when the same bug class recurs across multiple code paths, the fix is consolidation into a single service.** | All paths produce categorized items; new paths auto-get categories via service | Raw |
| Mar 14 | M9.16 | CoreData/SkipSaveBatch | `addIngredients()` calling `save()` internally would `rollback()` the entire context on failure. Fix: `skipSave: Bool` parameter. **Rule: batch operations called as sub-steps of larger transactions must support deferred saves.** | MealPlanService.generateGroceryList → addIngredients(skipSave: true) → single save at end | Raw |
| Mar 14 | M9.16 | SwiftUI/SheetSequencing | `DispatchQueue.main.asyncAfter` for sheet-to-sheet transitions is fragile. `.sheet(isPresented:, onDismiss:)` callback is the correct pattern. | Select list sheet → dismiss → ingredient selection sheet appears reliably | Raw |
| Mar 14 | M9.15.3 | CoreData/RelationshipVsKey | `migrateHouseholdDataToPersonal` fetched via relationships, but `findOrCreateTemplate` only sets `householdKey`. Fix: fetch by predicate. **Rule: query by the field ALL paths set (householdKey), not a relationship only some paths set.** | Create household → import recipes → delete household → templates migrate to personal | Raw |
| Mar 14 | M9.15.3 | CoreData/MigrationDedupMapping | Migration skipped duplicates but didn't map old UUID → existing entity, breaking downstream UUID-based mapping dicts. Fix: add skipped items to mapping dict. | Create household → import recipes with categories → delete household → categories preserved | Raw |
| Mar 14 | M9.15.3 | CoreData/CrossStoreSafety | `MealPlanService.generateGroceryList()` assigned cross-store categoryEntity without checking stores. **Rule: audit ALL code paths that assign Core Data relationships for cross-store safety.** | Generate grocery list from meal plan in household → categories appear | Raw |
| Mar 13 | M9.15.3 | CloudKit/ScopeResolution | Scope determined by store location rather than Household existence caused nil householdKey on all new entities. **Rule: scope determination must be based on entity existence, not store location.** | Create household → create entities → all appear in views immediately | Raw |
| Mar 13 | M9.15.3 | CloudKit/DiagnosticLogging | File-based DiagnosticLogger writing to Documents/ fills the gap where OSLog is inaccessible to TestFlight users. Dual-write pattern (OSLog + file) preserves both developer and user-accessible diagnostics. | Settings > Diagnostics > Diagnostic Log → shows events in Release build | Raw |
| Mar 13 | M9.15.3 | CloudKit/GhostHousehold | Failed `createHouseholdAndShare()` creates ghost Household that CloudKit re-downloads forever. Fix: explicitly delete ghosts so deletion syncs to CloudKit. | Delete app → reinstall → ghost household deleted, discovery times out cleanly | Raw |
| Mar 12 | M9.15 | CloudKit/ZoneImmutability | Zone assignment is permanent — `container.share()` cannot move existing CKRecords. "Attach-then-share" is broken for objects with existing CKRecords. Solution: "create-empty-then-share" — share empty Household first, then copy data as new objects. | Create household with existing data → no 134060 error | Raw |
| Mar 13 | M9.15 | CoreData/DynamicAttributeCopy | `NSEntityDescription.attributesByName` enables generic `copyAllAttributes(from:to:)` resilient to future schema additions. Skip `id` (fresh UUIDs) and relationships (reconstructed via maps). | Add new attribute → copy still works without code changes | Raw |
| Mar 12 | M9.15 | CoreData/ChildHouseholdScoped | Child entities (Ingredient, GroceryListItem) inherit `household`/`householdKey` from parent. Documented in ADR 014. | All Ingredient sites use parent pattern | Raw |
| Mar 12 | M9.14 | CloudKit/ObjectIDStaleness | `NSManagedObjectID` becomes stale after reinstall + CloudKit sync. Fix: fallback to fetchLimit=1 request. | Delete app → reinstall → join household → create meal plan → succeeds | Raw |
| Mar 12 | M9.14 | Import/HTMLEntity | Multiple extraction paths feeding same field must ALL apply same normalization. | Import recipe with "&" in title → displays correctly | Raw |
| Mar 11 | M9.13 | CoreData/FactoryHardening | `try? factory.make()` silently swallows errors. Convert to `do/catch` with `#if DEBUG print()`. `private(set) var factory` with `configure(factory:)` injection. | `grep 'try? factory.make'` → 0 results | Raw |
| Mar 11 | M9.13 | CoreData/FactoryEnforcement | `ManagedObjectFactory` established in M7.2.3 but never enforced until M9.13. `context.assign()` is fragile; factory-based creation resolves scope at creation time. | `/forager-architecture-audit` → zero violations | Raw |
| Mar 10 | M9.12 | CoreData/CrossStore | `Entity(context:)` defaults to private store. Cross-store relationships silently fail on save. Fix: `context.assign()` to target store. | Join household → import → add to grocery list → items appear | Raw |
| Mar 10 | M9.12 | CoreData/DefaultCategory | Default values flowing through update-or-create paths must not trigger the update branch. | Import with AI categories → categories preserved | Raw |
| Mar 9 | M9.12 | Import/HTMLEntity | Apply decoding to all string extraction from same source, not just one field. | Import recipe with "&amp;" in title → displays as "&" | Raw |
| Mar 9 | M9.12 | CoreData/DefaultCategory | Defaulting to "Uncategorized" entity eliminates category assignment modal. Cascading impact across 10 locations in 5 files. | Fresh install → import → no category modal | Raw |
| Mar 8 | M9.12 | CoreData/M9.12CategoryMigration | String-to-entity category migration across 30+ files requires 3 rounds of codebase auditing. | Build succeeds; category rename propagates via relationship | Raw |
| Mar 8 | M10.6.20 | CoreData/CrossStoreRelationships | GroceryItem.categoryEntity creates cross-store risk. Fix: NULL before migration, preserve string fallback. | Create household → no zone corruption errors | Raw |
| Mar 8 | M10.6.20 | CoreData/PlannedMealMigration | PlannedMeal was missing from all 4 household migration helpers. Fixed with mapping dictionaries. | Leave household → MealPlans AND PlannedMeals copy correctly | Raw |
| Mar 7 | M16 | Skills/AutoTrigger | Skill descriptions with TRIGGER phrases enable auto-matching without slash commands. | Say "commit this" → skill auto-activates | Raw |
| Mar 7 | M16 | Skills/SubAgents | Sub-agents do NOT inherit skills from main conversation. Must list in agent.md `skills:` field. | Delegate to agent → verify skills loaded | Raw |
| Mar 7 | M16 | Skills/ProgressiveDisclosure | Keep SKILL.md under 500 lines. Scripts execute without loading source into context. | Check context usage with/without script | Raw |
| Mar 5 | M10.6.16 | Import/Architecture | Import pipeline assumes 1:1 input-to-output. Splitting alternatives requires breaking this contract across 4+ layers. | N/A — future feature | Raw |
| Mar 4 | M10.6.16 | CoreData/Relationships | Always check linked relationships as fallback before declaring data absent — relationships are authoritative, re-derived data is not. | Import with AI → save → categories show | Raw |
| Mar 4 | M10.6.15 | Import/CategoryPreservation | Only update categories on positive signal, never remove on negative signal (nil re-match). | Edit text → category persists after re-match | Raw |
| Mar 4 | M10.6.15 | Parsing/Normalization | Phase 0c (parenthetical stripping) and 0d (single-char removal) added to normalize(). Order matters. | "shrimp (peeled and deveined)" → "shrimp" | Raw |
| Mar 4 | M10.6.14 | SwiftUI/DataBinding | Re-parsing vs using existing Core Data relationship causes match failures. Fix: nil-coalescing fallback to template relationship. | Saved imported recipe → all ingredients show categories | Raw |
| Mar 4 | M10.6.14 | LLM/PromptDesign | LLM prompt + normalization must both agree on name format. Unit words in names need explicit exclusion in both. | Template names don't contain unit words | Raw |
| Mar 27 | M16.9.3 | ML/Training | Oversampling factor is critical for combined dataset training. 8x (13.3% harness) caused marginal regressions on strangetom (sentence acc 94.96%, MODIFIER F1 0.9173 — both below thresholds). 4x (7.1%) passes all 8 criteria while adding 82 new vocab words. Model trained 26 epochs vs 14 with 8x — better gradient balance. Lesson: when supplementing a large dataset (55K) with a small one (1K), too aggressive oversampling pulls the model away from the dominant distribution. | Run `retrain_full.py --oversample 4` and `--oversample 8`, compare strangetom test metrics | Raw |
| Mar 27 | M16.9.1 | ML/DataConversion | Harness-to-training alignment achieves 98.8% (1,423/1,440). Three key challenges: (1) AI canonicalizes names — fuzzy plural matching needed (including irregular: leaf→leaves). (2) Notes spans can overwrite already-claimed NAME tokens — must skip claimed indices. (3) Container units dropped by AI (cloves, cans) — recover by detecting unclaimed unit tokens after QTY/UNIT. Remaining 1.2% are genuinely ambiguous (slash-alternatives, asterisks, diacritics). | Run `convert_harness_data.py --split --verbose` — check alignment failures < 20 | Raw |
| Mar 3 | M10.6.13 | CoreData/HouseholdKey | `findByCanonicalName` has no householdKey filter. Templates with nil householdKey invisible in household views. Fix: update householdKey on found templates. | Import in household → ALL templates have correct householdKey | Raw |
| Mar 3 | M10.6.13 | SwiftUI/StateRace | SwiftUI batches state changes within a MainActor turn. Fix: explicit callback before state reset. | Import → save → both sheet AND browser dismiss | Raw |
| Mar 3 | M10.6.13 | Swift/Concurrency | `Task { @MainActor in }` is correct fire-and-forget for @MainActor-isolated singletons from non-MainActor code. | Build succeeds with zero concurrency errors | Raw |
| Mar 3 | M10.6.12 | CoreData/ChildContext | Child-context error paths are the #1 source of silent data bugs. Ensure ALL code paths set householdKey, guard nil Household.id. | Import in household → templates have correct householdKey | Raw |
| Mar 3 | M10.6.11 | Architecture/HouseholdScoping | findOrCreate never set householdKey on new templates. Bug hidden in simulator (nil==nil passes filter). | Import on device with household → templates appear in IngredientsView | Raw |
| Mar 3 | M10.6.11 | Testing/SimVsDevice | Simulator without household masks scoping bugs. Always test household features on device with active household. | Compare template count sim vs device after import | Raw |
| Mar 3 | M10.6.10 | SwiftUI/StateObject | Create local service instances from `PersistenceController.shared` when `@EnvironmentObject` unavailable in `init()`. | Autocomplete populates in views | Raw |
| Mar 3 | M10.6.10 | SwiftUI/VisualFeedback | Three-state enum (`.ready`/`.needsCategory`/`.needsTemplate`) with distinct icons gives clear actionable feedback. | Three distinct icon styles appear | Raw |
| Mar 1 | M10.6.5 | LLM/ErrorHandling | `@Published var lastLLMError` is pragmatic for distinguishing "not configured" from "API failed" after nil return. | Toast shows specific error instead of generic | Raw |
