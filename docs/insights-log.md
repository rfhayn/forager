# Insights Log

**Purpose**: Lightweight capture of technical insights discovered during development sessions. Acts as a triage inbox — when 3+ insights cluster around a topic, promote to a Learning Note or ADR.

**Promotion rules**:
- **3+ related insights** → Learning Note (implementation journey)
- **Architectural decision with trade-offs** → ADR
- **Recurring pattern or gotcha** → Add to CLAUDE.md or development-guidelines.md

---

## Insights

| Date | Milestone | Topic | Insight | Verification | Status |
|------|-----------|-------|---------|--------------|--------|
| Feb 11 | M7.6.1 | iOS/LaunchScreen | UILaunchScreen plist dict does NOT resolve asset catalog dark mode appearance variants — must use LaunchScreen.storyboard approach instead | Manual: toggle device appearance, delete app + reboot to clear cache | Raw |
| Feb 11 | M7.6.1 | iOS/LaunchScreen | iOS caches launch screens at the system level, not the app level. Even clean build + reinstall can show stale launch screen. Only reliable fix: delete app + reboot device | Manual: change launch screen asset, rebuild, verify on device | Raw |
| Feb 11 | M7.6.1 | Xcode/IB | Hand-crafted storyboard XML requires `targetRuntime="iOS.CocoaTouch"` (not `"AppleSDK"`). Get correct tools version from `ibtool --version`. Xcode 26.2 uses `24506` | `ibtool --compile` gives detailed errors if wrong | Raw |
| Feb 11 | M7.6.1 | iOS/Vision | `VNGenerateForegroundInstanceMaskRequest` provides production-quality subject extraction for removing image backgrounds programmatically | Visual: compare input/output PNGs for clean edges | Raw |
| Feb 11 | M7.6.2 | Swift/Release | `print()` is NOT compiled out in Release builds — it still writes to stdout. Use `#if DEBUG` guards or `os.Logger` (which respects privacy redaction) for production apps | `xcodebuild -configuration Release build` compiles; Console.app on device to verify no output | Raw |
| Feb 11 | M7.6.2 | Swift/Compilation | `#if DEBUG` is a compile-time flag — unit tests always run in DEBUG configuration, so you cannot write a test asserting "this code is absent in Release". Verification requires Release build + manual inspection | Release build compiles; no automated test possible for conditional compilation | Raw |
| Feb 11 | M7.6.1 | Xcode/pbxproj | `PBXFileSystemSynchronizedRootGroup` (used by Services/) auto-detects new files. Manual `PBXGroup` (used by forager/ and foragerTests/) requires adding PBXFileReference, PBXBuildFile, group children, and build phase entries by hand | Build succeeds after adding; missing entries cause "file not found" errors | Raw |
| Feb 11 | M7.6.2 | SwiftUI/ViewBuilder | `#if DEBUG` / `#else` inside `@ViewBuilder` closures works cleanly as long as both branches return the same type — no type erasure needed. Good pattern for swapping debug vs production UI | Build succeeds in both configurations; visual check in simulator | Raw |
| Feb 11 | M7.6.2 | Xcode/Simulator | You can test Release builds in the simulator: Edit Scheme (Cmd+<) → Run → Info → change Build Configuration to "Release". Remember to switch back to Debug when done | Run in simulator with Release config; verify debug UI is absent | Raw |
| Feb 8 | M7.6 | CoreData/Schema | CloudKit Production schema is append-only once deployed — record types and fields can never be removed or renamed. Clean up before first Production deploy | CloudKit Console: verify Development schema before deploying | → ADR 007 |
| Feb 11 | M7.6.2 | Swift/Preview | `#Preview` macros are stripped from Release builds by the compiler, like `#if DEBUG`. Print statements inside preview blocks don't need explicit gating | Build Release config — preview code absent from binary | Raw |
| Feb 11 | M7.6.2 | Xcode/Scheme | Switching Xcode Run scheme to Release for testing persists in `.xcscheme` file. Always switch back to Debug after, or you'll commit a Release config that disables `#if DEBUG` code and debug symbols | `git diff` the scheme file after testing; restore if changed | Raw |
| Feb 11 | M7.6.2 | Xcode/pbxproj | Dead test files in the app target compile into the production binary — class metadata and string literals survive even if functions are never called. Remove from `PBXSourcesBuildPhase` or move to test target | Build succeeds after removal; `git diff` pbxproj confirms entries removed | Raw |
| Feb 12 | M7.6.4 | CoreData/Migration | Lightweight migration supports entity deletion, toOne→toMany cardinality changes, and hierarchy restructuring out of the box. Only attribute type changes (String→Int), entity hierarchy merges, and data transforms require custom mapping models or staged migrations | `NSMappingModel.inferredMappingModel(forSourceModel:destinationModel:)` returns non-nil if supported | → LN 31 |
| Feb 8 | M8.3 | Parser/Architecture | Confidence-based routing (regex fast path + NLP fallback) avoids NLP overhead for 85%+ of inputs while maintaining accuracy for edge cases | Unit tests: verify regex returns >0.8 confidence for common patterns, NLP activates below threshold | → ADR 010 |
| Feb 12 | M7.6.4 | CoreData/Versioning | Each `.xcdatamodel` inside `.xcdatamodeld` is a full schema snapshot. The `.xccurrentversion` plist tells Core Data which is active. Old model versions must remain in the bundle — Core Data needs them as the source to compute lightweight migrations | Add new model version, verify `NSMappingModel.inferredMappingModel` returns non-nil for old→new | → LN 31 |
| Feb 12 | M7.6.4 | Xcode/CoreData | Editing `.xccurrentversion` outside Xcode while the project is open can get reverted — Xcode overwrites it from its in-memory project state on build. Either close Xcode first or re-set the file after build. The committed state is what matters for production | Edit file, run build, re-read file to check if reverted | → LN 31 |
| Feb 12 | M7.6.5 | CoreData/Codegen | With Manual/None codegen, `@NSManaged` properties that don't exist in the schema compile fine but return `nil` at runtime. Core Data uses KVC under the hood — accessing an undefined key on NSManagedObject returns nil instead of crashing. This makes code-schema mismatches silent and hard to catch | Declare a property not in schema, set a value, save, re-fetch — value is nil | → LN 31 |
| Feb 12 | M7.6.5 | CloudKit/Schema | CloudKit cannot rename fields. To "rename" an attribute, add a new field, migrate data in code on first launch, and leave the old field as an orphan. Both fields coexist permanently in the CloudKit schema | CloudKit Console: verify both old and new fields appear after Production deploy | → LN 31 |
| Feb 12 | M7.6.6 | CoreData/Migration | When data is stored as a hack (e.g., tags encoded in `sourceURL` with a `tags:` prefix), migrating to a proper field is simpler than a CloudKit field rename — add the new attribute, write an idempotent one-time migration predicated on `oldField BEGINSWITH X AND newField == nil`, and clear the old value. No CloudKit orphan created because the data was never in a "real" schema field | Build succeeds; create recipe with tags, relaunch, verify tags migrated and sourceURL cleared | → LN 31 |
| Feb 12 | M7.6.3 | Xcode/CoreData | Xcode silently rewrites `.xccurrentversion` from its in-memory project state — even `git checkout --` is ineffective while Xcode is open. This caused a crash when it reverted v5→v2, losing the `tags` attribute. Fix: close Xcode before restoring, or use a direct file write tool. Committed state is correct but the build uses whatever Xcode last wrote to disk | Close Xcode, edit file, reopen, verify model inspector shows correct version | → LN 31 |
| Feb 12 | M7.6.3 | Xcode/pbxproj | `PBXFileSystemSynchronizedRootGroup` (Services/) auto-detects new files on disk. Manual `PBXGroup` (forager/) requires 4 project file entries: PBXFileReference, PBXBuildFile, group children, PBXSourcesBuildPhase. Missing any one causes "cannot find X in scope" at build time | Create file in each dir, build — Services/ compiles, forager/ fails until pbxproj updated | Raw |
| Feb 16 | M15 | Design/Tooling | The `frontend-design` Claude Code plugin provides structured design critique that catches visual consistency issues the author misses: font size proliferation (13 sizes → 8), insufficient card shadow weight against warm backgrounds, lack of visual identity on recipe cards (added category header bands), and button size mismatches between primary/secondary. Running it against HTML mockups before implementing in Swift saves significant rework — the feedback loop is minutes vs hours of refactoring compiled UI code | Create HTML mockup → run `frontend-design` review → iterate → then implement in Swift | → LN 33 |
| Feb 16 | M15 | Design/Typography | Serif fonts in utility apps create a "two apps glued together" feeling when only applied to one content type (recipes). In editorial-first apps (NYT Cooking, Bon Appetit) where the entire experience is serif, it works. In a utility-first app where most screens are lists/checkboxes, visual hierarchy should come from size and weight contrast within a single font family, not from switching families | Visual: compare mockup with and without serif on recipe screens — unified font reads as one app | → LN 33 |
| Feb 16 | M15 | Design/Shadows | On warm cream backgrounds (#FDFBF7), shadow level 1 (`0 1px 3px at 8%`) is nearly invisible — cards blend into the background. Shadow level 2 (`0 2px 8px at 12%`) is the minimum for tappable cards to read as interactive surfaces. Shadow level 1 should be reserved for non-interactive elements like type-sample cards or static info panels | Toggle between shadow-1 and shadow-2 on cards against cream background | → LN 33 |
| Feb 16 | M15 | Design/Typography | SF Mono for quantities in a grocery list creates visual dissonance — the monospaced glyphs look like they belong to a different app. `font-variant-numeric: tabular-nums` on the system font (SF Pro) gives the same column-alignment benefit without any visible font switch. In SwiftUI: `.monospacedDigit()` modifier. Reserve true monospace for code/hex values only | Side-by-side: SF Mono quantities vs system font with tabular-nums — alignment identical, visual consistency improved | → LN 33 |
| Feb 12 | M7.6.3 | SwiftUI/Lifecycle | `PersistenceController.shared` initializes during `foragerApp.init()` via stored property access. `loadPersistentStores()` is synchronous and blocks until SQLite files open + CloudKit mirroring sets up — SwiftUI cannot render any frame until init completes. Fix: defer store loading to `prepare()` called from splash `.onAppear` | Delete app, rebuild — splash with spinner appears before stores finish loading | Raw |
| Feb 12 | M7.6.3 | CoreData/Init | `NSPersistentCloudKitContainer(name:)` + `persistentStoreDescriptions` assignment is fast (model parsing + config). The slow call is `loadPersistentStores()` which opens SQLite and registers CloudKit mirroring. Splitting into two phases lets SwiftUI render between them | Time each phase with `CFAbsoluteTimeGetCurrent()` — container creation <0.01s, store loading 0.5-2s | Raw |
| Feb 12 | M7.6.3 | SwiftUI/Observation | A `let` stored property on an `App` struct does NOT trigger SwiftUI re-renders when its `@Published` properties change — only `@StateObject`/`@ObservedObject` do. Bridge with `@State` + `.onReceive(publisher.$property)` to avoid disrupting singleton ownership patterns | Change `@Published` value on singleton — verify UI updates only with onReceive bridge | Raw |
| Feb 12 | M7.6.7 | CloudKit/Environment | `NSPersistentStoreCloudKitEnvironment` defaults to Production when not explicitly set. Debug builds set it to "Development"; Release builds omit it, pushing schema to Production. To verify schema in the Development Console before deploying, must use a Debug build on a physical device | Run Debug on device → check Development Console; Run Release on device → schema goes to Production | → LN 34 |
| Feb 12 | M7.6.7 | CloudKit/Schema | `NSPersistentCloudKitContainer` does NOT automatically push schema for all entities — it pushes lazily when records are created. Entities with no instances (e.g., Household on a fresh device) won't appear in CloudKit Console. Must call `initializeCloudKitSchema(options: [])` once during development to force a full schema push, then remove it | Add temporary call, run on device, verify all record types appear in Console | → LN 34 |

| Feb 15 | M7.6.8 | CloudKit/Schema | `cloudkit.share` record type is only created in the schema when `container.share()` is first called — it doesn't exist by default. If you deploy schema to Production before ever creating a share in Development, Production will be missing this type entirely. `NSPersistentCloudKitContainer`'s mirroring delegate fails to initialize with "Cannot create new type cloudkit.share in production schema". Fix: create a share in Development, then re-deploy to Production | Create household in Dev → check `cloudkit.share` exists in Dashboard → Deploy to Production | → LN 34 |
| Feb 15 | M7.6.8 | CloudKit/Timing | `container.share()` can fail with `partialFailure` / "Failed to modify some records" on fresh installs because `NSPersistentCloudKitContainer` exports records asynchronously after `context.save()`. The record may not exist on the CloudKit server yet when share() runs. Retry with exponential backoff (2s, 4s) resolves this | Delete app → reinstall → create household immediately → observe if first share attempt fails | → LN 34 |
| Feb 15 | M7.6.8 | SwiftUI/ZStack | Views with `.ultraThinMaterial` (or any non-clear background) in a ZStack intercept all touch events, even if the underlying layer has `.onTapGesture`. Hit testing follows ZStack render order (last = top). Fix: add tap gesture directly on the material-backed view | Place a tap target behind a material view → tap is absorbed by material | Raw |
| Feb 15 | M7.6.8 | CloudKit/Sharing | `container.share([household])` only migrates the root record (Household) to the shared store — child records (HouseholdMember) stay in the owner's private store, invisible to other participants. Store essential cross-device data (like owner display name) directly on the shared root record | Create household, share, check shared store on joinee — only Household entity present, no HouseholdMember | → LN 34 |
| Feb 17 | M15.7 | SwiftUI/Accessibility | `@Environment(\.accessibilityReduceMotion)` must be declared on the specific struct that uses it — not a parent struct in the same file. When RecipeListView.swift contains both `RecipeListView` and `RecipeDetailView` as separate structs, adding `reduceMotion` only to `RecipeListView` causes "cannot find in scope" when `RecipeDetailView` references it. Each struct is a separate scope | Add `reduceMotion` to parent-only → child struct fails to compile; add to child → builds | → LN 35 |
| Feb 17 | M15.7 | SwiftUI/Accessibility | `@ScaledMetric(relativeTo:)` is a zero-effort way to make fixed-size UI elements respect Dynamic Type. Changing `var size: CGFloat = 56` to `@ScaledMetric private var size: CGFloat = 56` makes the element grow/shrink with the user's text size preference. The `relativeTo:` parameter ties scaling to a specific text style so the element scales proportionally | Toggle Dynamic Type in Settings → verify ring/circle sizes change proportionally | → LN 35 |
| Feb 17 | M15.7 | SwiftUI/Animation | The pattern `withAnimation(reduceMotion ? nil : .spring(...))` is the correct way to guard animations: passing `nil` as the animation parameter makes the state change instant (no animation). For state-change animations where some transition is needed for UX clarity, use `.easeInOut(duration: 0.15)` as the reduced-motion alternative — fast enough to not trigger motion sensitivity but visible enough to show state changed | Toggle Reduce Motion in Settings → verify decorative animations gone, state changes still perceptible | → LN 35 |
| Feb 17 | M15.7 | iOS/ContentUnavailableView | `ContentUnavailableView` (iOS 17+) is a direct replacement for custom empty state views. It provides `.search(text:)` for search-specific empty states and a general `Label + description + actions` pattern for other contexts. Adopting it eliminates custom view maintenance and ensures visual consistency with system conventions | Replace custom empty state → verify in simulator that layout matches system apps | Raw |
| Feb 15 | M7.6.8 | CloudKit/Schema | Repurposing a deprecated Core Data field (ownerEmail → ownerDisplayName) avoids a CloudKit schema change entirely. Since CloudKit cannot delete fields, unused fields are free real estate for new purposes — just add a computed alias and update all call sites | Read `ownerDisplayName` on joinee device after sync — value matches what owner set during creation | → LN 34 |
| Feb 15 | M7.6.8 | iOS/CKShare | iOS 16+ strips `nameComponents` from CKShare for the current user (owner). `share.currentUserParticipant?.userIdentity.nameComponents` returns nil on the owner's device. Other participants' names may also be nil unless they've granted discoverability. Never rely on CKShare.participants for display names | Log `share.participants` on owner device — own entry has nil nameComponents | → LN 34 |

| Feb 16 | M15 | Design/ColorSemantic | Using the same color palette for two different semantic meanings (category colors on grocery chip pills AND recipe card header bands) creates user confusion. Colors should have one meaning per context. Recipes don't have categories in the data model — the band color was derived from dominant ingredient category, which is ambiguous. Removed recipe bands entirely; category colors are now exclusive to grocery list context | Compare recipe list before/after — clean cards without bands avoid the "what does green mean here?" question | → LN 33 |
| Feb 16 | M15 | Design/InlineEditing | When a display format will transition to inline editing, the display layout should match the editing layout to avoid layout shift. Ingredient rows display as `• qty unit name` (one line) rather than a two-column table because the editing UX uses a single inline text field parsed by `IngredientParsingService`. Column layouts would visually break on edit-tap | Tap ingredient row to edit — verify no layout shift between display and edit modes | → LN 33 |
| Feb 16 | M15 | Design/StepNumbers | Large decorative step numbers (28pt) in recipe instructions invert the visual hierarchy — the number dominates while the instruction text (the actual content) recedes. Reducing to 17pt inline with the same baseline as the text makes numbers serve as navigation anchors without competing with content. This matches how printed cookbooks handle step numbers | Compare 28pt vs 17pt step numbers — text readability improves at 17pt, scanning remains easy | → LN 33 |
| Feb 16 | M15 | Design/DayDotInitials | Day-of-week dots without labels require the user to count from a known start day (Monday? Sunday?) to identify which dot is which. Adding single-letter initials (M/T/W/T/F/S/S) inside 22pt circles makes them self-documenting — no legend, no counting, no ambiguity. The slight increase in dot size (10pt→22pt) is justified by the cognitive load reduction | Compare unlabeled 10pt dots vs lettered 22pt dots — users identify specific days instantly with letters | → LN 33 |
| Feb 16 | M15 | Design/ButtonNaming | When two buttons perform related but distinct operations (create-new vs append-to-existing), using different verbs ("Generate" vs "Add to") while keeping the same noun ("Grocery List") communicates the distinction without requiring explanation. Using the same verb for both would hide the behavioral difference; using different nouns ("Shopping List" vs "Grocery List") creates terminology confusion | Compare "Generate Grocery List" and "Add to Grocery List" — users understand one creates, the other appends | → LN 33 |
| Feb 16 | M15 | Design/QuickSelectPills | Meal planning apps that require creating a "recipe" for non-cooking days (takeout, leftovers) add friction that discourages daily planning. Inline quick-select pills that mark a day as "planned" without a recipe entry cover ~90% of non-cooking scenarios (Takeout, Dining Out, Leftovers, No Meal) and keep the weekly completion dots accurate. The distinction between "unplanned" and "deliberately skipped" matters for grocery list generation | Tap "Takeout" pill on unplanned day — dot fills, day no longer contributes ingredients to grocery list generation | → LN 33 |
| Feb 21 | M8.4 | Process/ReviewConvergence | Five review passes showed monotonically decreasing severity (5 high → 2 → 1 → 2 → 0 high). When high-severity findings drop to zero and remaining items are typing/consistency level, the document has converged. Each pass found genuinely new things with zero overlap — validating multi-pass over single-pass review | Track high-severity count per pass — convergence = zero highs with only low/medium remaining | Raw |
| Feb 21 | M8.4 | Process/InternalVsExternal | External PRD review (Codex) checks internal logic and consistency. Internal code-audit review reads source files and verifies claims against ground truth. Pass 4 (internal) found things no PRD-reading would catch: 5 double-parse call sites (not 1), 11 unlogged parse operations, 204 tests (not 155). These are complementary, not redundant | Compare external findings (architectural, contract) vs internal findings (code cross-reference, stale counts) — different categories entirely | Raw |
| Feb 21 | M8.4 | Architecture/SemanticTyping | Semantic type distinctions (e.g., `ParsingSource` for parse-context vs `CorrectionSource` for edit-flow) only surface after use cases are fully specified. The distinction was invisible in passes 1-4 and only became clear in pass 5 when correction flows were concrete. This argues for iterative review of type design, not upfront design | Compare type definition at pass 2 (`ParsingSource?` reused) vs pass 5 (dedicated `CorrectionSource` enum) — the need for separation wasn't visible until edit flows were enumerated | Raw |
| Feb 21 | M8.4 | Telemetry/ProvenanceRules | When telemetry attribution depends on runtime state (is `parserUsed` still in memory?), define provenance rules per call site rather than assuming uniform availability. A 4-row table mapping edit flows to `parserUsed` availability made an underspecified metric precise and testable | Review provenance table — each flow explicitly states yes/no for parserUsed availability with rationale | Raw |
| Feb 21 | M8.4 | Process/IterativeReview | A second review pass on an updated PRD catches different issues than the first — pass 1 finds architectural gaps, pass 2 finds contract and migration gaps introduced by the fixes. The reviewer's fresh read of updated text catches inconsistencies the editor missed | Compare pass 1 findings (CRF parity, model I/O) vs pass 2 findings (non-existent fields, schema evolution) — no overlap | Raw |
| Feb 21 | M8.4 | Telemetry/SchemaMigration | Optional Codable fields (`String?`) give free JSON schema migration — `JSONDecoder` silently defaults missing optional fields to `nil`. No migration code, no version gating on load. This is a major advantage of JSON-based telemetry over Core Data schema evolution where every field change requires a migration step | Encode v3 event (with `parserUsed`), decode v2 event (without it) — both succeed, missing field is nil | Raw |
| Feb 21 | M8.4 | Architecture/GateRelaxation | When a quality gate depends on infrastructure that doesn't exist yet (e.g., "50 linked corrections" requiring a non-existent `parseEventId` field), relax the gate to match what's achievable rather than expanding scope to build the infrastructure. The gate's intent (minimum data volume) is preserved without a hidden prerequisite chain | Compare "50 linked corrections" (requires Core Data v7 migration) vs "50 corrections" (works today) — same retraining quality bar | Raw |
| Feb 21 | M8.4 | Telemetry/Attribution | Winner-only attribution (`parserUsed` = the parser that produced the returned result) is cleaner than router-level attribution (`"hybrid"`) for analytics. Per-parser accuracy measurement becomes a simple group-by query. A `"hybrid"` label would require additional metadata about which parsers were consulted vs which won — complexity with no current use case | Query corrections grouped by `parserUsed` — each group measures one parser's accuracy directly | Raw |
| Feb 21 | M9.5 | Swift/DI | Default parameters (`parser: IngredientParser = HybridIngredientParser()`) provide zero-blast-radius DI in Swift. All 11 existing call sites compile unchanged while tests can inject mocks. This avoids DI containers, Environment keys, or ServiceFactory patterns — the right tool for scoped DI where only a few constructors need injection | Grep for `IngredientParsingService(context:` — all existing call sites compile with no changes | Raw |
| Feb 21 | M9.5 | Swift/DI | Converting `private static let` properties to instance-level `let` properties assigned in `init` makes constants injectable while preserving immutability. The static → instance transition has zero performance cost (no synchronization needed) and enables test-time configuration (e.g., `regexConfidenceThreshold: 0.9`) without touching production defaults | Create HybridIngredientParser with custom threshold → verify routing changes | Raw |
| Feb 21 | M9.5 | Testing/Mock | A mock parser that tracks `parseCalls: [String]` enables negative assertions ("NLP should NOT be called when regex is confident") which are impossible with real implementations. Call-counting mocks catch routing bugs where both parsers fire when they shouldn't — saving NLP overhead on 85%+ of inputs | Run testHighConfidenceRegexSkipsNLP — verify `mockNLP.parseCalls.count == 0` | Raw |
| Feb 21 | M8.4 | ML/CRFTransitions | CRF transition matrix reveals learned grammar: QTY→UNIT has highest weight (1.28), NAME→NAME self-transition (1.32) captures multi-word ingredients, PREP→PREP (1.63) and COMMENT→COMMENT (1.69) model multi-word phrases. Start transitions favor QTY (0.59) reflecting that most ingredients begin with quantities | Inspect `transitions.json` — verify high-weight transitions match ingredient grammar intuition | Raw |
| Feb 21 | M8.4 | ML/ClassImbalance | MODIFIER label has lowest F1 (0.9261) due to severe class imbalance — only 1.1% of tokens vs NAME at 30.4%. The model still exceeds the 0.90 target but this is the weakest label. Future improvements: targeted augmentation or upsampling for MODIFIER examples | Check `dataset_statistics.json` label distribution — MODIFIER is smallest class | Raw |
| Feb 21 | M8.4 | ML/MPSTraining | Apple Silicon MPS (Metal Performance Shaders) is viable for small-to-medium model training — 1.35M parameter BiLSTM-CRF trained in ~39 min on MPS vs estimated 2+ hours on CPU. MPS device selection is automatic via `torch.backends.mps.is_available()` | Run `train_model.py` — observe MPS device selected and ~90s/epoch | Raw |
| Feb 21 | M8.4 | ML/EarlyStopping | Early stopping with patience=5 found optimal model at epoch 21 while training ran to epoch 26. The gap between train loss (0.17) and val loss (0.20) at stopping suggests mild overfitting — dropout 0.5 effectively controlled it. Patience=5 is a good default for this model size | Plot train vs val loss curves — divergence begins around epoch 13, best val at epoch 21 | Raw |
| Feb 21 | M8.4 | Python/StdoutBuffering | Python buffers stdout by default when not connected to a terminal (e.g., background execution). Training output was invisible until the process completed. Fix: `python -u` flag disables buffering, or `PYTHONUNBUFFERED=1` env var. Always use `-u` when running long training jobs in background | Run `python train.py` in background → no output; run `python -u train.py` → output streams live | Raw |
| Feb 21 | M8.4 | Python/EmDashEncoding | Python source files use ASCII `--` (double hyphen) but Markdown files often contain Unicode em dash `—` (U+2014). String replacement in `update_model_card()` silently failed because the search key didn't match. Fix: use `\u2014` escape in Python code to match the actual file content | Write replacement with `--` → replacement fails silently; use `\u2014` → replacement succeeds | Raw |
| Feb 16 | M15 | Design/ActionButtonProminence | Text-only action links (Done / Swap / Remove) inside cards disappear visually — they read as metadata, not interactive controls. Pill-shaped outlined buttons with icons create a distinct "action row" that users scan separately from card content. Equal-width buttons (`flex: 1`) reinforce that all three are peer-level actions | Compare text links vs pill buttons — users tap pill buttons faster due to clear affordance | → LN 33 |
| Feb 16 | M15 | Design/DestructiveConfirmation | A generic "Are you sure?" dialog is less useful than one that tells the user *why* removal matters. If ingredients from a meal's recipe are already on a grocery list, the confirmation says so — the user can decide whether to also clean up the list. "Informative confirmation" pattern vs the "speed bump" anti-pattern | Show alert with ingredient count ("3 ingredients from this recipe are on your grocery list") — user makes informed decision vs blind confirmation | → LN 33 |
| Feb 16 | M15 | Design/NavBarCentering | iOS nav bar titles must be centered relative to the full screen width, not the space remaining after back/trailing buttons. CSS approach: title with `position: absolute; left: 0; right: 0; text-align: center; pointer-events: none` floats in a center layer. Back and trailing buttons position independently at edges. This mirrors how `UINavigationBar` works internally — the `titleView` is centered in the full bar, `leftBarButtonItems`/`rightBarButtonItems` float at edges | Toggle dark mode — title stays centered regardless of back button text length ("‹ Lists" vs "‹ Settings") | → LN 33 |
| Feb 16 | M15 | Design/RecipeDetailNoNavTitle | Recipe detail screens benefit from omitting the nav bar title entirely when the hero section immediately below contains the same text. Showing "Honey Garlic Salmon" in the nav bar AND in the hero header is redundant — it wastes vertical space and creates a "stuttering" effect. The nav bar shows only "‹ Recipes" + Edit/••• actions; the recipe name appears once in the 28pt hero | Compare with/without nav title — single hero title reads cleaner and reclaims ~44pt of vertical space | → LN 33 |
| Feb 21 | M8.4 | ML/DocumentVsToken | Foundation Models and BiLSTM-CRF solve fundamentally different problems. Foundation Models excels at document-level understanding (which lines are ingredients vs. instructions). BiLSTM-CRF excels at token-level extraction (what is the quantity, unit, name within one ingredient line). The ideal recipe import pipeline uses Foundation Models for section detection and BiLSTM-CRF for ingredient parsing — complementary, not competing | Compare: LLM on "2 cups flour" (slow, overkill) vs BiLSTM-CRF on full recipe text (no section detection). Each tool where strongest | Raw |
| Feb 21 | M8.4 | iOS/ShareExtension | iOS Share Extensions are limited to ~120MB memory. Foundation Models loads a ~1.2GB model. This means Foundation Models CANNOT run in a Share Extension — recipe import via Share Sheet must extract URL/metadata only, with all AI processing deferred to the main app. This constraint reinforces minimal share extension architecture | Attempt FoundationModels import in share extension → memory crash | Raw |
| Feb 21 | M8.4 | Architecture/ConvergencePoint | All three recipe import paths (URL, text paste, photo) converge at the same point: an array of ingredient strings flowing through `parseAndConnectIngredients()`. Improving this shared bottleneck (via M8.4's ML parser) benefits all import modes simultaneously with zero import-specific code | Trace URL import → JSON-LD extract → recipeIngredient[] → same function as manual recipe creation | Raw |
| Feb 21 | M8.4 | Parsing/MealieWarning | Mealie's open-source recipe manager documents the #1 parser UX pitfall: re-parsing destroys user edits. When users modify parsed ingredients, clicking "Parse" again overwrites corrections with fresh parse results. Forager's correction instrumentation (M8.4 Phase 7) avoids this — corrections are logged but never override source text | Review Mealie issue #5336 — data loss on re-parse. Forager logs corrections separately from parse results | Raw |
| Feb 21 | M8.4 | Architecture/PitfallOwnership | Implementation pitfalls in a multi-layer pipeline split cleanly by *where in the pipeline* they occur. Parser-layer pitfalls (tokenizer parity, CRF export, unit canonicalization, confidence calibration) belong to the parser milestone. Pre-parser pitfalls (schema.org group header stripping, double-parse avoidance, OCR error correction) belong to whichever feature feeds text into the parser. Categorizing by pipeline layer prevents scope creep in both directions | Map each pitfall to its pipeline layer — parser pitfalls are already in M8.4 PRD phases; pre-parser pitfalls are notes for future import PRD | Raw |
| Feb 16 | M15 | Design/EmptyStateHierarchy | Empty states have a visual hierarchy: large icon → title → subtitle → CTA. The icon size and container create the "emotional hook" (what this space could become), the title names the state, the subtitle gives context, and the CTA provides escape. Positioning at ~40% from top (not centered) avoids the "lost in space" feeling — content occupies the upper-third reading zone | Compare vertically-centered vs 40%-from-top placement — upper placement feels purposeful, centered feels like a loading spinner | → LN 33 |
| Feb 16 | M15 | Design/SearchHighlightPattern | Highlighting search matches in `--accent-primary` with `<strong>` tags inside result text creates a visual "connect the dots" effect — the user's eye jumps between highlighted fragments across result rows, confirming each result's relevance at a glance. This works because the accent color is already associated with "active/selected" throughout the app | Search "garlic" — highlighted matches in recipe names, ingredient names, and grocery items all use the same accent treatment | → LN 33 |
| Feb 16 | M15 | Design/SyncFeedbackLevels | Three-tier sync feedback (progress → error → unavailable) maps to three UX severities: informational (blue, auto-dismiss), warning (amber, user-actionable retry), and blocking (full-screen empty state, system-level fix). The key insight is that sync-in-progress and sync-error are non-blocking — the user can always interact with locally-saved data. Only "no iCloud" replaces the full content area | Toggle airplane mode during sync — warning banner appears, content remains interactive. Disable iCloud account — empty state replaces all content | → LN 33 |
| Feb 16 | M15 | Design/SwipeMockupTranslateX | Static mockups can show mid-swipe states using CSS `transform: translateX(±80px)` on the content layer over an absolutely-positioned background layer. The `overflow: hidden` on the container clips cleanly. This communicates the swipe interaction without requiring animation, and is immediately recognizable to iOS users from muscle memory | Visual: swipe mockups show revealed action areas at consistent 80px offset — matches iOS default leading/trailing swipe distance | → LN 33 |
| Feb 16 | M15 | Design/EditModeFocusIndicator | In inline edit mode, the focused field should be visually distinct from unfocused fields. Using `--accent-primary` for the active field's bottom border (instead of `--border-default`) plus a pipe cursor character creates a clear "I'm editing this one" signal without disrupting the overall form layout. The 2px border on household name editing (vs 1px on recipe fields) signals "primary editable content" vs "row-level fields" | Tap between ingredient fields in recipe edit — border color changes on focus, cursor appears at insertion point | → LN 33 |
| Feb 16 | M15 | iOS/NavBarAddButton | The trailing nav bar `+` is the only correct iOS pattern for "create new item" on list screens. FABs (floating action buttons) are Android-native and fight with iOS tab bars and `.safeAreaInset` bottom bars. Inline dashed "add" cards scroll off-screen on long lists and conflate "card" with "action". Apple Notes, Reminders, Calendar, and Files all use trailing `+` | Check Apple's first-party apps — all use trailing nav bar `+` for creation on list screens | → LN 33 |
| Feb 16 | M15 | CSS/SemanticTokensDarkMode | Using `var(--btn-primary-text)` instead of hardcoded `#FFFFFF` on accent backgrounds eliminates the need for separate `[data-theme="dark"]` overrides. Before: each element had two rules (base `#FFFFFF` + dark override `#1C1A14`). After: one rule using the semantic variable that flips automatically. Removed 4 redundant dark mode overrides. Every manual dark mode override is a maintenance liability; every semantic variable is one fewer thing that can go stale | Toggle dark mode theme — all accent-on-white elements maintain contrast without individual overrides | → LN 32 |
| Feb 16 | M15 | Design/TapTargetSeparation | CSS mockups should reflect *visual* sizes (28px `+` button, 24px pills) while the SwiftUI implementation handles *hit areas* (44pt minimum via `.contentShape(Rectangle())` or `.frame(minHeight: 44)`). Making elements visually 44pt would look clunky — the standard iOS practice is small visual elements with large invisible hit targets. Document this split in the PRD to prevent implementers from inflating visual sizes | Compare 28pt visual `+` with 44pt invisible hit area — tappability improves without visual bloat | → LN 33 |
| Feb 16 | M15 | Design/PRDSyncAudit | After a batch of mockup changes, grepping the PRD for stale terminology (old button names, removed CSS patterns, outdated layout descriptions) catches drift invisible when reading sequentially. A single session's changes can touch 6+ PRD sections — any one being stale during implementation causes confusion about which mockup is authoritative | After mockup changes: `grep -i "Shopping List\|Add All\|✕.*Remove\|icon-only"` the PRD — stale hits = drift | → LN 33 |
| Feb 16 | M15 | Design/TextTertiaryContrast | `--text-tertiary` at `#7A7067` on `#FDFBF7` canvas is ~4.1:1 contrast — passes WCAG AA for large text (18px+) but fails for normal text. At 12px (category headers, captions), this is a real accessibility failure. Darkening to `#6A6057` bumps to ~5.8:1, passing AA at all sizes. The visual difference is subtle but the accessibility impact is significant | Use WCAG contrast checker: `#6A6057` on `#FDFBF7` = 5.8:1 vs `#7A7067` = 4.1:1 | → LN 33 |

| Feb 16 | M15 | CSS/TypeScaleVariables | Defining `--font-*` CSS variables as the canonical type scale without refactoring existing rules creates a contract for implementation. Individual CSS rules still use raw `px` values, but the variable definitions are the single source of truth. During M15.2 (SwiftUI migration), developers reference the variables — not grep for `font-size` values scattered across 100+ rules. Define the scale early, migrate incrementally | Check `:root` for 8 `--font-*` variables matching the PRD 4.2.2 table exactly | → LN 32 |
| Feb 16 | M15 | CSS/OrphanedVariables | CSS custom property definitions have zero runtime cost but real maintenance cost — every unused variable is a false signal to developers scanning `:root` for the design system's actual tokens. `--r-xl: 20px` was defined but never referenced by any `var(--r-xl)` — removing it tightens the contract. Conversely, `--info-fg`/`--info-bg` are currently unused but will be needed for Phase 3 error states — keeping them is correct because their future use is planned and imminent | `grep 'var(--r-xl)' *.html` returns 0 results → safe to remove; `grep 'var(--info-' *.html` returns swatch reference → keep for Phase 3 | → LN 32 |
| Feb 16 | M15 | CSS/ColorMixFunction | `color-mix(in srgb, var(--cat-produce) 12%, transparent)` derives category chip backgrounds from their text color variable — one source of truth per category. Previous approach used hardcoded `rgba()` values that duplicated and could drift from the `--cat-*` variables. `color-mix()` has 96%+ browser support (2024+) and is the modern replacement for opacity hacks | Toggle dark mode — chip backgrounds automatically derive from dark-mode category colors without any override | → LN 32 |
| Feb 16 | M15 | Design/ProgressBarDarkMode | Translucent backgrounds (`rgba()`) that look correct in light mode can become invisible in dark mode. The progress bar track used `rgba(176,168,158, 0.3)` — fine on `#FDFBF7` canvas, but nearly invisible on `#1C1A14` dark canvas. Using `var(--bg-tertiary)` ensures the track is always distinguishable from the canvas because semantic tokens already have tested dark-mode values | Toggle dark mode — progress bar track is visible against both light and dark canvas | → LN 32 |
| Feb 16 | M15 | PRD/TabArchitectureReduction | When restructuring navigation (6 tabs → 5), document the "what moved where" mapping explicitly — not just the new structure. Developers familiar with the old layout need a migration path: "Ingredients → Settings > Categories" prevents them from searching for a deleted tab. The before/after table format in §6 makes the mapping scannable | Check PRD §6 "Tab Architecture Change" table — every old tab has a destination in the new structure | → ADR 011 |
| Feb 16 | M15 | PRD/CrossCuttingPatterns | UI patterns that appear across multiple screens (celebration, dialogs, edit modes, loading/error, swipe actions) belong in the shared components section (§5), not duplicated in each screen spec. Screen-specific sections (§6) reference the shared patterns. This prevents drift when a pattern changes — update one place, all screens inherit the change | Check that §6 screen specs reference §5 patterns by section number (e.g., "see §5.9 Celebration State") rather than inlining pattern details | → LN 33 |
| Feb 16 | M15 | Design/PillRadiusHierarchy | Three-tier border-radius creates a visual hierarchy without adding color or size differences: capsule (999px) for primary navigation controls (filter pills), rounded-rect (`--r-sm: 8px`) for content-level interactive pills (timing, scale), and compact (`--r-xs: 4px`) for informational badges (category chips). Users instinctively recognize capsule shapes as "filters" and rectangles as "content" — matching iOS system conventions | Compare filter pills (capsule) vs scale pills (rounded-rect) vs cat chips (compact) — visual hierarchy is clear without labels | → LN 33 |
| Feb 16 | M15 | Design/ButtonPaddingVsFontSize | Consistent padding (14px) across all full-width buttons ensures equal tap target height regardless of button style. Font-size variation (17px primary/secondary vs 15px outline/danger) creates intentional visual hierarchy — primary actions are louder, outline actions are quieter. This separates two concerns: ergonomics (padding = touch target) from hierarchy (font-size = visual weight) | Measure rendered height of `.btn-primary` vs `.btn-outline` — padding-based height is identical; visual weight differs | → LN 33 |

| Feb 16 | M15 | CSS/SpecificityBorderTrap | When a class uses `border` shorthand (`.ingredient-lib-card { border: 1px solid ... }`), it resets ALL four borders — including `border-left` set by an earlier class (`.cat-strip { border-left: 4px solid ... }`). Same specificity + later source order = shorthand wins. Compound selectors (`.ingredient-lib-card.cat-strip`) fix this by bumping to 2-class specificity. This explains why dairy (`.cat-strip.dairy` = 2 classes) showed its color but produce (`.cat-strip` = 1 class) didn't | Inspect element in browser — produce items get `border-left: 4px solid var(--cat-produce)` with compound selector override | → LN 32 |
| Feb 16 | M15 | Design/MockupFrameHeight | Design system mockups should use fixed `height` (not `min-height`) on phone frames so all screens appear identical. `min-height: 852px` causes detail screens with more content to grow taller than overview screens, making side-by-side comparisons misleading. Fixed `height: 852px` with `overflow-y: auto` on the content area lets users scroll within individual frames — matching real phone behavior | Compare overview vs detail screens — both are 852px tall, detail screen scrolls internally | → LN 33 |
| Feb 16 | M15 | CSS/HardcodedColorSweep | After a batch of mockup work, grep for literal hex values (`#FFFFFF`, `#000`, `white`, `black`) outside of `:root` variable definitions. Each hit is a dark mode liability — it won't flip when the theme changes. Replacing with semantic variables (`var(--surface-primary)`, `var(--btn-primary-text)`) eliminates redundant `[data-theme="dark"]` overrides. In this review: 5 inline `#FFFFFF` values in swipe demos, day-chip dot, and toggle thumb were all replaced, removing 1 manual dark mode override in the process | `grep '#FFFFFF' mockup.html` — only `:root` definitions should remain; zero hits outside variable declarations | → LN 32 |
| Feb 17 | M15 | Design/ParseConfidenceFeedback | Real-time parse confidence indicators on ingredient entry (green ✓ for high confidence, amber ● for low) give users immediate feedback about whether the parser understood their input — without requiring them to inspect parsed fields. The indicator auto-dismisses after 1.5s to avoid visual clutter. This maps directly to `IngredientParsingService`'s confidence score: ≥0.8 = green, <0.8 = amber | Type "2 cups flour" → green checkmark appears trailing; type "a handful of herbs" → amber dot appears | → LN 33 |
| Feb 17 | M15 | Design/OnboardingPagePattern | Onboarding screens use a consistent template (icon circle → title → subtitle → page dots → CTA) that varies only the content per screen. This template maps to a SwiftUI `TabView(.page)` where each page is identical structurally — only the icon, text, and CTA label change. The "Skip" button on every screen respects users who prefer to explore by doing. Screen 4 ("Get Started") replaces "Next" with a primary action that dismisses onboarding and sets `UserDefaults.hasCompletedOnboarding` | Swipe through 4 screens — layout is identical, only content changes; tap "Skip" on screen 1 — goes directly to main app | → LN 33 |
| Feb 17 | M15 | Design/InlineValidation | Inline validation (red border + caption below field) is less intrusive than modal alerts for form errors. The error state is visually anchored to the specific field that needs attention — the user's eye doesn't need to leave the form to read an alert and then find the problematic field. This pattern matches iOS system apps (Contacts, Settings) where validation is always inline | Tap Save with empty title → red border appears on title field, "Recipe name is required" caption below, no modal | → LN 33 |
| Feb 17 | M15 | PRD/FileToMockupMapping | Adding a "Swift File → Mockup Screen" mapping table to the PRD eliminates the mental translation layer during implementation. Without it, developers must search the PRD for screen names, then figure out which Swift file to modify. With it, they go directly from file to mockup to spec section. Particularly valuable for new contributors who don't know the codebase's file naming conventions | Check §8 mapping table — every mockup has a corresponding Swift file; `SearchView.swift` and `OnboardingView.swift` marked as new files | → LN 33 |

| Feb 17 | M15.3 | SwiftUI/List | `List` with `.listRowBackground(Color.clear)` + `.listRowSeparator(.hidden)` preserves `.swipeActions()` while enabling fully custom card-style row rendering. `ScrollView` + `LazyVStack` would break swipe actions entirely — `.swipeActions()` is a `List`-only modifier | Add `.swipeActions` to both List and ScrollView rows — only List row shows swipe | Raw |
| Feb 17 | M15.3 | SwiftUI/SafeAreaInset | `.safeAreaInset(edge: .bottom)` participates in the safe area calculation — List/ScrollView content automatically insets to avoid the sticky bar, unlike `.overlay()` which covers content. The bar also pushes up naturally when the keyboard appears | Show keyboard with `.safeAreaInset` bottom bar — bar rises above keyboard; compare with `.overlay` — content hidden behind bar | Raw |
| Feb 17 | M15.3 | SwiftUI/FilterPill | SwiftUI idiom: filter pills should be pure views (not buttons with action closures). Callers wrap in `Button` or `Menu` as needed. This keeps the component composable — a `Menu` wrapper with long-press options can't wrap a component that's already a `Button` | Wrap FilterPill in Menu vs Button — both work cleanly because FilterPill is a pure view | Raw |
| Feb 17 | M15.3 | CoreData/Relationships | `GroceryListItem.sourceRecipes` is an `NSSet?` relationship to Recipe — not a string property. Use `addToSourceRecipes(recipe)` to link items. The computed property `sourceRecipeNames` extracts display names from the set. Always check Core Data model for relationship types before assuming string fields | Read `GroceryListItem+CoreDataProperties.swift` — `sourceRecipes: NSSet?` with generated add/remove methods | Raw |

| Feb 17 | M15.4 | SwiftUI/Scaling | Inline scale pills outperform modal scaling sheets for recipes because the user stays in context while adjusting portions. `RecipeScalingService.scale(recipe:scaleFactor:)` returns pre-formatted `ScaledIngredient.displayText` strings — the view just swaps them in when `scaleFactor != 1.0`. Combined with `presentationDetents([.medium])` for the custom picker, the full scaling UX happens without ever leaving the detail view | Change scale from 1× to 2× — ingredient quantities update inline; tap custom gear — half-sheet appears without obscuring recipe | Raw |
| Feb 17 | M15.4 | SwiftUI/DisclosureGroup | `DisclosureGroup` is ideal for de-emphasizing optional content (usage analytics) that doesn't merit a full section header. It collapses by default and the user can expand if interested. Unlike `ForagerSectionHeader` (which requires manual `@State` for collapse), DisclosureGroup manages its own expansion state | Tap "Usage" label — section expands in-place with spring animation; tap again — collapses | Raw |
| Feb 17 | M15.4 | Swift/Regex | `cleanStepText()` using `#"^(?:Step\s+)?\d+[.):]?\s*"#` strips step prefixes ("1.", "Step 2:", "3)") while leaving content intact. The `^` anchor is critical — without it, "100g flour" would lose "100". Raw string literals (`#"..."#`) avoid double-escaping backslashes | Parse "Step 3: Preheat oven" → "Preheat oven"; parse "100g flour" → "100g flour" (unchanged) | Raw |
| Feb 17 | M15.4 | Design/IngredientDisplay | Splitting ingredient display into `displayText` (quantity+unit in secondary color with `.monospacedDigit()`) and `name` (ingredient name in primary color) creates a natural two-tone visual hierarchy where quantities align vertically without needing a table layout. The bullet color doubles as a confidence indicator (green vs amber) — no extra UI element needed | View ingredients at different scales — quantity column stays aligned due to monospaced digits; low-confidence ingredients show amber bullets | Raw |

| Feb 17 | M15.5 | CoreData/Migration | Core Data v6 lightweight migration adding an optional String attribute (`quickOption`) requires no mapping model — just create a new `.xcdatamodel` with the field, update `.xccurrentversion`, and add the `@NSManaged` property. However, Xcode silently reverts `.xccurrentversion` from its in-memory state during build — re-write the file after the build if edited externally | Add new model version, build, re-read `.xccurrentversion` to verify it wasn't reverted | Raw |
| Feb 17 | M15.5 | SwiftUI/ScrollViewReader | `ScrollViewReader` with `.scrollTo(id, anchor: .center)` in `.onAppear` auto-centers the horizontal day strip on today's date. The key insight: use the date itself (formatted as ISO string) as the scroll target ID, not an index. This avoids off-by-one errors when the plan starts on a non-today date | Open plan detail where today is mid-week — strip auto-scrolls to center today | Raw |
| Feb 17 | M15.5 | SwiftUI/Menu | `Menu` is the correct control for quick-select pills (Takeout, Dining Out, etc.) — not individual buttons. A single "Choose Recipe" menu button with recipe list AND quick options creates one tap target instead of 5+ separate buttons for unplanned days. This reduces visual noise on the most common card state (unplanned day) | Tap "Choose Recipe" on empty day — menu shows recipes + quick options in one sheet | Raw |
| Feb 17 | M15.5 | Design/ReviewTriage | One-at-a-time review flows (like the IngredientReviewSheet) should show *why* each item needs attention — a reason badge ("Contains parenthetical text", "Starts with a unit/measure") gives the user context to make a faster decision. Without the reason, users must visually parse the ingredient name to figure out what's wrong | Review sheet shows reason badge per ingredient — user understands issue before reading name | Raw |
| Feb 17 | M15.5 | Design/FilterPillSemantic | Using individual `FilterPill` buttons per category (with `ForagerTheme.categoryColor`) is superior to a dropdown `Menu` for category filtering because: (1) all options are visible without a tap, (2) the color dots preview what the filter will show, (3) the pill highlights which filter is active. The tradeoff is horizontal space — `ScrollView(.horizontal)` handles overflow cleanly | Compare dropdown menu filter vs horizontal pill row — pill row communicates more state with fewer taps | Raw |

| Feb 17 | M15.5b | Xcode/SourceKit | SourceKit diagnostics report false "Cannot find type in scope" errors for types from other files (ForagerTheme, HouseholdService, ShareParticipant) when editing in isolation. These are false positives — SourceKit doesn't have the full build graph. Always trust `xcodebuild` output over SourceKit squiggles when working with large projects | Run `xcodebuild build` — BUILD SUCCEEDED confirms the types resolve correctly at compile time | Raw |
| Feb 17 | M15.5b | CoreData/Versioning | `.xccurrentversion` can silently regress when Xcode overwrites it from in-memory state — even during routine builds. Before committing, always check `git diff` for this file. A regression from v6 to v5 would break lightweight migration for users who already have v6 data | `git diff forager/forager.xcdatamodeld/.xccurrentversion` before every commit touching Core Data | Raw |
| Feb 17 | M15.5b | SwiftUI/ViewExtraction | When extracting a view from a parent (SettingsView → HouseholdView), async CKShare data that was loaded in the parent's `.task` must be re-loaded in the child's own `.task`. Don't assume the parent passes down pre-loaded state — the child view may be navigated to independently. This is why HouseholdView has its own `loadHouseholdData()` async task | Navigate to HouseholdView — member list loads asynchronously via its own `.task` | → LN 35 |

| Feb 17 | M15.6 | iOS26/GlassAPI | `Glass` type in SwiftUI has 3 members: `.regular` (standard frosted glass), `.clear` (subtle translucency), `.identity` (no effect). There is NO `.prominent` variant despite WWDC documentation suggesting it. The "prominent" concept only exists for button styles (`.buttonStyle(.glassProminent)`). For emphasized cards, use `.regular` with a larger corner radius for visual distinction | `type 'Glass' has no member 'prominent'` — compiler error confirms; check SwiftUICore.swiftinterface for actual members | → LN 37 |
| Feb 17 | M15.6 | iOS26/GlassEffect | `.glassEffect()` replaces manual shadows — glass elements create depth through light refraction, so adding both `.shadow()` and `.glassEffect()` creates visual confusion (double depth cue). Always remove `.shadow()` calls when applying glass. The glass also handles dark mode elevation automatically (no manual rim light overlay needed) | Compare card with glass-only vs glass+shadow — shadow creates an uncanny "floating over floating" effect | → LN 37 |
| Feb 17 | M15.6 | iOS26/GlassButtons | `.buttonStyle(.glass)` completely replaces custom label content styling — it overrides backgrounds, foreground colors, and borders. This makes it unsuitable for buttons with semantic color states (green active, red danger, grey neutral). Glass buttons are best for standalone, single-purpose CTAs where the glass material itself is the visual language. For inline action buttons with distinct semantic states, keep explicit ForagerTheme-based styling | Apply `.buttonStyle(.glass)` to Done/Remove buttons — semantic colors disappear, all buttons look identical | → LN 37 |
| Feb 17 | M15.6 | iOS26/TabBarMinimize | `.tabBarMinimizeBehavior(.onScrollDown)` has 4 options: `.automatic`, `.onScrollDown`, `.onScrollUp`, `.never`. The `.onScrollDown` option collapses the Liquid Glass tab bar when the user scrolls content down, maximizing screen real estate for content consumption. The bar reappears on scroll-up. Works automatically with List/ScrollView content | Scroll a List down — tab bar minimizes; scroll up — tab bar reappears | → LN 37 |

| Feb 18 | M15 | Swift/Text | iOS 26 deprecated `Text("a") + Text("b")` concatenation. The new pattern uses `LocalizedStringKey` interpolation: `Text("\(Text("a"))\(Text("b").bold())")`. Each interpolated `Text` retains its individual styling. This affects any search-match highlighting or multi-style inline text | Build with iOS 26 SDK — `+` operator on `Text` produces deprecation warning; interpolation compiles cleanly | Raw |
| Feb 18 | M15 | Swift/StringIndex | `String.Index` from `lowercased()` is NOT safe to use on the original string — Unicode characters can change byte length under case mapping (e.g., Turkish dotted-I). Use `text.range(of: highlight, options: .caseInsensitive)` directly on the original string for safe, single-pass case-insensitive range finding | Search for Turkish characters ("İstanbul") — lowercased index approach crashes; .caseInsensitive works | Raw |
| Feb 18 | M15 | CoreData/AsyncDismiss | When using `performWrite` (async background context), calling `dismiss()` synchronously after the write call makes `onError` unreachable — the view is gone before the error callback fires. Move `dismiss()` into `onSuccess` callback so errors can still be shown to the user | Delete recipe → force Core Data error → verify alert appears before navigation pop | → LN 36 |
| Feb 18 | M15 | Swift/DateFormatter | `DateFormatter` is expensive to allocate. In row views called per-item (MealPlanRowView, WeeklyListRowView), creating a formatter on every render creates dozens of allocations per scroll frame. Static lets on a `DateFormatter` extension are initialized once and thread-safe (Swift guarantees lazy initialization of static lets is atomic) | Profile with Instruments Time Profiler — DateFormatter allocation drops from N-per-frame to 0-per-frame | Raw |
| Feb 18 | M15 | CoreData/RollbackState | When reverting a failed `viewContext.save()`, capture ALL original values before mutation — not just the primary field. `item.isCompleted.toggle()` + `item.dateCompleted = nil` on revert loses the original `dateCompleted` date. Pattern: `let orig = item.dateCompleted` before mutation, restore in catch | Toggle completed item off → force save error → verify dateCompleted is restored to original value | → LN 36 |

| Feb 20 | M15 | CoreData/FetchRequest | `@FetchRequest` observes direct attribute changes on its entity but NOT relationship-derived changes. `GroceryListItem.categoryName` reads through `ingredientTemplate?.category?.name` — when you recategorize a template, the item's category changes but the item entity itself is untouched. SwiftUI sees no change. Fix: `viewContext.refreshAllObjects()` forces Core Data to re-fault all objects, making relationships re-evaluate on next access | Recategorize item → navigate back to list overview → verify category chips update (requires refreshAllObjects) | → LN 35 |
| Feb 20 | M15 | Parser/TemplateSanitization | Template name corruption has three independent causes: (1) `normalize()` lacks leading-punctuation stripping ("/ black pepper"), (2) `extractCleanIngredientName()` regex uses `\d+` which splits fractions (1/4 → strips "1", leaves "/"), (3) missing count units (cloves, cans) in regex patterns. All three must be fixed independently — normalize() is the chokepoint for all template creation, but extractCleanIngredientName() runs in view-level matching code that bypasses the service | Add "1/4 tsp black pepper" → template should be "black pepper"; add "3 cloves garlic" → template should be "garlic" | Raw |
| Feb 20 | M15 | Swift/Dictionary | `Dictionary(uniqueKeysWithValues:)` is a fatal assertion — it crashes if ANY keys are duplicated. In multi-device CloudKit sync, `Category` entities can temporarily duplicate before `CategoryDeduplicator` converges. Using `Dictionary(_:uniquingKeysWith: { first, _ in first })` handles collisions gracefully by keeping the first value. Found in `AddIngredientsToListView.sortedCategoryNames` and `RecipeScalingView.sortedCategoryNames` | Crash report: `specialized _NativeDictionary.init<A>(uniqueKeysWithValues:)` in Thread 0 | Raw |
| Feb 20 | M15 | SwiftUI/ViewIdentity | `.id(stateVar)` + mutating `stateVar` in `.onAppear` creates an infinite loop: SwiftUI treats the new ID as a new view → destroys and recreates → `.onAppear` fires → new ID → repeat. This is a crash, not just a performance issue. If you need to force a view refresh, `refreshAllObjects()` or `@State` toggle without `.id()` is safe. Only use `.id()` with values that change from *outside* the view (parent state, model changes) | Add `.id(UUID())` in .onAppear → app freezes/crashes immediately on that view | → LN 35 |
| Feb 20 | M15 | SwiftUI/EditRecipe | `EditRecipeView.completeSave()` must mirror `CreateRecipeView` exactly for structured fields. When you delete and recreate Ingredient entities (the edit pattern), any fields not explicitly set are zero/nil — unlike updates where existing values persist. This makes edit-save a stricter code path than create-save | Edit recipe → verify displayText, numericValue, standardUnit all populated after save | → LN 36 |
| Feb 20 | M15 | CoreData/TemplateDuplication | Template deduplication requires TWO passes: (1) normalize names, (2) group by canonicalName and merge collisions. The original `migrateExistingTemplates()` only did pass 1 — renaming templates but never checking if two now share the same canonical. Also, `canonicalName` (simple lowercase) diverged from `normalize()` (4-phase pipeline), so templates created at different times could have matching names but different canonicals. Fix: always sync `canonicalName` from the normalized name | After migration, query `SELECT canonicalName, COUNT(*) FROM IngredientTemplate GROUP BY canonicalName HAVING COUNT(*) > 1` — should return 0 rows | → LN 36 |
| Feb 20 | M15 | CoreData/GroceryItemName | `GroceryListItem.name` is a display field, not an identifier. The original code stored template names ("garlic") which worked with a separate displayText column. After removing that column, `name` must contain the full ingredient text ("2 cloves garlic") for the quantity to be visible. Template name is used for matching/merging via a separate `cleanName` variable | Add ingredients from recipe → grocery items should show full text with quantities, not just ingredient names | → LN 36 |

| Feb 20 | M15 | UX/PatternReuse | The Ingredients tab's three-layer quality-check pattern (warning banner → filter pill → guided sheet) is a reusable framework. Duplicate detection was built on the same pattern as "needs review" with zero new interaction concepts — users already understand the flow. The only new verb is "merge" instead of "review". This pattern extends to any future quality check (orphaned templates, missing categories, etc.) | Add duplicates → verify banner appears → tap "Review Now" → guided merge sheet | Raw |
| Feb 20 | M15 | SwiftUI/CodeDuplication | Copy-pasted helper functions across views (like `categoryEmoji()` duplicated in 4 structs) create maintenance debt — when removing or changing behavior, every copy must be found via project-wide grep. Better pattern: single static method on theme struct or computed property on the entity. ForagerTheme.categoryColor(for:) already follows this pattern correctly. | `grep -r "categoryEmoji" forager/` should return 0 results | → ADR 012 |
| Feb 20 | M15 | UX/TwoStepCreation | Two-step creation flows (create entity with defaults → show dialog for user input) must backfill Step 1's entity with Step 2's data. Quick-add created GroceryListItem with `defaultCategory` (Produce), then showed template dialog where user picked real category — but never wrote it back. Fix: hold `lastAddedItem` reference, update in `saveToTemplates()`. | Quick-add "Fruit Snacks" → assign to "Snacks" in dialog → should appear in Snacks section, not Produce | → ADR 012 |
| Feb 20 | M15 | Parsing/ConfidenceSemantics | `parseConfidence` has three semantic levels: 0.0 = "no quantity attempted" (name-only, no warning), >0 && <0.7 = "parser tried but uncertain" (warning badge), >=0.8 = "user-validated" (recipe-sourced, autocomplete-selected). Floor confidence at 0.8 for any user-validated path to prevent false warnings. | Add "cucumber" via autocomplete → no warning. Add "2 clv garlic" → may warn. | → ADR 012 |

| Feb 20 | M8.4 | ML/BiLSTMvTransformer | BiLSTM-CRF is the right architecture for on-device ingredient parsing: 2-5 MB model size (vs 50-100 MB transformer), <5ms inference (vs 20-50ms), and 260k training sentences is excellent for BiLSTM but marginal for transformers. The tradeoff: BiLSTM has no attention mechanism, so very long inputs (>30 tokens) with distant dependencies may suffer — but ingredient lines rarely exceed 15 tokens | Compare model sizes and inference times after training both architectures on the same dataset | Raw |
| Feb 20 | M8.4 | ML/BIOTagging | BIO (Begin-Inside-Outside) sequence labeling is superior to flat classification ("2.0\|cup\|flour") for ingredient parsing because: (1) handles variable-length multi-word names naturally, (2) captures token-level boundaries between qty/unit/name/comment, (3) model learns from position context rather than memorizing formats. The original M8.4 plan's Text Classifier approach would fail on "1/4 tsp freshly ground black pepper" where "freshly ground black pepper" spans 4 tokens | Train both approaches on same dataset — BIO handles "freshly ground black pepper" as contiguous NAME span; classifier can't separate it | Raw |
| Feb 20 | M8.4 | ML/DatasetBootstrapping | Open-source datasets (NYT 180k + strangetom 81k = 260k labeled sentences) eliminate the cold-start problem for ML parsing. The original M8.4 plan required 100+ user corrections before training — meaning the model couldn't help until users had already suffered through hundreds of bad parses. With dataset bootstrapping, the model is production-ready from day one | Compare model accuracy at 0 user corrections (bootstrap only) vs requiring 100+ corrections | Raw |
| Feb 20 | M8.4 | Architecture/ParserProtocol | The M8.3 `IngredientParser` protocol was designed for exactly this ML integration. `ParserResult` already has `parserUsed: String` for telemetry, `HybridIngredientParser` already routes by confidence threshold, and `IngredientParsingService` delegates without knowing which parser runs. Adding `MLIngredientParser` requires zero protocol changes and zero changes to the public API — it slots into the router as a third option between regex and NLP | Build with MLIngredientParser — IngredientParsingService callers compile with no changes; telemetry shows "ml" in parserUsed field | Raw |

| Feb 20 | M7.5 | Architecture/ServiceDI | Services that accept `IngredientParsingService` as an init parameter (rather than creating it internally) become forward-compatible with M9.5-partial DI and M8.4 ML parser integration — when the parser swaps, the service layer doesn't change. This is a thin DI pattern: one init parameter, one stored property, zero new abstractions | Create RecipeService with injected parser → swap parser at init → verify service delegates to injected instance | Raw |
| Feb 20 | M7.5 | PRD/StaleAudit | PRD audits should verify entity names against actual Core Data model files, not trust the PRD's own code samples. The original M7.5 PRD referenced `RecipeIngredient` (doesn't exist), `displayName` on IngredientTemplate (correct is `name`), `cloudKitUserID` on HouseholdMember (doesn't exist), and `quantity`/`unit` on Ingredient (correct is `numericValue`/`standardUnit`). All 5 were wrong | `grep -r "RecipeIngredient\|\.displayName\|cloudKitUserID\|\.quantity\|\.unit" docs/prds/m7.5*.md` — should return 0 matches in corrected PRD | Raw |
| Feb 20 | M7.5 | Architecture/DescopeReasoning | Enum routing for views with 1-4 booleans adds pure ceremony (enum definition, Identifiable conformance, id computation) with no practical benefit. The threshold for enum routing is 6+ booleans, where the combinatorial explosion of boolean states makes reasoning about which sheets can co-present difficult. Below that threshold, booleans are fine — SwiftUI's `.sheet(isPresented:)` is simpler and more readable | Count booleans per view — views with 6+ get enum routing, views with 1-5 keep booleans | Raw |

| Feb 20 | M7.5 | Architecture/EstimateAccuracy | M7.5 estimated 14-19h but completed in ~5h. M15's visual refresh had already simplified navigation patterns (enum routing partially done) and eliminated many direct saves (views were rewritten cleaner), making the architecture hardening significantly cheaper. Lesson: doing a visual refresh first made the structural cleanup trivial — validates the decision to reorder M15 before M7.5 | Compare PRD estimates with actual hours in roadmap.md; check git diff stats for reduced scope vs original PRD expectations | Raw |

| Feb 21 | M9.0 | CloudKit/DeprecationNoReplacement | Apple deprecated `discoverUserIdentity` and `userIdentity(forUserRecordID:)` in iOS 17 with **no replacement API** — a rare "just remove it" deprecation motivated by privacy. The `nameComponents` property already returned nil for the current user on iOS 16+, making these APIs both deprecated AND unreliable. When the fallback path is the common path, the deprecated call is dead weight — remove it entirely rather than migrating to a "modern" version that doesn't exist | Build with deprecated calls removed → household creation still works; display name comes from user input during creation, not API | Raw |
| Feb 21 | M9.0 | Swift/MainActorAwait | When a class is `@MainActor` and calls a `@MainActor` closure, `await` is unnecessary — both are already on the same actor, so there's no actor hop. This commonly appears after refactoring to class-level actor isolation: existing `await` calls on MainActor closures become redundant warnings. The fix is simply removing `await`, not restructuring the concurrency model | Remove `await` from `onStatus()` calls in `@MainActor` class → warning resolves, behavior unchanged | Raw |
| Feb 21 | M9.0 | PRD/StalenessPattern | PRDs written >2 weeks before implementation are reliably stale in specific ways: (1) warning lists miss new warnings introduced by intervening milestones, (2) fix descriptions assume APIs that have since changed, (3) time estimates include research that's already been done during planning. The M9 PRD's Phase 0 missed 3 GroceryListDetailView warnings, listed MealPlanDetailView warnings that no longer existed, and estimated 2-3h for work that took <1h. Always do a fresh build audit before implementing from a stale PRD | Run `xcodebuild clean build | grep warning:` before starting PRD-guided work; compare with PRD's warning list | Raw |

| Feb 21 | M9.0.1 | SwiftUI/FocusState | `@FocusState` supports custom `Hashable` types, not just `Bool`. Using `@FocusState private var focusedSearchDate: Date?` with `.focused($focusedSearchDate, equals: date)` elegantly handles "only one field active at a time" across multiple instances of the same view in a `ForEach`. The `nil` state means nothing is focused. Much cleaner than tracking a separate `activeDate` state variable and keeping it synchronized | Create 3+ TextFields with Date-based focus — only one can be active; switching clears the other | Raw |
| Feb 21 | M9.0.1 | SwiftUI/InlineAutocomplete | Inline autocomplete in a `LazyVStack` inside `ScrollView` works naturally — search results expand the card's height and SwiftUI recalculates layout as results appear/disappear. The key design decision is limiting results (e.g., `.prefix(5)`) to prevent a single day card from dominating the scroll view. This is preferable to a modal sheet for quick-access patterns because the user stays in context | Type in inline search field → card expands smoothly as results appear; select result → card contracts back | Raw |

| Feb 21 | M9.1.2 | Architecture/StaticParser | `static let` on a class property is thread-safe in Swift (lazy atomic init via `dispatch_once`). When the underlying type holds only `let` properties and `parse()` creates no shared mutable state, a single static instance can safely serve all call sites without synchronization. This enabled `IngredientParsingService.extractCleanIngredientName(from:)` as a static utility that delegates to a shared `HybridIngredientParser` | Verify `HybridIngredientParser` has no `var` properties; call from multiple threads — no data races | Raw |
| Feb 21 | M9.1.2 | Parser/ViewLayerDivergence | View-layer reimplementations of parser logic diverge silently. AddIngredientsToListView had 40 lines of regex (7 pattern groups + qualifier stripping), while MealPlanDetailView had 18 lines (2 pattern groups, no qualifier stripping). The weaker version produced junk templates like "Salt To Taste" instead of "Salt". Centralizing to a single `static` method that delegates to the parser eliminates this class of bug permanently | Compare template names before/after: "salt to taste" → "Salt To Taste" (old) vs "Salt" (new) | Raw |
| Feb 21 | M9.5 | Architecture/DualParserInstances | `IngredientParsingService` has two independent `HybridIngredientParser` instances: instance-level (`private let parser`) used by `parseIngredient()`/`parseToStructured()`, and static-level (`private static let sharedParser`) used by `extractCleanIngredientName()`. DI for the instance parser doesn't affect the static one. The static method is a pure text utility — its shared parser doesn't need injection (testing via integration tests is sufficient), but this dual-instance pattern should be documented to prevent confusion during M8.4 | Grep for `HybridIngredientParser()` — should appear in exactly 2 places in IngredientParsingService | Raw |
| Feb 21 | M9.5 | Architecture/PRDPhaseConflict | M8.4 PRD Phase 5 shows `MLIngredientParser` hardcoded in `HybridIngredientParser.init()` — which contradicts M9.5's goal of injectable parsers. When prerequisites and features are planned by different sessions, their implementation details can conflict. Resolution: M9.5-partial makes existing parsers injectable; M8.4 adds the optional `mlParser` slot and routing changes. Cross-reference both PRDs before implementing either | Compare M8.4 Phase 5 code sample against M9.5 target state — should use init parameter, not hardcoded property | Raw |
| Feb 21 | M9.5 | Architecture/DefaultParamBackcompat | Swift default parameter values (`parser: IngredientParser = HybridIngredientParser()`) make DI refactoring zero-blast-radius: all 11 existing call sites compile unchanged, while tests gain the ability to inject mocks. This is the "thin DI" pattern — one init parameter, one stored property, zero new abstractions. Unlike full DI containers (ServiceFactory, Environment injection), thin DI adds testability without ceremony | Add `parser:` param with default to init → verify all callers compile without changes → write one mock test | Raw |

---

| Feb 21 | M8.4 | Process/ExternalReview | Using a separate AI agent (Codex) as a "principal engineering reviewer" on a PRD catches real engineering gaps (CRF decode parity, double-parsing, missing start/end transitions) that the author misses. The review found 11 issues — 5 high severity — that would have surfaced as bugs mid-implementation. The key was treating review findings as concrete PRD edits with acceptance criteria, not just acknowledgments | Compare PRD v1 vs v2 diff — every finding has a corresponding spec change and acceptance criterion | Raw |
| Feb 21 | M8.4 | ML/CRFDecoding | CRF Viterbi decoding requires THREE parameter sets: the 7×7 transition matrix, the 1×7 start-of-sequence transitions, and the 1×7 end-of-sequence transitions. Omitting start/end transitions produces different label sequences than the training-time CRF decoder, even when emission scores are identical. This is a common gap in "simplified" Viterbi implementations | Run Python CRF decode vs Swift decode on 1000 samples — with all 3 params, ≥99.9% agreement; without start/end, agreement drops significantly | Raw |
| Feb 21 | M8.4 | ML/WordVsChar | Character-level features (char CNN/LSTM) in ingredient parsing add dual-input CoreML conversion complexity and per-token character ID preprocessing for marginal accuracy gain. Ingredient vocabulary is distinctive at the word level — "cups", "tsp", "garlic" are unambiguous words that don't need character-level decomposition. Word-only BiLSTM halves the model I/O contract complexity | Train word-only vs word+char models on same dataset — compare accuracy difference vs conversion/preprocessing complexity | Raw |
| Feb 21 | M8.4 | Architecture/DoubleParse | `parseAndConnectIngredients()` calls `parseIngredient()` and `parseToStructured()` sequentially — both internally invoke `parser.parse()`. With regex-only this is microseconds wasted; with ML inference in the chain, it doubles the expensive operation. The fix is calling `parser.parse()` once and mapping the single `ParserResult` to both output formats. Always audit callsite multiplication before adding expensive inference to an existing pipeline | Grep for `parser.parse` call count inside `parseAndConnectIngredients` — should be exactly 1 after refactor | Raw |
| Feb 21 | M8.4 | Process/CorrectionDataGate | A retraining pipeline without production correction data flowing in is an empty pipeline. `logCorrection()` existed since M8.1 (with tests) but no production edit flow called it. Gate retraining claims on minimum correction corpus (≥50 linked corrections) to prevent shipping an unused capability. Wire the data collection before building the consumption pipeline | Export corrections via debug menu → count should be >0 after user has edited ingredients in production use | Raw |

| Feb 21 | M8.4 | CoreML/Warmup | CoreML performs JIT compilation on first `prediction()` call — 100-500ms spike. This can freeze the main thread if the ML parser is first invoked during user interaction. Fix: background warmup prediction during app initialization via `DispatchQueue.global(qos: .utility).async { _ = parse("warmup") }`. The `static let sharedParser` lazy init triggers the warmup on first access | Time first vs second `prediction()` call — first is 100-500ms, second is <5ms | Raw |
| Feb 21 | M8.4 | CoreML/MLMultiArray | `MLMultiArray` element access returns `NSNumber`. Neural Engine returns Float16, CPU returns Float32 — always use `.floatValue` accessor which handles both. Direct pointer access via `dataPointer` is faster but requires explicit stride calculation: `emissions.strides[dim].intValue` for each dimension. The subscript-based `[[0, t, j] as [NSNumber]]` is safer and sufficient for <64-token inputs | Compare `.floatValue` on Neural Engine vs CPU outputs — both return correct Float values. Profile subscript vs pointer access — negligible difference for short sequences | Raw |
| Feb 21 | M8.4 | CoreML/RangeDim | `ct.RangeDim(1, 64)` declares dynamic input shapes in CoreML — the model accepts variable-length inputs without padding. However, non-default shapes (anything ≠ the "default" size set during export) fall back from Neural Engine to CPU. Do NOT right-pad to a fixed size — pass actual token count and accept CPU execution for non-default lengths. Padding wastes compute and can introduce noise | Export with `RangeDim(1, 64)`, predict with length 5 vs length 64 — check MLComputeDevice in output; non-default lengths report CPU | Raw |
| Feb 21 | M8.4 | Swift/NFKD | `String.decomposedStringWithCanonicalMapping` gives NFD (canonical decomposition), NOT NFKD (compatibility decomposition). For tokenizer normalization that maps "½" → "1/2" and "ﬁ" → "fi", you need NFKD: `text.applyingTransform(.init("NFKD"), reverse: false)`. This is a common confusion — the property name sounds like it should work for all decomposition, but NFD only separates combining characters | Compare "½".decomposedStringWithCanonicalMapping (still "½") vs "½".applyingTransform(.init("NFKD"), reverse: false) (becomes "1⁄2") | Raw |
| Feb 21 | M8.4 | CoreML/SilentFailure | `MLIngredientParser.init?()` returns nil if the CoreML model bundle is missing or corrupted — no crash, no error message. When routed through `HybridIngredientParser` with `mlParser: IngredientParser? = nil`, all ML tests silently pass via fallback path. Fix: add `#if DEBUG` warning log on nil init + model presence guard test (`XCTAssertNotNil(MLIngredientParser())`) that fails loudly if the model is absent from the test bundle | Remove .mlpackage from bundle → run tests → without guard, all pass silently; with guard, one test fails explaining why | Raw |

| Feb 21 | M8.4 | ML/DatasetDeduplication | strangetom's 81,316 rows contain 12,470 exact duplicates (15.3%). Without deduplication before splitting, duplicates leak between train/val/test sets, inflating evaluation metrics. Deduplicate by sentence text before splitting — reduced to 68,846 unique samples. This is a standard ML hygiene step but easily overlooked when the source dataset doesn't document its duplicate rate | Count `SELECT sentence, COUNT(*) FROM en GROUP BY sentence HAVING COUNT(*) > 1` — should match dedup count. Verify zero sentence overlap between train/val/test after splitting | Raw |
| Feb 21 | M8.4 | ML/FractionDecoding | strangetom encodes fractions as `#num$den` (simple) and `whole#num$den` (mixed) — e.g., `#1$2` = 0.5, `1#1$2` = 1.5. Using `re.sub` with three ordered passes (mixed → prefixed → simple) handles these patterns inline, including when embedded in ranges like `1$2-#3$4` or suffixed tokens. The ordering matters: mixed fractions must match before simple fractions to prevent `1#1$2` from matching `1$2` first | Run decode_fraction on `"1#1$2"` → `"1.5"`, `"#3$4"` → `"0.75"`, `"1$2-3$4"` → `"0.5-0.75"` | Raw |
| Feb 21 | M8.4 | Architecture/SingleParseRefactor | `parseAndConnectIngredients()` previously called both `parseIngredient()` and `parseToStructured()`, each internally invoking `parser.parse()` — doubling the expensive operation. The `parseUnified()` refactor calls `parseCore()` once and maps the single `ParserResult` to both `ParsedIngredient` and `StructuredQuantity`. This is mandatory prep before adding ML inference — without it, every recipe import would run the model twice per ingredient | Grep for `parser.parse` calls inside IngredientParsingService — should be exactly 1 in `parseCore()` after refactor | Raw |
| Feb 21 | M8.4 | ML/StratifiedSplitting | When a dataset has multiple sources (NYT, AllRecipes, BBC, Cookstr, TC) with different distributions and writing styles, random splitting can over-represent one source in the test set. `train_test_split(stratify=sources)` ensures each split has proportional representation from every source. For 68,846 samples across 5 sources, this prevents evaluation bias from source-specific patterns (e.g., NYT uses more complex modifiers than AllRecipes) | Compare `Counter(s["source"] for s in test)` proportions against full dataset proportions — should be within 1% for each source | Raw |
| Feb 21 | M8.4 | ML/LabelMapping | Mapping 13 fine-grained labels to 7 coarser labels (e.g., B_NAME_TOK + I_NAME_TOK + NAME_VAR + NAME_SEP → NAME) is a one-way information loss. The mapping must be done at dataset preparation time, not training time, so the training data format matches the production inference format. Keeping the mapping as an explicit dict (`LABEL_MAP`) rather than regex-based rules makes it auditable and prevents silent misclassification when source labels change | Verify every strangetom label appears in LABEL_MAP keys — unknown labels should raise an error, not silently default | Raw |

**Last Updated**: February 21, 2026 (session 26 — M8.4 Phase 0+1, dataset preparation insights)
